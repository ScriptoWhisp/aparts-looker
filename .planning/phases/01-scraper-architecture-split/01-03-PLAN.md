---
phase: 01-scraper-architecture-split
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - app/agent_job.py
  - app/requirements.txt
  - app/tests/__init__.py
  - app/tests/conftest.py
  - app/tests/test_ingest.py
  - app/tests/test_heartbeat.py
  - app/tests/test_listing_contract.py
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "After two consecutive POST /api/heartbeat calls with `listing_count: 0`, the next scheduler tick calls telegram_client.send_message with a message containing the words 'Scraper returned 0 listings' and the last-heartbeat timestamp (per ARCH-04)"
    - "When `last_heartbeat_ts` is older than `max(HEARTBEAT_TIMEOUT_HOURS, CHECK_INTERVAL_HOURS * 2 + 0.5)` hours, the next scheduler tick calls telegram_client.send_message with a message containing 'Scraper offline' and the stale timestamp (per D-10)"
    - "When `last_heartbeat_ts` is None (no baseline ever received), no offline alert fires — the check exits early (per RESEARCH Pitfall 4)"
    - "A heartbeat with `listing_count > 0` resets `consecutive_zero_count` to 0 (already implemented in plan 01-02 handle_heartbeat; test proves the behavior)"
    - "Both alerts respect an anti-fatigue cooldown: `last_scraper_alert_sent_at` is set on send; alerts do not re-fire within 24h of the previous send (per RESEARCH Open Question 2)"
    - "run_check() in app/agent_job.py calls process_send_commands(state), then check_heartbeat_timeout(state), then check_consecutive_zeros(state) — in that order, all inside the existing try/except/finally block"
    - "pytest app/tests/ -v exits 0 (all listed test files run green with the mocked telegram_client and mocked agent_state fixtures)"
  artifacts:
    - Updated app/agent_job.py (check_heartbeat_timeout, check_consecutive_zeros functions; run_check wired)
    - Updated app/requirements.txt (adds pytest, httpx)
    - app/tests/__init__.py (empty package marker)
    - app/tests/conftest.py (TestClient fixture + patch_ingest_token + tmp_agent_state fixtures)
    - app/tests/test_ingest.py (auth 200/403 + batch processing + no-playwright-import assertions)
    - app/tests/test_heartbeat.py (storage, consecutive zeros, offline timeout, no-baseline guard, cooldown)
    - app/tests/test_listing_contract.py (round-trip serialization, unknown-field tolerance)
  key_links:
    - "check_consecutive_zeros reads state['consecutive_zero_count'] (populated by plan 01-02 handle_heartbeat) and state['last_heartbeat_ts']"
    - "check_heartbeat_timeout reads state['last_heartbeat_ts'] and computes threshold = HEARTBEAT_TIMEOUT_HOURS if > 0 else CHECK_INTERVAL_HOURS * 2 + 0.5"
    - "Both checks call telegram_client.send_message directly; both set state['last_scraper_alert_sent_at'] before returning so the outer save_agent_state persists the cooldown timestamp"
    - "run_check's try/except/finally block already calls data_store.save_agent_state(state) in `finally` — no additional plumbing needed; the two new functions only mutate `state`"
---

<objective>
Close the loop on ARCH-04 — the scraper health alert. Two conditions trigger a Telegram message: (a) `consecutive_zero_count >= 2` (scraper is alive but finding nothing — kv.ee blocking or search URL returned no results); (b) the last heartbeat is older than `max(HEARTBEAT_TIMEOUT_HOURS, CHECK_INTERVAL_HOURS * 2 + 0.5)` hours (scraper is silent — mini PC or Docker crashed). Both checks run on every scheduler tick from within `agent_job.run_check`. This plan also lands the test scaffold that plan 01-02's ingest/heartbeat handlers assume (pytest + httpx + FastAPI TestClient), making the phase's Validation Strategy real instead of aspirational.

Purpose: Give Daniel a Telegram alert when the mini PC scraper stops working — either because Docker/mini PC is down (heartbeat timeout) or because kv.ee started blocking / the search returns 0 results (consecutive zeros). Together with the alert cooldown, this satisfies ARCH-04's "notification if 0 listings are returned for 2 consecutive runs" without spamming Daniel every 2 hours while the outage persists.

Output: Two new functions in `agent_job.py`, wired into `run_check()`; two new dependencies (pytest, httpx) in `app/requirements.txt`; and the full pytest scaffold declared in the phase Validation Strategy (`app/tests/__init__.py`, `conftest.py`, and three test modules covering ARCH-02/03/04). This depends on plan 01-02's `ingest_handler.handle_heartbeat` (which writes the state this plan reads) and plan 01-02's `_verify_ingest_token` (which the auth tests here exercise) — Wave 2 depends on 01-02.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scraper-architecture-split/01-CONTEXT.md
@.planning/phases/01-scraper-architecture-split/01-RESEARCH.md
@.planning/phases/01-scraper-architecture-split/01-PATTERNS.md
@.planning/phases/01-scraper-architecture-split/01-VALIDATION.md
@app/agent_job.py
@app/config.py
@app/data_store.py
@app/main.py
@app/telegram_client.py
@app/kv_listing_parser.py
</context>

## Phase Goal

**As a** Daniel (running the apartment-hunting system), **I want to** receive a Telegram alert within a few hours of the scraper going silent or returning zero listings twice in a row, **so that** I know to fix the mini PC or the kv.ee search URL before I miss listings for days.

<artifacts_produced>
## Artifacts this phase produces

**Updated file:**
- `app/agent_job.py` — adds `check_heartbeat_timeout(state: dict) -> None` and `check_consecutive_zeros(state: dict) -> None`; `run_check()` calls both after `process_send_commands`

**Updated file:**
- `app/requirements.txt` — appends `pytest>=8.0.0` and `httpx>=0.27.0`

**New files:**
- `app/tests/__init__.py` — empty package marker
- `app/tests/conftest.py` — pytest fixtures: `client` (FastAPI TestClient with a patched INGEST_TOKEN), `tmp_agent_state` (redirects config.AGENT_STATE_FILE to a temp path so tests can't corrupt real state), `mock_telegram` (monkeypatches telegram_client.send_message + send_photo to record calls)
- `app/tests/test_ingest.py` — POST /api/ingest returns 200 with valid token; 403 without token; 403 with wrong token; batch of one Listing dict is processed (evaluate_listing mocked); assertion that ingest_handler module does not have `playwright` in its imports
- `app/tests/test_heartbeat.py` — heartbeat POST stores fields; two zero-count POSTs then a scheduler tick fires an alert; non-zero POST resets counter; timeout alert fires when last_heartbeat_ts is stale; no alert fires when last_heartbeat_ts is None; both alerts respect 24h cooldown
- `app/tests/test_listing_contract.py` — Listing → dataclasses.asdict → _deserialize_listing round-trips preserving all fields; a dict with an unknown extra key deserializes without raising

**New symbols:**
- `agent_job.check_heartbeat_timeout(state: dict) -> None`
- `agent_job.check_consecutive_zeros(state: dict) -> None`
- `agent_job._alert_cooldown_ok(state: dict, now_iso: str) -> bool` (helper: returns True if `last_scraper_alert_sent_at` is None or older than 24h; used by both check functions to gate re-firing)

**New environment variables:** none (uses INGEST_TOKEN + HEARTBEAT_TIMEOUT_HOURS + CHECK_INTERVAL_HOURS from plan 01-02 / existing config)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pytest + httpx to requirements.txt and scaffold app/tests/ (init, conftest, three test files with RED stubs)</name>
  <files>app/requirements.txt, app/tests/__init__.py, app/tests/conftest.py, app/tests/test_ingest.py, app/tests/test_heartbeat.py, app/tests/test_listing_contract.py</files>
  <read_first>
    - app/requirements.txt (current pinning style — >= lower-bound pins)
    - app/main.py (FastAPI app instance to import in TestClient fixture; the app.mount for static files means TestClient needs a `static/` directory to exist — either point mount at a temp dir in tests or accept the mount noise)
    - app/config.py (INGEST_TOKEN + AGENT_STATE_FILE — fixtures monkeypatch these)
    - app/data_store.py (load_agent_state / save_agent_state — the redirect fixture points AGENT_STATE_FILE at tmp_path so tests don't touch real data)
    - app/kv_listing_parser.py (Listing dataclass — round-trip test constructs an instance and calls dataclasses.asdict)
    - app/ingest_handler.py (created by plan 01-02 — process_ingest_batch, _deserialize_listing, handle_heartbeat; the tests exercise these directly and via HTTP)
    - app/telegram_client.py (send_message signature — mock_telegram fixture patches this)
    - .planning/phases/01-scraper-architecture-split/01-VALIDATION.md (Wave 0 Requirements — this task IS Wave 0)
    - .planning/phases/01-scraper-architecture-split/01-PATTERNS.md (app/tests/conftest.py analog + test file analogs)
    - .planning/phases/01-scraper-architecture-split/01-RESEARCH.md (Validation Architecture section — quick run command, framework)
  </read_first>
  <behavior>
    - `pip install -r app/requirements.txt` inside the app container installs pytest and httpx alongside existing deps (no version conflicts)
    - `pytest app/tests/ -v --collect-only` reports collection of at least the eight test functions listed in the Per-Task Verification Map of the VALIDATION strategy
    - The `client` fixture yields a `fastapi.testclient.TestClient(app)` with `config.INGEST_TOKEN` monkeypatched to `"test-token-abc"`
    - The `tmp_agent_state` fixture creates a temp directory, monkeypatches `config.AGENT_STATE_FILE` to point at `<tmpdir>/agent_state.json`, and yields the tmpdir path; tests use this to avoid mutating the real agent_state.json
    - The `mock_telegram` fixture monkeypatches `telegram_client.send_message` and `telegram_client.send_photo` with `unittest.mock.MagicMock` and yields the mock so tests can assert `mock_telegram.assert_called_once()` etc.
    - All test functions are RED-scaffolded — they are declared with the required assertions from VALIDATION.md but may xfail or skip until Task 2 lands the check functions (marked with `@pytest.mark.skip(reason='pending Task 2')` where appropriate so `pytest app/tests/` still exits 0 in this task)
  </behavior>
  <action>
Edit app/requirements.txt: append `pytest>=8.0.0` and `httpx>=0.27.0` on new lines after the existing entries. Follow the existing `>=` lower-bound pinning style (do not exact-pin, do not use `~=`).

Create empty app/tests/__init__.py — literally a zero-byte file (or a single-line comment). The presence of the file makes app/tests/ a package so pytest can discover modules by relative path.

Create app/tests/conftest.py with the three fixtures below.

Fixture `client`: `@pytest.fixture def client(monkeypatch, tmp_path): monkeypatch.setattr(config, "INGEST_TOKEN", "test-token-abc"); ...`. Because main.py mounts StaticFiles at `/`, the FastAPI app import will fail if the `static/` directory does not exist — check current app/ for a `static/` folder; if it does not exist inside the executor's cwd, do NOT create one — instead, before importing main, monkeypatch the app.mount call by importing FastAPI directly and building a minimal app for testing (or use `sys.path` manipulation to import `main` from repo root where `static/` exists). Simplest workable path: `sys.path.insert(0, "app")` then `from main import app`; TestClient(app) — if the static mount fails at import due to missing directory, create app/static/ with a placeholder `.gitkeep` file inside conftest before the import (this is a test-time workaround and is documented in the fixture docstring). Yield `TestClient(app)`.

Fixture `tmp_agent_state`: takes `monkeypatch, tmp_path`; sets `state_file = tmp_path / "agent_state.json"`; monkeypatches `config.AGENT_STATE_FILE = str(state_file)`; also monkeypatches `config.APP_DATA_FILE = str(tmp_path / "app_data.json")` so add_property_if_new doesn't corrupt real data; yields state_file.

Fixture `mock_telegram`: takes `monkeypatch`; `mock_send = MagicMock(); mock_photo = MagicMock(); monkeypatch.setattr("telegram_client.send_message", mock_send); monkeypatch.setattr("telegram_client.send_photo", mock_photo)`. Also monkeypatch these on the modules that import them by name (agent_job imports send_message at module scope; ingest_handler imports send_message and send_photo at module scope) — patch `agent_job.send_message`, `ingest_handler.send_message`, and `ingest_handler.send_photo` too so the in-module references pick up the mock. Yield a namespace object exposing `send_message=mock_send` and `send_photo=mock_photo`.

Create app/tests/test_ingest.py with these RED-scaffolded functions (each contains an assertion that will pass once plan 01-02's endpoints are live — this plan runs AFTER 01-02, so the tests should GREEN immediately after Task 1 completes):

  - `test_missing_auth(client)`: `resp = client.post("/api/ingest", json=[])` → assert `resp.status_code == 403` (per VALIDATION 1-02-01, ARCH-02).
  - `test_wrong_token(client)`: `resp = client.post("/api/ingest", json=[], headers={"Authorization": "Bearer wrong"})` → assert `resp.status_code == 403` (per VALIDATION 1-02-02, ARCH-02).
  - `test_ingest_batch(client, tmp_agent_state, mock_telegram, monkeypatch)`: monkeypatch `ingest_handler.evaluate_listing` to return `{"score": 80, "verdict": "ok", "should_draft_email": False}`; POST a single-element JSON array `[{"id": "test-1", "url": "https://www.kv.ee/test-1.html", "title": "Test", "price_eur": 200000, "rooms": 3, "area_sqm": 60.0, "image_count": 10, "raw_ok": true}]` with `Authorization: Bearer test-token-abc`; assert 200; assert `mock_telegram.send_message.called or mock_telegram.send_photo.called` (per VALIDATION 1-02-03, ARCH-03).
  - `test_no_playwright_import()`: `import ingest_handler; import sys; assert "playwright" not in [k for k in dir(ingest_handler) if not k.startswith("_")]` and additionally `import ast; tree = ast.parse(open("app/ingest_handler.py").read()); imports = [n for n in ast.walk(tree) if isinstance(n, (ast.Import, ast.ImportFrom))]; names = set(); [names.add(a.name if isinstance(n, ast.Import) else n.module) for n in imports for a in getattr(n, "names", [])]; assert "playwright" not in names and "kv_alert_reader" not in names and "kv_scraper" not in names` — proves ARCH-03 via static analysis.

Create app/tests/test_heartbeat.py with these RED-scaffolded functions (mark heartbeat-storage as ready-to-green after 01-02; mark timeout/cooldown/consecutive-zero-alert as `@pytest.mark.skip(reason="wired in Task 2 of plan 01-03")` so pytest exits 0 in Task 1):

  - `test_heartbeat_stored(client, tmp_agent_state)`: POST `{"timestamp": "2026-07-08T10:00:00+00:00", "listing_count": 5, "source": "kv.ee"}` with token; assert 200; `import data_store; state = data_store.load_agent_state(); assert state["last_heartbeat_ts"] == "2026-07-08T10:00:00+00:00"; assert state["last_heartbeat_listing_count"] == 5; assert state["consecutive_zero_count"] == 0`.
  - `test_zero_count_increments(client, tmp_agent_state)`: POST two heartbeats with `listing_count: 0`; assert `state["consecutive_zero_count"] == 2`.
  - `test_zero_count_resets(client, tmp_agent_state)`: POST one zero, then one non-zero; assert `state["consecutive_zero_count"] == 0`.
  - `test_zero_listing_alert(tmp_agent_state, mock_telegram, monkeypatch)`: seed state with `consecutive_zero_count=2, last_heartbeat_ts=<recent iso>`; import agent_job; call `agent_job.check_consecutive_zeros(state)`; assert `mock_telegram.send_message` was called with a string containing "0 listings" (skip until Task 2).
  - `test_offline_alert(tmp_agent_state, mock_telegram, monkeypatch)`: seed state with `last_heartbeat_ts` set to (now - 100 hours) iso; assert `agent_job.check_heartbeat_timeout(state)` triggers send_message with a string containing "offline" (skip until Task 2).
  - `test_no_alert_when_no_baseline(tmp_agent_state, mock_telegram)`: `last_heartbeat_ts` is None; `agent_job.check_heartbeat_timeout(state)`; assert `mock_telegram.send_message` was NOT called (skip until Task 2).
  - `test_alert_cooldown(tmp_agent_state, mock_telegram, monkeypatch)`: seed state with a fresh `last_scraper_alert_sent_at` timestamp (5 hours ago); trigger conditions; assert `mock_telegram.send_message` was NOT called (skip until Task 2).

Create app/tests/test_listing_contract.py with two tests:

  - `test_roundtrip()`: `original = Listing(id="123456", url="https://www.kv.ee/test-123456.html", title="X", price_eur=200000, rooms=3, area_sqm=60.0, image_count=10)`; `serialized = dataclasses.asdict(original)`; `reconstructed = ingest_handler._deserialize_listing(serialized)`; assert all field values match on both objects.
  - `test_unknown_fields_ignored()`: `data = dataclasses.asdict(Listing(id="1", url="u")); data["future_field"] = "some_value"`; `listing = ingest_handler._deserialize_listing(data)`; assert `listing.id == "1"` and no attribute `future_field` on listing (the filter dropped it).

The point of RED-scaffolding stubs in this task is that Task 2 will unmark the `@pytest.mark.skip` decorators as it wires the check functions; a single-commit hop from "skipped test defined" to "test green" is the desired TDD cycle here.
  </action>
  <verify>
    <automated>test -f app/tests/__init__.py && test -f app/tests/conftest.py && test -f app/tests/test_ingest.py && test -f app/tests/test_heartbeat.py && test -f app/tests/test_listing_contract.py && grep -q '^pytest' app/requirements.txt && grep -q '^httpx' app/requirements.txt && python3 -c "import ast; [ast.parse(open(f).read()) for f in ['app/tests/conftest.py','app/tests/test_ingest.py','app/tests/test_heartbeat.py','app/tests/test_listing_contract.py']]" && python3 -c "src = open('app/tests/test_ingest.py').read(); assert 'test_missing_auth' in src and 'test_wrong_token' in src and 'test_ingest_batch' in src and 'test_no_playwright_import' in src" && python3 -c "src = open('app/tests/test_heartbeat.py').read(); assert 'test_heartbeat_stored' in src and 'test_zero_count_increments' in src and 'test_zero_count_resets' in src and 'test_zero_listing_alert' in src and 'test_offline_alert' in src and 'test_no_alert_when_no_baseline' in src and 'test_alert_cooldown' in src" && python3 -c "src = open('app/tests/test_listing_contract.py').read(); assert 'test_roundtrip' in src and 'test_unknown_fields_ignored' in src"</automated>
    <human-check>After `pip install -r app/requirements.txt` inside the app container, run `pytest app/tests/ -v`. Expected: all not-skipped tests pass (auth 200/403, heartbeat storage/counter, listing round-trip); tests that reference check_consecutive_zeros / check_heartbeat_timeout are skipped with `wired in Task 2` messages. `pytest` overall exit code must be 0.</human-check>
  </verify>
  <done>All six test-related files exist; requirements.txt lists pytest and httpx; every test function named in VALIDATION.md's Per-Task Verification Map is defined; test files parse as valid Python; tests that depend on Task 2's functions are decorated with `@pytest.mark.skip(reason="wired in Task 2 of plan 01-03")` so the suite exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add check_heartbeat_timeout and check_consecutive_zeros to agent_job.py; wire both into run_check; unblock the skipped heartbeat tests</name>
  <files>app/agent_job.py, app/tests/test_heartbeat.py</files>
  <read_first>
    - app/agent_job.py (as modified by plan 01-02 — run_check now only calls process_send_commands inside the try; the two new functions go into the try block after process_send_commands, in the order documented in RESEARCH VPS-Side Refactor)
    - app/config.py (HEARTBEAT_TIMEOUT_HOURS + CHECK_INTERVAL_HOURS — the threshold formula per D-10)
    - app/data_store.py (state schema — read `last_heartbeat_ts`, `consecutive_zero_count`, `last_scraper_alert_sent_at`; mutations happen in-place on the `state` dict which run_check's finally block saves)
    - app/telegram_client.py (send_message signature — string only, no chat_id argument since it defaults to config.TELEGRAM_CHAT_ID)
    - .planning/phases/01-scraper-architecture-split/01-RESEARCH.md (Pattern 3 — check_heartbeat_timeout and check_consecutive_zeros reference implementations with grace_hours formula; Pitfall 3 — never-reset bug; Pitfall 4 — None-baseline guard; Open Question 2 — cooldown recommendation)
    - .planning/phases/01-scraper-architecture-split/01-CONTEXT.md (D-10 timeout formula; D-11 heartbeat vs consecutive-zero orthogonality)
    - .planning/phases/01-scraper-architecture-split/01-PATTERNS.md (agent_job.py analog — the run_check structure with the two new calls; the shared never-raise + logging patterns for the new functions)
    - app/tests/test_heartbeat.py (from Task 1 — this task unskips the pending tests and confirms they GREEN)
  </read_first>
  <behavior>
    - `check_heartbeat_timeout(state)` returns early with no side effect when `state.get("last_heartbeat_ts")` is None (RESEARCH Pitfall 4)
    - `check_heartbeat_timeout(state)` computes the threshold as `HEARTBEAT_TIMEOUT_HOURS if HEARTBEAT_TIMEOUT_HOURS > 0 else CHECK_INTERVAL_HOURS * 2 + 0.5` (D-10 formula; explicit override via env var takes precedence)
    - When elapsed > threshold AND cooldown allows: call `telegram_client.send_message(f"Scraper offline — last heartbeat: {last_ts} ({int(elapsed)}h ago). Check the mini PC.")` and set `state["last_scraper_alert_sent_at"] = now_iso`
    - `check_consecutive_zeros(state)` fires when `state.get("consecutive_zero_count", 0) >= 2` AND cooldown allows: calls `send_message(f"Scraper returned 0 listings for {count} consecutive runs. Last heartbeat: {last_ts}. kv.ee may be blocking or the search URL returned no results.")` and sets `state["last_scraper_alert_sent_at"] = now_iso`
    - `_alert_cooldown_ok(state, now_iso)` returns True when `state.get("last_scraper_alert_sent_at")` is None OR the timestamp is older than 24 hours before `now_iso` (RESEARCH Open Question 2 — 24h cooldown recommendation)
    - Both check functions log via the module `log` (INFO on skip, WARNING on alert) and never re-raise on failure (never-raise per the surrounding `run_check` try/except)
    - `run_check()` now calls `process_send_commands(state); check_heartbeat_timeout(state); check_consecutive_zeros(state)` inside its try block, in that order, before the outer try/except swallows any exception (RESEARCH VPS-Side Refactor section)
    - `pytest app/tests/test_heartbeat.py -v` — after this task removes the `@pytest.mark.skip` decorators from `test_zero_listing_alert`, `test_offline_alert`, `test_no_alert_when_no_baseline`, and `test_alert_cooldown` — all four transition from SKIPPED to PASSED
  </behavior>
  <action>
Edit app/agent_job.py: add `import config` if not already imported (should be — used by DRAFT_SCORE_THRESHOLD prior to plan 01-02's cleanup; verify). Add `from datetime import datetime, timezone` at module top if not already there. Ensure `from telegram_client import send_message` is still present (plan 01-02 left it since process_send_commands uses it).

Below process_send_commands and above run_check, add three new module-level definitions:

`def _alert_cooldown_ok(state: dict, now: datetime) -> bool:` — try/except around the parse: `last = state.get("last_scraper_alert_sent_at")`; if `last is None`: `return True`; try `parsed = datetime.fromisoformat(last)`; if the parse or the tz-aware arithmetic raises, return True (fail-open — better to double-alert than to swallow a real outage). Otherwise `return (now - parsed).total_seconds() / 3600 >= 24`.

`def check_heartbeat_timeout(state: dict) -> None:` — module docstring "Fires a Telegram alert if the scraper has gone silent longer than the threshold. Idempotent within a 24h cooldown window (per RESEARCH Open Question 2). Returns silently on any parse failure — never re-raises inside run_check's try block."

  Body: `last_ts_str = state.get("last_heartbeat_ts")`; `if last_ts_str is None: return` (Pitfall 4). Parse `last_ts = datetime.fromisoformat(last_ts_str)` inside try/except ValueError — on parse failure, log.warning and return (never-raise). Compute `now = datetime.now(timezone.utc)`; ensure last_ts is tz-aware (if naive, treat as UTC via `last_ts = last_ts.replace(tzinfo=timezone.utc)`). Compute `elapsed_hours = (now - last_ts).total_seconds() / 3600`. Compute `threshold = config.HEARTBEAT_TIMEOUT_HOURS if config.HEARTBEAT_TIMEOUT_HOURS > 0 else config.CHECK_INTERVAL_HOURS * 2 + 0.5` (D-10). If `elapsed_hours <= threshold`, return. Check cooldown: `if not _alert_cooldown_ok(state, now): log.info("Skipping offline alert — cooldown active"); return`. Fire: `send_message(f"Scraper offline — last heartbeat: {last_ts_str} ({int(elapsed_hours)}h ago). Check the mini PC.")`; `state["last_scraper_alert_sent_at"] = now.isoformat()`; `log.warning("Scraper heartbeat overdue by %.1fh (threshold %.1fh)", elapsed_hours, threshold)`.

`def check_consecutive_zeros(state: dict) -> None:` — module docstring "Fires a Telegram alert if the scraper reported zero listings for 2+ consecutive runs (ARCH-04). Cooldown-gated by _alert_cooldown_ok."

  Body: `count = state.get("consecutive_zero_count", 0)`; `if count < 2: return`. `last_ts = state.get("last_heartbeat_ts", "unknown")`; `now = datetime.now(timezone.utc)`. `if not _alert_cooldown_ok(state, now): log.info("Skipping zero-listing alert — cooldown active"); return`. Fire: `send_message(f"Scraper returned 0 listings for {count} consecutive runs. Last heartbeat: {last_ts}. kv.ee may be blocking or the search URL returned no results.")`; `state["last_scraper_alert_sent_at"] = now.isoformat()`; `log.warning("Zero-listing alert: %d consecutive empty runs", count)`.

Edit run_check(): inside the try block that plan 01-02 shortened to just `process_send_commands(state)`, add the two new calls in this exact order — `process_send_commands(state); check_heartbeat_timeout(state); check_consecutive_zeros(state)`. Order matters: process_send_commands runs first because it's the highest-priority user-facing action (delivering emails Daniel has requested); the two health checks are lower priority. The outer try/except already logs any exception via `log.exception("agent_job.run_check failed")` and the finally block already calls `data_store.save_agent_state(state)` — no additional plumbing needed to persist the new `last_scraper_alert_sent_at` mutations.

Edit app/tests/test_heartbeat.py: remove the four `@pytest.mark.skip(reason="wired in Task 2 of plan 01-03")` decorators added in Task 1 on `test_zero_listing_alert`, `test_offline_alert`, `test_no_alert_when_no_baseline`, and `test_alert_cooldown`. Do NOT modify the assertions or setup — the test bodies were written in Task 1 to match the exact behavior contract this task implements.
  </action>
  <verify>
    <automated>python3 -c "import ast; src = open('app/agent_job.py').read(); tree = ast.parse(src); fns = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}; assert {'check_heartbeat_timeout','check_consecutive_zeros','_alert_cooldown_ok'} <= fns, fns; assert 'check_heartbeat_timeout(state)' in src and 'check_consecutive_zeros(state)' in src, 'run_check does not call the new check functions'; assert 'HEARTBEAT_TIMEOUT_HOURS' in src, 'threshold formula not implemented'; assert 'CHECK_INTERVAL_HOURS' in src, 'threshold formula not using CHECK_INTERVAL_HOURS'" && ! grep -c 'wired in Task 2 of plan 01-03' app/tests/test_heartbeat.py</automated>
    <human-check>Run `pytest app/tests/ -v` after this task completes. All 13 tests across three test files must show PASSED (no SKIPPED). Attach the pytest output to the SUMMARY.</human-check>
  </verify>
  <done>app/agent_job.py defines _alert_cooldown_ok, check_heartbeat_timeout, and check_consecutive_zeros; run_check() invokes both new checks in order after process_send_commands; the None-baseline guard, threshold formula, and 24h cooldown are all implemented; no `wired in Task 2` skip decorators remain in test_heartbeat.py.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import ast; ast.parse(open('app/agent_job.py').read())"` exits 0.
2. `grep -q 'pytest' app/requirements.txt && grep -q 'httpx' app/requirements.txt` — dev deps present.
3. `python3 -c "src = open('app/agent_job.py').read(); assert src.count('check_heartbeat_timeout') >= 2 and src.count('check_consecutive_zeros') >= 2"` — each function is defined AND called at least once (definition + call in run_check).
4. Manual (recorded in SUMMARY): `pip install -r app/requirements.txt && pytest app/tests/ -v` reports all 13 tests PASSED, 0 SKIPPED, 0 FAILED.
5. Manual (recorded in SUMMARY): after deploying plan 01-02 + 01-03 to the VPS, POST two heartbeats with `listing_count: 0` (via curl using the real INGEST_TOKEN), wait for the next scheduler tick, confirm the "Scraper returned 0 listings" Telegram message arrives.
</verification>

<success_criteria>
- ARCH-04 satisfied end-to-end: two consecutive zero-listing heartbeats + one scheduler tick triggers a Telegram alert; a stale-heartbeat state triggers a separate offline alert; both alerts respect a 24-hour cooldown so Daniel is not spammed while the outage persists.
- The full test suite from VALIDATION.md is realized: 13 pytest tests across three files cover ARCH-01 (Listing round-trip), ARCH-02 (auth 200/403), ARCH-03 (no playwright/kv_alert_reader/kv_scraper imports in ingest_handler), and ARCH-04 (heartbeat storage + consecutive-zero alert + offline alert + no-baseline guard + cooldown).
- run_check's wiring keeps the highest-priority action (delivering pending emails) first; the two health checks are cheap in-memory operations and cannot block Telegram command processing.
</success_criteria>

<threat_model>
## Trust Boundaries

Same as plan 01-02 — no new external endpoints introduced by this plan. The scheduler tick runs in-process and reads state written by plan 01-02's handle_heartbeat.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Denial of service | Alert spam via forged heartbeats | low | accept | Bearer-token gate on /api/heartbeat (from plan 01-02) means only holder of INGEST_TOKEN can flip consecutive_zero_count; 24h cooldown further caps outbound Telegram traffic. |
| T-01-08 | Tampering | Alert-cooldown parse failure | low | mitigate | _alert_cooldown_ok returns True on parse failure (fail-open) — over-alerting is preferable to silent alert loss during a real outage. |
| T-01-09 | Information disclosure | Alert message content | low | accept | Telegram messages contain a timestamp and generic diagnostics; no INGEST_TOKEN, no listing data, no PII. |
</threat_model>

<output>
Create `.planning/phases/01-scraper-architecture-split/01-03-SUMMARY.md` when done, noting:
- Files modified and the exact function bodies added to agent_job.py.
- Pytest output (13 passed, 0 skipped, 0 failed) — attach the tail of the command.
- Confirmation that the human-check end-to-end alert test on the deployed VPS produced a Telegram message (or note if deferred to the phase's `/gsd-verify-work` step).
- Any deviations from the RESEARCH Pattern 3 reference implementation (e.g., cooldown duration adjusted, threshold formula rewritten).
</output>
