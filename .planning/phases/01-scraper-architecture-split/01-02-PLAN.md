---
phase: 01-scraper-architecture-split
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/data_store.py
  - app/ingest_handler.py
  - app/main.py
  - app/agent_job.py
  - app/kv_scraper.py
  - Caddyfile
autonomous: true
requirements:
  - ARCH-02
  - ARCH-03
user_setup:
  - service: vps-env
    why: "Daniel must set INGEST_TOKEN in the VPS .env — the same random hex value from plan 01-01's mini PC .env. This is a shared secret, not derived automatically."
    env_vars:
      - name: INGEST_TOKEN
        source: "Generate with `python -c 'import secrets; print(secrets.token_hex(32))'` — must match the value put in scraper-client/.env in plan 01-01"
      - name: HEARTBEAT_TIMEOUT_HOURS
        source: "Optional override. Default 0 means use the formula CHECK_INTERVAL_HOURS * 2 + 0.5 (per D-10)"

must_haves:
  truths:
    - "POST /api/ingest with header Authorization: Bearer {matching INGEST_TOKEN} and a JSON array body returns 200 and processes the batch"
    - "POST /api/ingest with a missing Authorization header returns 403"
    - "POST /api/ingest with a wrong token returns 403"
    - "POST /api/heartbeat with the matching token stores last_heartbeat_ts, last_heartbeat_listing_count, and updates consecutive_zero_count in agent_state.json (increments when listing_count == 0, resets to 0 when listing_count > 0)"
    - "The ingest handler processes a batch of Listing JSON dicts by: deserializing via _deserialize_listing (unknown keys filtered), deduping via seen_listing_ids, applying MIN_ROOMS/MAX_PRICE_EUR/MIN_IMAGES filters, calling evaluate_listing + add_property_if_new + send_photo/send_message + create_draft (per D-07), all inside data_store._lock"
    - "The ingest handler does not import from kv_alert_reader (renamed to kv_scraper on VPS) or playwright (per D-08 + ARCH-03)"
    - "app/agent_job.py no longer imports fetch_listing_urls, get_session, fetch_listing, or extract_object_id; process_new_listings() is removed"
    - "Caddy routes /api/ingest and /api/heartbeat to app:8000 without basicauth so the Bearer token can pass through unmodified (per RESEARCH Pitfall 1)"
  artifacts:
    - app/ingest_handler.py
    - app/kv_scraper.py (renamed from app/kv_alert_reader.py)
    - Updated app/config.py (INGEST_TOKEN, HEARTBEAT_TIMEOUT_HOURS)
    - Updated app/data_store.py (DEFAULT_AGENT_STATE with heartbeat fields)
    - Updated app/main.py (POST /api/ingest, POST /api/heartbeat, HTTPBearer dependency)
    - Updated app/agent_job.py (process_new_listings removed, scraping imports removed)
    - Updated Caddyfile (named matcher bypasses basicauth for /api/ingest and /api/heartbeat)
  key_links:
    - "app/main.py POST /api/ingest handler calls ingest_handler.process_ingest_batch(list[dict]); handler acquires data_store._lock for the full load-process-save sequence (per RESEARCH Pitfall 5 — prevents race between concurrent requests)"
    - "app/main.py POST /api/heartbeat handler calls ingest_handler.handle_heartbeat(dict); updates last_heartbeat_ts, last_heartbeat_listing_count, and consecutive_zero_count on agent_state — plan 01-03's check_consecutive_zeros/check_heartbeat_timeout read exactly these fields"
    - "_verify_ingest_token dependency uses fastapi.security.HTTPBearer; rejects with 403 (not 401) when config.INGEST_TOKEN is empty (misconfigured) or credentials.credentials does not match"
    - "The renamed module (kv_alert_reader → kv_scraper) is not imported anywhere on the VPS side (grep app/ -e kv_alert_reader returns nothing); the file is kept in-repo for reference but has no import path"
---

<objective>
Land the VPS side of the vertical slice: (a) the `POST /api/ingest` endpoint that receives batches of parsed Listing JSON dicts from the mini PC and runs the full evaluate + queue + notify pipeline that used to live inside `agent_job.process_new_listings()`; (b) the `POST /api/heartbeat` endpoint that stores heartbeat state for plan 01-03's alert logic; (c) the deletion of VPS-side scraping (`process_new_listings` removed, `kv_alert_reader.py` renamed to `kv_scraper.py` and unimported); (d) the Caddyfile update so Caddy stops intercepting the machine-to-machine endpoints with basic auth. Together with plan 01-01 (the mini PC client), this delivers the first end-to-end MVP slice — a mini PC scrape POSTs data, the VPS receives, evaluates, and notifies Daniel over Telegram.

Purpose: Move all scraping off the VPS (ARCH-03) and give the mini PC a receiving endpoint authenticated by a shared secret (ARCH-02).

Output: One new `app/ingest_handler.py`, five modified `app/**` files, one renamed file (`kv_alert_reader.py` → `kv_scraper.py`), and an updated `Caddyfile`. Files touched are disjoint from plan 01-01's `scraper-client/**` files, so both plans run in parallel in Wave 1.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scraper-architecture-split/01-CONTEXT.md
@.planning/phases/01-scraper-architecture-split/01-RESEARCH.md
@.planning/phases/01-scraper-architecture-split/01-PATTERNS.md
@app/main.py
@app/agent_job.py
@app/config.py
@app/data_store.py
@app/kv_listing_parser.py
@Caddyfile
</context>

## Phase Goal

**As a** Daniel (running the apartment-hunting system), **I want to** have my VPS receive parsed listing data over HTTP from an authenticated mini PC endpoint and run the same AI evaluation and Telegram notification flow as before, **so that** the VPS never launches a browser and Cloudflare stops blocking me.

<artifacts_produced>
## Artifacts this phase produces

**New file:**
- `app/ingest_handler.py` — batch ingest handler + heartbeat handler + Listing deserializer

**Renamed file:**
- `app/kv_alert_reader.py` → `app/kv_scraper.py` (module contents unchanged; kept in-repo for Phase 4 reference; NO imports of it remain on VPS)

**Updated files:**
- `app/config.py` — adds `INGEST_TOKEN`, `HEARTBEAT_TIMEOUT_HOURS`
- `app/data_store.py` — extends `DEFAULT_AGENT_STATE` with `last_heartbeat_ts`, `last_heartbeat_listing_count`, `consecutive_zero_count`, `last_scraper_alert_sent_at`
- `app/main.py` — adds `_verify_ingest_token` dependency, `POST /api/ingest`, `POST /api/heartbeat`
- `app/agent_job.py` — removes imports of fetch_listing_urls/get_session/fetch_listing/extract_object_id, removes `process_new_listings()` and `_listing_to_property()`, removes the `process_new_listings(state)` call from `run_check()`; `run_check()` remains but only calls `process_send_commands()` (heartbeat checks are added by plan 01-03 in Wave 2)
- `Caddyfile` — named matcher `@machine path /api/ingest /api/heartbeat` bypasses basicauth for the two endpoints

**New symbols:**
- `config.INGEST_TOKEN` (str, from `os.environ.get("INGEST_TOKEN", "")`)
- `config.HEARTBEAT_TIMEOUT_HOURS` (float, from `os.environ.get("HEARTBEAT_TIMEOUT_HOURS", "0")`; sentinel `0` means "use the formula CHECK_INTERVAL_HOURS * 2 + 0.5" — read by plan 01-03)
- `data_store.DEFAULT_AGENT_STATE` keys: `last_heartbeat_ts`, `last_heartbeat_listing_count`, `consecutive_zero_count`, `last_scraper_alert_sent_at`
- `main._bearer` (HTTPBearer instance)
- `main._verify_ingest_token(credentials) -> None` (FastAPI dependency; raises 403 on missing/mismatched token or unconfigured INGEST_TOKEN)
- `main.ingest(request) -> dict` (POST /api/ingest handler; async body read, sync delegate)
- `main.heartbeat(request) -> dict` (POST /api/heartbeat handler; async body read, sync delegate)
- `ingest_handler.LISTING_FIELD_NAMES` (frozenset of Listing field names, cached via `dataclasses.fields`)
- `ingest_handler._deserialize_listing(data: dict) -> Listing` (filters unknown keys; never raises on unknown keys)
- `ingest_handler._listing_to_property(listing, evaluation) -> dict` (moved from agent_job.py verbatim)
- `ingest_handler.process_ingest_batch(listing_dicts: list[dict]) -> dict` (holds data_store._lock for the full batch)
- `ingest_handler.handle_heartbeat(payload: dict) -> dict` (updates last_heartbeat_ts/count and consecutive_zero_count under lock)

**New environment variables:**
- `INGEST_TOKEN` — VPS side of shared secret with mini PC (must equal scraper-client/.env INGEST_TOKEN)
- `HEARTBEAT_TIMEOUT_HOURS` — optional; default `0` selects the formula-based threshold in plan 01-03
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config.py and data_store.py — INGEST_TOKEN, HEARTBEAT_TIMEOUT_HOURS, DEFAULT_AGENT_STATE heartbeat fields</name>
  <files>app/config.py, app/data_store.py</files>
  <read_first>
    - app/config.py (current env-var pattern — MIN_IMAGES/MIN_ROOMS/MAX_PRICE_EUR/CHECK_INTERVAL_HOURS follow os.environ.get with int/float cast; INGEST_TOKEN is a string so no cast)
    - app/data_store.py (current DEFAULT_AGENT_STATE at line 45; load_agent_state's setdefault-based auto-upgrade pattern at lines 91-95 means new fields upgrade existing JSON files without a migration script)
    - .planning/phases/01-scraper-architecture-split/01-CONTEXT.md (D-06 INGEST_TOKEN + Bearer; D-09/D-10 heartbeat storage schema)
    - .planning/phases/01-scraper-architecture-split/01-RESEARCH.md (Pattern 3 — DEFAULT_AGENT_STATE additions; Runtime State Inventory — setdefault auto-upgrade)
    - .planning/phases/01-scraper-architecture-split/01-PATTERNS.md (app/config.py analog, app/data_store.py analog)
  </read_first>
  <behavior>
    - `import config; config.INGEST_TOKEN` returns the value of `INGEST_TOKEN` env var, or empty string if unset
    - `import config; config.HEARTBEAT_TIMEOUT_HOURS` returns a float; unset defaults to 0.0 which downstream code interprets as "use formula" (per D-10)
    - `data_store.DEFAULT_AGENT_STATE` gains keys `last_heartbeat_ts` (None), `last_heartbeat_listing_count` (None), `consecutive_zero_count` (0), `last_scraper_alert_sent_at` (None)
    - Loading an existing `agent_state.json` file created before this change returns a dict containing the new keys with their default values (thanks to the existing setdefault loop in load_agent_state — no schema migration needed)
  </behavior>
  <action>
Edit app/config.py: append two new lines after the existing `CHECK_INTERVAL_HOURS = float(...)` line — `INGEST_TOKEN = os.environ.get("INGEST_TOKEN", "")` (no cast — token is a string) and `HEARTBEAT_TIMEOUT_HOURS = float(os.environ.get("HEARTBEAT_TIMEOUT_HOURS", "0"))` (0 is the sentinel meaning "use formula CHECK_INTERVAL_HOURS * 2 + 0.5", consumed by plan 01-03's check_heartbeat_timeout). Do NOT log the token value anywhere in the module (per RESEARCH Security Domain — Token exposure in logs).

Edit app/data_store.py: extend the `DEFAULT_AGENT_STATE` dict at line 45 by adding four new keys — `last_heartbeat_ts: None`, `last_heartbeat_listing_count: None`, `consecutive_zero_count: 0`, `last_scraper_alert_sent_at: None`. Keep the existing four keys (`seen_listing_ids`, `pending_drafts`, `last_telegram_update_id`, `last_processed_uid`) unchanged and in place. The existing `load_agent_state()` at lines 91-95 already iterates `DEFAULT_AGENT_STATE.items()` and calls `state.setdefault(k, v)` for each — this auto-upgrades any existing JSON file with the new fields on next load, so no migration script is needed (per RESEARCH Runtime State Inventory).

Do NOT modify any other line of data_store.py — specifically do not touch DEFAULT_PROPERTIES or the existing _read_json/_write_json helpers.
  </action>
  <verify>
    <automated>python3 -c "import ast; tree = ast.parse(open('app/config.py').read()); assigns = [n for n in ast.walk(tree) if isinstance(n, ast.Assign)]; names = {t.id for a in assigns for t in a.targets if isinstance(t, ast.Name)}; assert 'INGEST_TOKEN' in names, 'INGEST_TOKEN not defined'; assert 'HEARTBEAT_TIMEOUT_HOURS' in names, 'HEARTBEAT_TIMEOUT_HOURS not defined'" && python3 -c "import ast; src = open('app/data_store.py').read(); tree = ast.parse(src); assert 'last_heartbeat_ts' in src and 'last_heartbeat_listing_count' in src and 'consecutive_zero_count' in src and 'last_scraper_alert_sent_at' in src, 'missing heartbeat keys'" && grep -q 'INGEST_TOKEN = os.environ.get' app/config.py && grep -q 'HEARTBEAT_TIMEOUT_HOURS = float' app/config.py</automated>
  </verify>
  <done>config.py exposes INGEST_TOKEN (str, defaults to "") and HEARTBEAT_TIMEOUT_HOURS (float, defaults to 0.0); data_store.DEFAULT_AGENT_STATE contains all four new keys with their documented defaults; load_agent_state()'s existing setdefault loop provides forward-compat with existing agent_state.json files (no migration script needed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create app/ingest_handler.py — Listing deserializer, batch handler, heartbeat handler (moves process_new_listings body)</name>
  <files>app/ingest_handler.py, app/agent_job.py, app/kv_scraper.py</files>
  <read_first>
    - app/agent_job.py (source of process_new_listings body at lines 72-123 — the filter + evaluate + notify block; _listing_to_property helper at lines 26-48 also moves; the process_send_commands function at 51-69 stays put; run_check at 126-138 loses the process_new_listings call)
    - app/kv_listing_parser.py (Listing dataclass — fields used by _deserialize_listing; extract_object_id used for dedup key)
    - app/data_store.py (_lock is a threading.RLock; ingest handler must hold it across load_agent_state → process → save_agent_state to prevent races per RESEARCH Pitfall 5)
    - app/config.py (INGEST_TOKEN, MIN_IMAGES, MIN_ROOMS, MAX_PRICE_EUR, DRAFT_SCORE_THRESHOLD — the filter thresholds live on VPS per D-02)
    - app/kv_alert_reader.py (about to be renamed to app/kv_scraper.py — its exports fetch_listing_urls/get_session must NOT be imported by agent_job.py or ingest_handler.py; the file stays in-repo for Phase 4 reference)
    - .planning/phases/01-scraper-architecture-split/01-RESEARCH.md (Pattern 2 — ingest handler batch loop with deserialization; Pitfall 2 — filter unknown keys with dc_fields; Pitfall 5 — lock-scope)
    - .planning/phases/01-scraper-architecture-split/01-PATTERNS.md (app/ingest_handler.py analog — the exact block of process_new_listings to lift out; deserialization pattern using dataclasses.fields)
  </read_first>
  <behavior>
    - `ingest_handler.process_ingest_batch([listing_dict, ...])` returns `{"ok": True, "processed": <int>}` and mutates agent_state.json with new seen_listing_ids, pending_drafts, and calls evaluate_listing + Telegram + Gmail exactly as `process_new_listings` did (per D-07)
    - `_deserialize_listing` accepts a JSON dict, filters keys not in the Listing dataclass field set, and constructs a `Listing(**filtered)` — a dict containing extra keys does NOT raise (per RESEARCH Pitfall 2)
    - A dict missing required fields (`id`, `url`) is skipped with `log.warning`, not raised (never-raise pattern)
    - The batch handler acquires `data_store._lock` for the full load-process-save sequence — even for a 20-listing batch, this is <5s on the VPS (single-user scale per CLAUDE.md)
    - `handle_heartbeat({"timestamp": ..., "listing_count": N, "source": ...})` sets `last_heartbeat_ts`, `last_heartbeat_listing_count`; when listing_count == 0 increments `consecutive_zero_count`, otherwise resets it to 0 (per RESEARCH Pitfall 3 — explicit reset required)
    - `app/agent_job.py` no longer imports from kv_scraper or kv_listing_parser; `process_new_listings` and `_listing_to_property` are removed from agent_job; run_check() no longer calls process_new_listings (plan 01-03 will add heartbeat checks in Wave 2)
    - `app/kv_alert_reader.py` is renamed to `app/kv_scraper.py`; no imports of either name exist anywhere in `app/**` (grep gate verifies this)
  </behavior>
  <action>
Create app/ingest_handler.py with module docstring "Ingest handler: filter + evaluate + notify pipeline for listings POSTed by the mini PC scraper client. Also stores heartbeat state consumed by plan 01-03's scheduler-tick alert checks. Extracted from agent_job.process_new_listings() as part of Phase 1 (D-08)."

Imports: logging, dataclasses.fields as dc_fields, datetime.datetime + datetime.timezone; local imports config, data_store; from ai_evaluator import evaluate_listing; from gmail_client import create_draft; from telegram_client import format_listing_card, send_message, send_photo; from kv_listing_parser import Listing, extract_object_id. Do NOT import from kv_alert_reader or kv_scraper — ARCH-03 forbids Playwright/browser use on VPS.

Module-level `log = logging.getLogger("ingest_handler")`.

Module-level `LISTING_FIELD_NAMES = frozenset(f.name for f in dc_fields(Listing))` computed once at import.

`_deserialize_listing(data: dict) -> Listing`: `known = {k: v for k, v in data.items() if k in LISTING_FIELD_NAMES}`; `return Listing(**known)`. Do not raise on unknown keys — filtering prevents TypeError per RESEARCH Pitfall 2.

`_listing_to_property(listing, evaluation) -> dict`: move the function body verbatim from agent_job.py lines 26-48 (do not change its logic; it belongs alongside the code that calls it).

`process_ingest_batch(listing_dicts: list[dict]) -> dict`: log.info the batch size. Acquire `with data_store._lock:` and inside the lock call `state = data_store.load_agent_state()`. Loop over listing_dicts: wrap each iteration in `try: listing = _deserialize_listing(data)` / `except (TypeError, KeyError) as exc: log.warning("Malformed listing dict: %s", exc); continue`. After successful deserialize: compute `dedup_key = extract_object_id(listing.url) or listing.url`; if `dedup_key in set(state["seen_listing_ids"]): continue`. Append the dedup key (not listing.id — the existing code at agent_job.py:87 appends listing.id, which is already the object id when extract_object_id succeeded; keep the same behavior by appending listing.id per RESEARCH Pattern 2 reference); then apply the three filters (`price_eur > config.MAX_PRICE_EUR` → skip; `rooms < config.MIN_ROOMS` → skip; `image_count < config.MIN_IMAGES` → skip) exactly as agent_job.process_new_listings does (per D-02 filters run on VPS). Call `evaluation = evaluate_listing(listing)`, `data_store.add_property_if_new(_listing_to_property(listing, evaluation))`, `format_listing_card(listing, evaluation)`, and either `send_photo(listing.image_url, card_text)` when image_url is set or `send_message(card_text)` otherwise. Draft flow: same `should_draft = evaluation.get("should_draft_email") and evaluation.get("score", 0) >= config.DRAFT_SCORE_THRESHOLD` gate; on match call `create_draft(listing.contact_email, subject, body)` and update `state["pending_drafts"][listing.id] = {...}`. After the loop, `data_store.save_agent_state(state)`. Return `{"ok": True, "processed": len(listing_dicts)}`. Never re-raise inside the per-listing block (never-raise pattern); a failed evaluate_listing on one entry logs and the loop continues.

`handle_heartbeat(payload: dict) -> dict`: acquire `with data_store._lock:` and inside the lock load state; set `state["last_heartbeat_ts"] = payload.get("timestamp") or datetime.now(timezone.utc).isoformat()`; set `state["last_heartbeat_listing_count"] = int(payload.get("listing_count", 0))`; if that integer == 0, `state["consecutive_zero_count"] = state.get("consecutive_zero_count", 0) + 1`; otherwise `state["consecutive_zero_count"] = 0` (explicit reset per RESEARCH Pitfall 3). Save state. Log at info level source and consecutive_zero_count but NEVER log INGEST_TOKEN (per RESEARCH Security Domain). Return `{"ok": True}`.

Edit app/agent_job.py: remove the imports `from kv_alert_reader import fetch_listing_urls, get_session` and `from kv_listing_parser import fetch_listing, extract_object_id` (both no longer used on the VPS). Also remove `from ai_evaluator import evaluate_listing`, `from gmail_client import create_draft`, and the `send_photo`, `format_listing_card` from the telegram_client import — those move to ingest_handler.py (keep `send_message` if still used; process_send_commands calls it). Delete the `_listing_to_property` function and the entire `process_new_listings` function. In `run_check()`, remove the `process_send_commands(state); process_new_listings(state)` pair and replace with just `process_send_commands(state)` — plan 01-03 will add `check_heartbeat_timeout(state)` and `check_consecutive_zeros(state)` in Wave 2. Keep the outer try/except/finally structure and the `log.info` calls intact; update the "Running kv.ee check..." message to "Running scheduler tick..." and the "Check complete." message to "Tick complete." to reflect the new responsibility. Keep the module docstring — but its content is now stale; update line 2 wording from "The periodic kv.ee check..." to "The periodic scheduler tick — telegram command polling and (added in Phase 1 plan 03) heartbeat/zero-listing checks. Scraping moved to the mini PC client (Phase 1, plan 01) and is invoked via POST /api/ingest, not from here."

Rename app/kv_alert_reader.py to app/kv_scraper.py — use `git mv` if inside a git worktree; otherwise `mv`. Do NOT modify the module contents. The file remains in-repo purely as reference for Phase 4 (new scraper sources) — no VPS-side code imports from it after this task. If you cannot rename via git mv in the executor environment, use plain `mv` and rely on git to detect the rename.

After the rename, run a grep to confirm no `app/` file imports `kv_alert_reader` or `kv_scraper` — this is a hard gate on ARCH-03.
  </action>
  <verify>
    <automated>test -f app/ingest_handler.py && test -f app/kv_scraper.py && test ! -f app/kv_alert_reader.py && python3 -c "import ast; tree = ast.parse(open('app/ingest_handler.py').read()); fns = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}; assert fns >= {'_deserialize_listing','_listing_to_property','process_ingest_batch','handle_heartbeat'}, fns" && python3 -c "src = open('app/agent_job.py').read(); assert 'def process_new_listings' not in src, 'process_new_listings not removed'; assert 'def _listing_to_property' not in src, '_listing_to_property not removed from agent_job'; assert 'kv_alert_reader' not in src and 'kv_scraper' not in src, 'stale kv_* import remains in agent_job'; assert 'fetch_listing_urls' not in src, 'fetch_listing_urls still imported in agent_job'" && grep -Rq --include='*.py' 'from kv_alert_reader\|import kv_alert_reader' app/ && exit 1 || true && grep -Rq --include='*.py' 'from kv_scraper\|import kv_scraper' app/ && exit 1 || true && grep -Rq --include='*.py' 'playwright' app/ingest_handler.py && exit 1 || true</automated>
  </verify>
  <done>app/ingest_handler.py exists with the four required functions and no import of kv_alert_reader/kv_scraper/playwright; app/kv_scraper.py exists (renamed); app/kv_alert_reader.py no longer exists; app/agent_job.py has no reference to process_new_listings, _listing_to_property, kv_alert_reader, kv_scraper, or fetch_listing_urls; run_check() no longer calls process_new_listings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire POST /api/ingest and POST /api/heartbeat in main.py; update Caddyfile to bypass basicauth for both</name>
  <files>app/main.py, Caddyfile</files>
  <read_first>
    - app/main.py (existing route pattern — put_data is async def with await request.json() and returns dict; the ingest/heartbeat endpoints follow the same shape with an added Depends dependency)
    - app/ingest_handler.py (freshly created in the previous task — provides process_ingest_batch and handle_heartbeat)
    - app/config.py (INGEST_TOKEN read for token comparison)
    - Caddyfile (current global basicauth block — must be split into a named matcher block + a fallback block that keeps basicauth for the frontend)
    - .planning/phases/01-scraper-architecture-split/01-RESEARCH.md (Pattern 1 — FastAPI HTTPBearer dependency; Pitfall 1 — Caddy basicauth bypass with named matcher; Verified Pattern: async body reading + sync helper)
    - .planning/phases/01-scraper-architecture-split/01-PATTERNS.md (app/main.py analog — Bearer Token Auth Guard shared pattern; Caddyfile analog)
    - .planning/phases/01-scraper-architecture-split/01-CONTEXT.md (D-06 Bearer token; D-07 batch body; D-09 heartbeat payload)
  </read_first>
  <behavior>
    - `curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8000/api/ingest -H 'Content-Type: application/json' -d '[]'` → 403 (no Bearer header)
    - `curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:8000/api/ingest -H 'Authorization: Bearer wrong-token' -H 'Content-Type: application/json' -d '[]'` → 403
    - With `INGEST_TOKEN=test-abc` env set and `Authorization: Bearer test-abc`, POST /api/ingest with `[]` returns 200 and `{"ok": true, "processed": 0}`
    - Same auth rules apply to POST /api/heartbeat; on 200, agent_state.json contains updated `last_heartbeat_ts` and `last_heartbeat_listing_count`
    - Caddyfile has a named matcher `@machine path /api/ingest /api/heartbeat` in a `handle` block without basicauth; the fallback `handle` block still applies basicauth (frontend + all other API routes unchanged)
    - The static frontend under `/` still requires basic auth; `curl -s -o /dev/null -w '%{http_code}' http://caddy/api/ingest` (via Caddy, no Authorization header, no basic auth) reaches FastAPI and receives 403 from FastAPI's HTTPBearer dependency (not 401 from Caddy)
  </behavior>
  <action>
Edit app/main.py: extend the existing `from fastapi import FastAPI, Request` import to add `Depends`, `HTTPException`, `status`. Add `from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer`. Add `import config` and `import ingest_handler` beneath the existing `import data_store` line.

After the `log = logging.getLogger("app")` line, add module-level `_bearer = HTTPBearer(auto_error=False)`. Then define `def _verify_ingest_token(credentials: HTTPAuthorizationCredentials | None = Depends(_bearer)) -> None:` — inside, first check `if not config.INGEST_TOKEN: raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Ingest not configured")`; then check `if credentials is None or credentials.credentials != config.INGEST_TOKEN: raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Invalid token")`. Do NOT log the token value or the presented credential. Use auto_error=False on HTTPBearer so missing headers yield credentials=None (which we handle as 403), not FastAPI's default 403 with a differently-worded detail — this keeps the response schema uniform.

Add two new route handlers ABOVE the existing `app.mount("/", StaticFiles(...))` line (order matters — static mount must remain last, per the existing comment at line 54).

`@app.post("/api/ingest", dependencies=[Depends(_verify_ingest_token)])` `async def ingest(request: Request) -> dict:` body: `payload = await request.json()`; if `not isinstance(payload, list): return JSONResponse({"error": "expected a JSON array of listings"}, status_code=400)`; `return ingest_handler.process_ingest_batch(payload)`. The async wrapper does the body read; the sync helper `process_ingest_batch` runs in the thread pool via FastAPI's default sync-in-threadpool behavior when invoked (but here we're calling it synchronously from an async handler, so the caller thread — the event loop — will hold data_store._lock briefly; at single-user scale this is acceptable per CLAUDE.md — the note is preserved in RESEARCH Verified Pattern).

`@app.post("/api/heartbeat", dependencies=[Depends(_verify_ingest_token)])` `async def heartbeat(request: Request) -> dict:` body: `payload = await request.json()`; if `not isinstance(payload, dict): return JSONResponse({"error": "expected a JSON object"}, status_code=400)`; `return ingest_handler.handle_heartbeat(payload)`.

Edit Caddyfile: replace the current `:80 { basicauth { ... } reverse_proxy app:8000 }` block with the named-matcher pattern from RESEARCH Pitfall 1 — `:80 { @machine path /api/ingest /api/heartbeat  handle @machine { reverse_proxy app:8000 }  handle { basicauth { daniel <existing bcrypt hash> } reverse_proxy app:8000 } }`. Keep the existing bcrypt hash exactly as-is; do not regenerate it. The named matcher must come before the fallback handle block for Caddy's matcher precedence to route correctly. Preserve any comments at the top of the file (the `# Replace with your actual domain...` comment stays).
  </action>
  <verify>
    <automated>python3 -c "import ast; src = open('app/main.py').read(); tree = ast.parse(src); routes = [d for f in ast.walk(tree) if isinstance(f, ast.AsyncFunctionDef) or isinstance(f, ast.FunctionDef) for d in getattr(f, 'decorator_list', [])]; assert '/api/ingest' in src and '/api/heartbeat' in src, 'missing route paths'; assert 'HTTPBearer' in src and '_verify_ingest_token' in src, 'missing auth dependency'; assert 'Depends(_verify_ingest_token)' in src, 'ingest routes are not gated by the auth dependency'; assert 'ingest_handler.process_ingest_batch' in src, 'main.py does not call process_ingest_batch'; assert 'ingest_handler.handle_heartbeat' in src, 'main.py does not call handle_heartbeat'" && grep -q '@machine path /api/ingest /api/heartbeat' Caddyfile && grep -q 'handle @machine' Caddyfile && grep -q 'basicauth {' Caddyfile</automated>
    <human-check>After bringing the stack up locally with `docker compose up --build`, run: (a) `curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost/api/ingest -d '[]' -H 'Content-Type: application/json'` — expect `403`. (b) `INGEST_TOKEN=test123 docker compose up`, then `curl -s -o /dev/null -w '%{http_code}\n' -X POST http://localhost/api/ingest -H 'Authorization: Bearer test123' -d '[]' -H 'Content-Type: application/json'` — expect `200`. Attach the raw curl output to the SUMMARY.</human-check>
  </verify>
  <done>app/main.py declares HTTPBearer, `_verify_ingest_token`, and both ingest + heartbeat routes each gated by Depends(_verify_ingest_token); the static mount remains last; Caddyfile has a `@machine` named matcher block preceding the basicauth fallback; local curl checks show 403 without token and 200 with matching token.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import ast; [ast.parse(open(f).read()) for f in ['app/config.py','app/data_store.py','app/main.py','app/agent_job.py','app/ingest_handler.py','app/kv_scraper.py']]"` exits 0.
2. `grep -R --include='*.py' 'kv_alert_reader\|fetch_listing_urls\|from kv_scraper\|import kv_scraper' app/` returns nothing (proves ARCH-03 — no VPS-side scraping imports remain).
3. `grep -q '@machine path /api/ingest /api/heartbeat' Caddyfile` and `grep -q 'basicauth' Caddyfile` both succeed.
4. Manual (recorded in SUMMARY): `docker compose up --build` then `curl -X POST -H 'Authorization: Bearer <token>' -d '[]' http://localhost/api/ingest` returns 200; `curl -X POST -d '[]' http://localhost/api/ingest` returns 403.
</verification>

<success_criteria>
- ARCH-02 satisfied: POST /api/ingest and POST /api/heartbeat exist, require `Authorization: Bearer <INGEST_TOKEN>`, and are exposed through Caddy without basic auth so the mini PC can reach them.
- ARCH-03 satisfied: no import of kv_alert_reader, kv_scraper, or playwright exists in app/main.py, app/agent_job.py, app/scheduler.py, or app/ingest_handler.py; the ingest handler runs the full evaluate+notify pipeline that previously lived in process_new_listings; kv_alert_reader.py has been renamed to kv_scraper.py.
- The heartbeat handler stores the fields plan 01-03 will read: `last_heartbeat_ts`, `last_heartbeat_listing_count`, and `consecutive_zero_count` (increment/reset semantics correct).
- data_store's existing setdefault-based auto-upgrade means the running production `agent_state.json` file will pick up the new fields on next load without a migration script.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mini PC → VPS over public internet | Untrusted network; INGEST_TOKEN is the only access control |
| Caddy → app (Docker internal network) | Trusted; Caddy already terminates whatever TLS exists and proxies to app:8000 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | POST /api/ingest, POST /api/heartbeat | high | mitigate | HTTPBearer dependency validates Authorization: Bearer against config.INGEST_TOKEN; empty token config returns 403 (fail-closed); token is a 64-hex random string generated by `secrets.token_hex(32)`. |
| T-01-02 | Tampering | Ingest payload | medium | mitigate | `_deserialize_listing` filters incoming dict keys against `dc_fields(Listing)` so extra keys cannot inject state; per-listing try/except means one malformed dict cannot corrupt the batch. |
| T-01-03 | Repudiation | Ingest & heartbeat calls | low | accept | Existing logging captures source + counts; single-user personal tool; no audit log requirement. |
| T-01-04 | Information disclosure | Log emissions | medium | mitigate | Ingest handler and auth dependency MUST NOT log the INGEST_TOKEN value or the presented Bearer credential; only "auth ok" / "auth failed" and non-secret metadata (source, listing_count). |
| T-01-05 | Denial of service | POST /api/ingest lock contention | low | accept | Single mini PC client; one POST per CHECK_INTERVAL_HOURS; lock hold time is proportional to batch size, and single-user scale keeps batches small (<50 listings/run). |
| T-01-06 | Elevation of privilege | Ingest bypass via Caddy | high | mitigate | Caddyfile named matcher only exposes /api/ingest and /api/heartbeat without basicauth; all other paths (including future /api/* routes) remain behind basicauth by falling through to the default handle block. |
</threat_model>

<output>
Create `.planning/phases/01-scraper-architecture-split/01-02-SUMMARY.md` when done, noting:
- Files created/modified/renamed and confirmation that `kv_alert_reader.py` no longer exists as an importable module.
- The VPS `.env` INGEST_TOKEN value (or a note that it was set out-of-band) and confirmation that it matches the mini PC .env value from plan 01-01.
- Attached curl outputs from the human-check verification.
- Any Caddyfile syntax caveats encountered (Caddy sometimes reformats the file on first run — that's fine as long as the named matcher survives).
</output>
