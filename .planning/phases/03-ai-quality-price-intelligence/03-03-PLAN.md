---
phase: 03-ai-quality-price-intelligence
plan: 03
type: execute
wave: 3
status: complete
completed_at: "2026-07-08"
depends_on:
  - 03-02
files_modified:
  - app/data_store.py
  - app/ingest_handler.py
  - app/static/index.html
  - app/tests/test_price_intelligence.py
requirements:
  - INTEL-01
  - INTEL-02
autonomous: true
tags:
  - price-history
  - data-model
  - frontend
must_haves:
  truths:
    - "DEFAULT_APP_DATA and load_app_data include a price_history top-level key initialized to {} — zero-downtime migration via setdefault (D-11, INTEL-01)"
    - "record_price_in_data(data, listing_id, price_eur, date_str) mutates data['price_history'][listing_id] in place: appends a new {date, price} entry OR overwrites the last entry's price when the date matches (idempotent same-day re-runs) (D-11, D-12, INTEL-01)"
    - "get_price_history(listing_id) returns the list of {date, price} entries for a listing (or [] if not found), acquiring _lock for the read (INTEL-01)"
    - "process_ingest_batch records the current price for EVERY listing on EVERY scrape run — new AND already-seen dedup-hit listings — via record_price_in_data before the dedup-continue skip (D-12)"
    - "GET /api/data response payload includes the price_history key (no backend routing change needed since /api/data returns load_app_data() verbatim)"
    - "The dossier main card (renderMain) shows a Days-on-market number computed as today - price_history[id][0].date (D-13, INTEL-02)"
    - "3 of the 9 test_price_intelligence.py stubs (test_record_price_new, test_record_price_idempotent, test_ingest_records_price_for_known) flip from XFAIL to PASSED"
  artifacts:
    - "DEFAULT_APP_DATA[price_history] key + load_app_data setdefault line"
    - "data_store.record_price_in_data(data, listing_id, price_eur, date_str) — no-lock in-place mutator"
    - "data_store.get_price_history(listing_id) — thread-safe reader"
    - "ingest_handler dedup-branch change: known-listing path records price before continue"
    - "index.html state.priceHistory field + daysOnMarket(id) helper + buildPriceHistoryEl(id) helper + renderMain integration"
  key_links:
    - "record_price_in_data is called from process_ingest_batch INSIDE the outer with data_store._lock: block (no internal locking); caller ensures data mutation is written back via save_app_data at loop end"
    - "GET /api/data is unchanged — returns load_app_data() verbatim; frontend simply reads the new price_history top-level key from the response"
    - "renderMain and buildPendingCard both call daysOnMarket(id) and buildPriceHistoryEl(id); missing history renders nothing (safe null branch)"
---

<objective>
Deliver end-to-end price history tracking + days-on-market display: every scrape run records the current price for every listing (new AND known) into a new price_history top-level key, the /api/data endpoint returns it automatically, and the dossier card renders a plain-text price history list + a days-on-market count.

Purpose: Give Daniel visibility into how long a listing has been on the market and whether the price has moved — foundational data for plan 03-04's automatic re-evaluation on drops, and standalone useful for INTEL-02.

Output: Data model migration, two data_store helpers (record_price_in_data no-lock mutator + get_price_history thread-safe reader), ingest_handler dedup-branch update, frontend priceHistory state field + rendering helpers, and 3 INTEL-01 test stubs flipped to PASS.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-quality-price-intelligence/03-CONTEXT.md
@.planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md
@.planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md
@.planning/phases/03-ai-quality-price-intelligence/03-VALIDATION.md
@.planning/phases/03-ai-quality-price-intelligence/03-01-SUMMARY.md
@.planning/phases/03-ai-quality-price-intelligence/03-02-SUMMARY.md
@.claude/CLAUDE.md

# Source files the executor MUST read before touching anything
@app/data_store.py
@app/ingest_handler.py
@app/static/index.html
@app/main.py
@app/tests/test_price_intelligence.py
@app/tests/test_pending.py
@app/tests/conftest.py
</context>

<artifacts_this_phase_produces>
New symbols in plan 03-03:
- app.data_store.DEFAULT_APP_DATA["price_history"] initialized to {} 
- app.data_store.record_price_in_data(data, listing_id, price_eur, date_str) — no-lock in-place mutator
- app.data_store.get_price_history(listing_id) — thread-safe reader returning list
- state.priceHistory field in index.html (populated from GET /api/data response)
- daysOnMarket(listingId) JS helper
- buildPriceHistoryEl(listingId) JS helper

Symbols extended in plan 03-03:
- load_app_data() — adds data.setdefault("price_history", {}) migration line
- process_ingest_batch() — records price for every listing including dedup hits (before continue)
- renderMain() and/or buildPendingCard() — appends the days-on-market label + price history list element

Downstream consumers:
- Plan 03-04 reads get_price_history to compute drop % (last two entries), triggers _handle_price_drop
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add price_history data model + record/get helpers + ingest recording on every scrape</name>
  <files>app/data_store.py, app/ingest_handler.py, app/tests/test_price_intelligence.py</files>
  <read_first>
    - app/data_store.py (DEFAULT_APP_DATA at lines 45-51; load_app_data setdefault pattern at lines 82-90; RLock at line 26; save_app_data at lines 93-95)
    - app/ingest_handler.py (process_ingest_batch loop at lines 51-131; dedup continue at lines 62-64 which currently drops known listings — this is the key change point; app_data reload pattern at line 120)
    - app/tests/test_price_intelligence.py (Wave 0 xfail stubs: test_record_price_new, test_record_price_idempotent, test_ingest_records_price_for_known)
    - app/tests/test_pending.py (data_store._lock seeding pattern at lines 209-230; monkeypatch lambda pattern for evaluate_listing accepting context_prefix="")
    - app/tests/conftest.py (client, tmp_agent_state, mock_send_pending_card fixtures)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 3 (price_history data model with setdefault migration)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 4 (record_price_in_data no-lock mutator + get_price_history thread-safe reader)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 5 (dedup+continue change to record-price+check-drop)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pitfall 5 (cap history at 90 entries per listing)
    - .planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md app/data_store.py section (helper shapes) and app/ingest_handler.py section (dedup pattern change)
  </read_first>
  <behavior>
    - After the migration, load_app_data always returns a dict containing a price_history key mapped to a dict — even on a fresh install AND on an already-deployed VPS with an existing app_data.json missing the key
    - record_price_in_data(data, listing_id, price_eur, date_str) mutates data in place: navigate data.setdefault("price_history",{}).setdefault(listing_id, []); if the last entry's date equals date_str, overwrite its price (idempotent same-day re-run); else append a new {date, price} entry. Additionally cap the history at the last 90 entries per listing (RESEARCH Pitfall 5)
    - get_price_history(listing_id) returns [] when the listing is unknown; otherwise returns the list; acquires _lock via load_app_data()
    - process_ingest_batch computes today_str once BEFORE the batch loop starts (single date snapshot for the whole batch); inside the loop, records the price for EVERY listing (dedup hit or not) whose price_eur is a positive int, using record_price_in_data(app_data, listing.id, listing.price_eur, today_str)
    - For dedup hits, price is recorded BEFORE the continue statement so history captures every scrape (D-12) — the continue then skips filters + evaluation for the known listing
    - The batch mutates a data dict that is saved once at loop end (single save_app_data(app_data) call) instead of relying on individual helper writes
    - test_record_price_new PASSES: seeds an empty state, calls record_price_in_data, saves, then get_price_history returns the recorded entry
    - test_record_price_idempotent PASSES: two record_price_in_data calls for the same listing_id and same date result in a single entry (the second call overwrites the price)
    - test_ingest_records_price_for_known PASSES: two POST /api/ingest calls with the same listing (mocking evaluate_listing to accept context_prefix=""); after the second call, app_data.price_history[listing_id] contains at least 1 entry
  </behavior>
  <action>
In app/data_store.py:
1. Extend DEFAULT_APP_DATA (currently lines 45-51) by adding a price_history key initialized to an empty dict — the value is a mapping of listing_id to a list of {date, price} entries. Add a trailing comment noting D-11.
2. Extend load_app_data (currently lines 82-90) by adding data.setdefault("price_history", {}) after the existing rejected setdefault line — zero-downtime migration per D-11.
3. Add a new helper record_price_in_data(data, listing_id, price_eur, date_str) that mutates data in place with NO lock acquisition (caller holds _lock) and NO file I/O. Body: navigate history = data.setdefault("price_history",{}).setdefault(listing_id, []). If history is non-empty AND history[-1]["date"] == date_str: overwrite history[-1]["price"] = price_eur (idempotent same-day). Else: history.append({"date": date_str, "price": price_eur}). After append, cap: if len(history) > 90: del history[:len(history)-90] — keep the tail 90 entries (RESEARCH Pitfall 5, D-13 relies on history[0] being the earliest kept entry, so the cap trims the OLDEST rows first).
4. Add a new helper get_price_history(listing_id) that acquires _lock via load_app_data() and returns data.get("price_history",{}).get(listing_id, []). Return an empty list on any KeyError.

In app/ingest_handler.py:
1. Import date as _date from datetime at the module top (extend the existing "from datetime import datetime, timezone" line to also import date as _date).
2. Refactor process_ingest_batch to hold a single app_data reference throughout the batch instead of relying on data_store.load_app_data() calls scattered across the loop:
   - After the existing "state = data_store.load_agent_state()" call, add: app_data = data_store.load_app_data() and today_str = _date.today().isoformat() (both once, before the for loop).
   - Change all data_store.load_app_data() calls inside the for loop body to reuse app_data (this includes the block at lines 120-126 that patches tg_message_id — mutate app_data in place instead).
   - The two data_store.add_to_pending(pending_entry) and data_store.write_checklist_ai(...) calls stay unchanged for now (they load their own copy) but their writes will race with the outer save unless we serialize; because _lock is an RLock and each helper acquires it internally and immediately saves, the ordering is: add_to_pending saves file → we later save app_data over it. To avoid clobbering the checklist and pending writes, either (a) reload app_data after each add_to_pending/write_checklist_ai call before mutating further, or (b) mutate app_data["pending"] in-place ourselves and skip add_to_pending. Chosen approach: continue to call data_store.add_to_pending and data_store.write_checklist_ai (they self-save), then reload app_data via data_store.load_app_data() into the same variable so subsequent price_history mutations pick up the fresh state, then at loop end save_app_data(app_data) writes only the price_history changes. This preserves add_to_pending/write_checklist_ai semantics while adding price_history in the outer save.
3. Rewrite the dedup-continue block (currently lines 62-64):
   - Compute dedup_key as before via extract_object_id(listing.url) or listing.url.
   - If dedup_key in seen_set: if isinstance(listing.price_eur, int) and listing.price_eur > 0: data_store.record_price_in_data(app_data, listing.id, listing.price_eur, today_str). Then continue (skip filters/evaluation for the known listing per D-12).
   - If dedup_key NOT in seen_set: fall through to the existing append-to-seen + filter + evaluate flow. Additionally, AFTER add_to_pending + write_checklist_ai and after any tg_message_id patch, reload app_data via data_store.load_app_data() and call record_price_in_data(app_data, listing.id, listing.price_eur, today_str) if listing.price_eur is a positive int — this records the initial price for newly-seen listings.
4. At the end of process_ingest_batch, AFTER data_store.save_agent_state(state) and BEFORE returning: call data_store.save_app_data(app_data) so the price_history mutations persist. This save runs while still inside the outer with data_store._lock: block.
5. Guard record_price_in_data calls with try/except Exception logging log.exception (never-raise pattern per D convention) — do NOT let a price-recording failure kill the batch.

In app/tests/test_price_intelligence.py:
1. Replace the xfail stub bodies for test_record_price_new, test_record_price_idempotent, test_ingest_records_price_for_known with real implementations. Remove the @pytest.mark.xfail decorator on ONLY these three tests.

test_record_price_new(tmp_agent_state,): under with data_store._lock, load app_data, call data_store.record_price_in_data(data, "test-1", 200000, "2026-07-01"), then save_app_data(data). Assert data_store.get_price_history("test-1") == [{"date":"2026-07-01","price":200000}].

test_record_price_idempotent(tmp_agent_state,): same-day double-record. Seed via with lock: record_price_in_data(data, "test-1", 200000, "2026-07-01"); record_price_in_data(data, "test-1", 195000, "2026-07-01"); save. Assert get_price_history returns exactly ONE entry with price 195000 (the second value overwrites).

test_ingest_records_price_for_known(client, tmp_agent_state, mock_send_pending_card, monkeypatch): monkeypatch ingest_handler.evaluate_listing to a lambda accepting (listing, context_prefix="") returning score 80, verdict Good, strengths [], concerns [], draft_body b, checklist a full 7-key dict of unknowns. POST payload (id test-1, url https://www.kv.ee/test-1.html, title Test, price_eur 200000, rooms 3, area_sqm 60.0, image_count 10, raw_ok True) with Bearer test-token-abc. Then POST the SAME payload a second time (same id). Read app_data_file = tmp_agent_state / app_data.json; app_data = json.loads(app_data_file.read_text()). Assert "test-1" in app_data.get("price_history",{}) AND len(app_data["price_history"]["test-1"]) >= 1.

Cite per D-11, D-12, D-13, RESEARCH Pattern 3/4/5, Pitfall 5.
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_price_intelligence.py::test_record_price_new tests/test_price_intelligence.py::test_record_price_idempotent tests/test_price_intelligence.py::test_ingest_records_price_for_known -v</automated>
  </verify>
  <acceptance_criteria>
    - The 3 named tests report PASSED — 0 FAILED
    - `grep -c "price_history" app/data_store.py` returns at least 3 (DEFAULT_APP_DATA key, load_app_data setdefault line, helpers)
    - `grep -c "def record_price_in_data" app/data_store.py` returns 1
    - `grep -c "def get_price_history" app/data_store.py` returns 1
    - `grep -c "record_price_in_data" app/ingest_handler.py` returns at least 2 (call in dedup-hit branch + call in new-listing branch)
    - `grep -c "today_str" app/ingest_handler.py` returns at least 2 (definition + at least one use)
    - `grep -c "from datetime import" app/ingest_handler.py` returns 1 line and it includes date as _date (or plain date) alongside datetime and timezone
    - The full existing test suite still passes: `cd app && python -m pytest tests/ -x -q` — 3 tests flipped to PASS; 6 stubs still XFAIL (2 in test_eval_quality reserved for later + 6 in test_price_intelligence — wait, 4 remain here; total XFAIL count is 4 eval + 6 intel - 3 flipped = 4 + 6 = 10 currently in files minus 3 flipped = 7 XFAIL total after this task)
    - `cd app && python -c "import data_store; d = data_store.load_app_data(); assert 'price_history' in d and isinstance(d['price_history'], dict); print('OK')"` prints OK (migration lands on fresh install)
    - Same-day idempotency validated by the automated verify above
    - No file corruption: `python -c "import json; json.loads(open('app/tests/test_price_intelligence.py').read())" || true` — file remains valid Python (best-effort compile check via py_compile below)
    - `cd app && python -c "import py_compile; py_compile.compile('data_store.py', doraise=True); py_compile.compile('ingest_handler.py', doraise=True); print('OK')"` prints OK
  </acceptance_criteria>
  <done>DEFAULT_APP_DATA + load_app_data include price_history; record_price_in_data + get_price_history helpers implemented; process_ingest_batch records prices for both known and new listings on every scrape; today_str computed once per batch; 3 INTEL-01 tests flipped to PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend — expose price_history + days-on-market on the dossier card</name>
  <files>app/static/index.html</files>
  <read_first>
    - app/static/index.html (state object at line 472; loadData at lines 496-509; saveData at 511-520; renderMain at 763-815; buildPendingCard at 1143-1207; buildAiChecklistEl reference from plan 03-02)
    - app/main.py (GET /api/data endpoint — confirm it returns data_store.load_app_data() verbatim, so price_history flows through automatically)
    - .planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md app/static/index.html section (state.priceHistory extension, daysOnMarket helper, buildPriceHistoryEl helper — vanilla JS + textContent-only)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 9 (frontend rendering — days-on-market and price history plain text list, no chart per deferred)
  </read_first>
  <behavior>
    - state.priceHistory is a dict populated from parsed.price_history on every loadData() invocation; defaults to {} when the API response omits the key
    - daysOnMarket(listingId) returns an integer (days since price_history[id][0].date) or null when the listing has no history; JS date arithmetic uses Math.floor((now - first) / 86400000)
    - buildPriceHistoryEl(listingId) returns a DOM div containing one row per history entry, formatted as "YYYY-MM-DD — 200 000 €" using textContent; returns null when the listing has no history
    - renderMain appends a "Days on market: N" label AND the price history element (when non-null) to the head-actions row of the dossier card
    - buildPendingCard also appends the days-on-market label to its meta line (append " · N days on market" when non-null)
    - Neither renderMain nor buildPendingCard introduces innerHTML for any new element built by these helpers
    - saveData does NOT include price_history in its PUT body — price_history is server-owned and never round-tripped through the frontend
  </behavior>
  <action>
In app/static/index.html:
1. Extend the state object literal (currently line 472). Add a priceHistory field initialized to {}.
2. Extend loadData (lines 496-509) to also assign state.priceHistory = parsed.price_history || {} in the try block after state.checklists is populated.
3. Do NOT change saveData (line 511-520) — priceHistory must NOT be included in the PUT body (server-owned, per D-11/D-12).
4. Add a top-level helper daysOnMarket(listingId) that reads state.priceHistory[listingId]; returns null when missing or empty; else parses new Date(hist[0].date), computes Math.floor((now - first) / 86400000), returns the integer. Place this function near the other utility functions (before buildPendingCard) so both renderMain and buildPendingCard can call it.
5. Add a top-level helper buildPriceHistoryEl(listingId) that reads state.priceHistory[listingId]; returns null when missing or empty; else creates a wrapper div with fontFamily var(--font-mono), fontSize 11px, color var(--ink-soft), marginTop 8px; for each entry creates a row div with textContent = entry.date + " — " + Number(entry.price).toLocaleString("et-EE") + " €"; appends each row; returns the wrapper.
6. Extend renderMain (lines 763-815) — when a property p is selected, insert a Days-on-market label and the price history block into the head-actions area near the export/delete buttons. Because renderMain currently uses innerHTML assembly at line 814 (el.innerHTML = head + overview + notes + sectionsHtml), you MUST inject the new elements POST-innerHTML: after el.innerHTML = ... at line 814, if daysOnMarket(p.id) is not null, create a small div with textContent "Days on market: " + n + " days" and append it to el.querySelector(".head-actions") (or the closest element via el.querySelector). Similarly append buildPriceHistoryEl(p.id) if non-null. This preserves the existing head-actions innerHTML while adding safe textContent nodes.
7. Extend buildPendingCard (line 1143-1207): inside the metaParts.filter(Boolean) array, add a conditional entry for days-on-market — computed via daysOnMarket(entry.id) — that emits "N days on market" when non-null, or "" when null (filter(Boolean) drops the empty string). This appends the label to the existing meta line joined with " · " so the pending card also shows longevity.
8. Do NOT add innerHTML for any new element. All new nodes use document.createElement + textContent per existing convention.

Cite per D-11, D-12, D-13, INTEL-01, INTEL-02, RESEARCH Pattern 9.
  </action>
  <verify>
    <automated>python -c "s = open('app/static/index.html').read(); assert 'priceHistory' in s and 'daysOnMarket' in s and 'buildPriceHistoryEl' in s and 'toLocaleString(\"et-EE\")' in s; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "state.priceHistory" app/static/index.html` returns at least 2 (declaration in state object + assignment in loadData)
    - `grep -c "function daysOnMarket" app/static/index.html` returns 1
    - `grep -c "function buildPriceHistoryEl" app/static/index.html` returns 1
    - `grep -c "daysOnMarket(p.id)\|daysOnMarket(entry.id)" app/static/index.html` returns at least 2 (called from renderMain and buildPendingCard)
    - `grep -c "buildPriceHistoryEl(p.id)" app/static/index.html` returns at least 1 (renderMain integration)
    - saveData body does NOT include priceHistory: `grep -A5 "async function saveData" app/static/index.html | grep -c "priceHistory"` returns 0
    - JS syntax sanity: `node -e "const fs=require('fs'); const html=fs.readFileSync('app/static/index.html','utf8'); const start=html.indexOf('<script>'); const end=html.lastIndexOf('</script>'); new Function(html.substring(start+8, end)); console.log('OK')"` prints OK — best-effort inline-script parse (Node available in dev env; skip if not present, use a fallback: `python -c "print('skipped')" && exit 0`)
    - Fallback JS check when Node unavailable: `python -c "import re; s = open('app/static/index.html').read(); assert s.count('function daysOnMarket') == 1 and s.count('function buildPriceHistoryEl') == 1; print('OK')"` prints OK
    - Full test suite still passes: `cd app && python -m pytest tests/ -x -q`
  </acceptance_criteria>
  <done>state.priceHistory populated from API; daysOnMarket + buildPriceHistoryEl helpers implemented; renderMain appends Days-on-market label + history block to the dossier card via textContent-only DOM insertion; buildPendingCard adds "N days on market" to its meta line; saveData does not round-trip price_history.</done>
</task>

</tasks>

<verification>
Plan-level checks after both tasks complete:

1. `cd app && python -m pytest tests/ -v` — INTEL-01 tests (record_price_new, record_price_idempotent, ingest_records_price_for_known) PASS; remaining Wave 0 stubs still XFAIL (4 in test_eval_quality initially minus 2 already flipped in 03-02 = 4 total XFAIL in test_eval_quality; 9 in test_price_intelligence minus 3 flipped = 6 XFAIL in test_price_intelligence — actually 4 remain after 03-02 in test_eval_quality is wrong; recount: 03-01 wrote 6 xfail in test_eval_quality, 03-02 flipped 2 → 4 remain, 03-04 flips the rest. 03-01 wrote 9 xfail in test_price_intelligence, 03-03 flips 3 → 6 remain, 03-04 flips the rest)
2. `grep -c "price_history" app/main.py` returns 0 — no backend routing changes needed (GET /api/data already returns load_app_data verbatim; if the executor discovers otherwise during read_first, they update main.py)
3. Manual sanity (optional): open dossier in browser after triggering an ingest; verify a "Days on market" number appears on any selected listing card and the price history list renders below the head-actions row
4. `cd app && python -c "import data_store; d = data_store.load_app_data(); assert 'price_history' in d; print('OK')"` prints OK on a fresh install
</verification>

<success_criteria>
- Price history recorded on every scrape for every listing (new + known)
- Same-day double-scrape is idempotent (no duplicate entries per day)
- History capped at 90 entries per listing (bounded growth per Pitfall 5)
- Days-on-market visible on dossier card and pending card
- Price history plain-text list visible on dossier card (chart deferred per CONTEXT)
- 3 INTEL-01 tests flipped to PASS
- No new packages required
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper-client → VPS ingest | listing.price_eur field is untrusted input from the mini PC payload; type-check before arithmetic |
| VPS → Browser | price_history rendered into DOM as text; textContent-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | listing.price_eur could be non-integer or negative from a malformed scraper payload | medium | mitigate | Guard record_price_in_data call with isinstance(listing.price_eur, int) and listing.price_eur > 0 before invocation (RESEARCH Security Domain) |
| T-03-09 | Denial of Service | price_history dict grows unbounded across months of scraping | medium | mitigate | Cap history at 90 entries per listing inside record_price_in_data (RESEARCH Pitfall 5); at 100 listings x 90 entries x ~40 bytes = 360 KB, bounded |
| T-03-10 | Tampering | listing_id path into price_history could contain filesystem-unsafe characters | low | mitigate | listing_id is derived from extract_object_id(url) — already validated regex-restricted to \d{6,8} in kv_listing_parser (VERIFIED in RESEARCH) |
| T-03-11 | Information Disclosure | price_history returned via GET /api/data behind Caddy basic auth — same threat surface as existing data | low | accept | Existing auth boundary covers all app_data content; no new exposure |
| T-03-12 | Tampering (XSS) | Price history date/price rendered into DOM | high | mitigate | buildPriceHistoryEl uses textContent exclusively; Number(entry.price).toLocaleString() coerces to string; entry.date is a server-controlled ISO-8601 string |
| T-03-SC | Tampering | No new package installs in this plan | low | accept | No additions to requirements.txt |
</threat_model>

<output>
Create `.planning/phases/03-ai-quality-price-intelligence/03-03-SUMMARY.md` when done, following @.claude/gsd-core/templates/summary.md.
</output>
