---
phase: 03-ai-quality-price-intelligence
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - app/ingest_handler.py
  - app/data_store.py
  - app/static/index.html
  - app/tests/test_price_intelligence.py
  - app/tests/test_eval_quality.py
requirements:
  - EVAL-04
  - INTEL-03
  - EVAL-01
  - EVAL-03
autonomous: true
status: complete
completed_at: 2026-07-09
tags:
  - re-evaluation
  - price-drop
  - removed-listing
  - telegram
must_haves:
  truths:
    - "process_ingest_batch, after recording a new price via record_price_in_data, computes the price drop against the previous history entry; when (prev - new) / prev >= config.PRICE_DROP_THRESHOLD (default 0.05) it triggers _handle_price_drop (D-14, EVAL-04)"
    - "_handle_price_drop dispatches by listing state: approved listing in properties[] re-evaluates + updates score/verdict + appends a note + sends Telegram notification (D-15); pending listing in pending[] re-evaluates silently + updates score/verdict/checklist (D-16); rejected listing with rejection_reason=='price' re-queues as a new pending entry with a price-drop note (D-17); rejected with other reason is untouched (D-17)"
    - "process_ingest_batch, after the batch loop, marks listings with raw_ok=False as removed:true + removed_at:'YYYY-MM-DD' on their entry in properties[] or pending[] (D-18, INTEL-03)"
    - "The remaining 4 test_eval_quality stubs (test_anchor_injection, test_anchor_skipped_below_threshold, test_district_avg_injected, test_district_avg_omitted_unknown) are green (xfail decorator removed)"
    - "The remaining 6 test_price_intelligence stubs (test_price_drop_reeval_pending, test_price_drop_below_threshold_no_reeval, test_price_rejected_requeued, test_location_rejected_not_requeued, test_removed_listing_marked, test_removed_listing_marked_pending) are green (xfail decorator removed)"
    - "The dossier card renders a Removed badge with the removed_at date when a listing has removed:true (INTEL-03)"
  artifacts:
    - "ingest_handler._record_and_check_price_drop(app_data, listing, today_str) — private orchestrator combining record + drop-detection"
    - "ingest_handler._handle_price_drop(app_data, listing, prev_price, new_price) — re-evaluation dispatcher"
    - "ingest_handler._mark_removed_listings(app_data, batch_dicts) — post-loop removed marker"
    - "data_store.get_rejected_by_reason(reason) — thread-safe reader for the rejected[] listing whose id + rejection_reason match"
    - "Removed badge rendering in index.html renderMain + buildPendingCard"
  key_links:
    - "_handle_price_drop calls _build_context_prefix + evaluate_listing (both landed in 03-01) — anchors ensure re-scoring uses the same calibration path as new listings"
    - "For pending-listing re-evaluation, _handle_price_drop calls data_store.write_checklist_ai (landed in 03-02) so the checklist is refreshed"
    - "Telegram notification uses telegram_client.send_message (existing; no new dependency)"
    - "Removed-listing marker runs AFTER the batch loop completes, so single-run detection catches raw_ok=False signals from the scraper"
---

<objective>
Deliver the last two behaviors of Phase 3: (1) when a scrape delivers a price ≥5% below the previous entry for a known listing, automatically re-evaluate the listing and dispatch by its current state (approved: notify Daniel; pending: update silently; price-rejected: re-queue); (2) when the scraper reports raw_ok=False for a listing, mark it removed on its dossier or pending entry. Flip the last 10 Wave 0 stubs to PASS.

Purpose: Close the intelligence loop — Daniel gets alerted when a previously-scored listing gets cheaper enough to reconsider, and stale listings that disappear from kv.ee stop cluttering the dossier.

Output: Three new ingest_handler private helpers, one new data_store reader, Removed badge rendering, and 10 test stubs flipped to PASS. All 7 phase requirements green.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-quality-price-intelligence/03-CONTEXT.md
@.planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md
@.planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md
@.planning/phases/03-ai-quality-price-intelligence/03-VALIDATION.md
@.planning/phases/03-ai-quality-price-intelligence/03-01-SUMMARY.md
@.planning/phases/03-ai-quality-price-intelligence/03-02-SUMMARY.md
@.planning/phases/03-ai-quality-price-intelligence/03-03-SUMMARY.md
@.claude/CLAUDE.md

# Source files the executor MUST read before touching anything
@app/ingest_handler.py
@app/data_store.py
@app/ai_evaluator.py
@app/telegram_client.py
@app/static/index.html
@app/config.py
@app/kv_listing_parser.py
@app/tests/test_price_intelligence.py
@app/tests/test_eval_quality.py
@app/tests/test_pending.py
@app/tests/conftest.py
</context>

<artifacts_this_phase_produces>
New symbols in plan 03-04:
- ingest_handler._record_and_check_price_drop(app_data, listing, today_str)
- ingest_handler._handle_price_drop(app_data, listing, prev_price, new_price)
- ingest_handler._mark_removed_listings(app_data, batch_dicts)
- data_store.get_rejected_by_reason(reason: str) — thread-safe reader returning list of rejected entries with matching rejection_reason

Symbols extended in plan 03-04:
- process_ingest_batch — invokes _record_and_check_price_drop on both dedup-hit and new-listing branches; invokes _mark_removed_listings after the loop
- renderMain and buildPendingCard — render a "Removed from kv.ee — {date}" badge when p.removed is true

Phase 3 complete after this plan: all 7 requirement IDs green.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement price-drop detection + re-evaluation dispatch + removed-listing marking</name>
  <files>app/ingest_handler.py, app/data_store.py</files>
  <read_first>
    - app/ingest_handler.py (process_ingest_batch structure post-03-03 — record_price_in_data calls in dedup-hit and new-listing branches, _build_context_prefix from 03-01, _whitelist_checklist from 03-02)
    - app/data_store.py (RLock at line 26; add_to_pending at lines 109-120; approve_listing at 129-140; reject_listing at 143-166 — model for the new get_rejected_by_reason helper; _pending_to_property at 169-205 — DO NOT mutate; understand shape for constructing new pending entries)
    - app/ai_evaluator.py (evaluate_listing(listing, context_prefix="") signature from 03-01)
    - app/telegram_client.py — send_message(text) signature; understand chat_id guard for send-only path
    - app/kv_listing_parser.py (Listing dataclass at lines 50-71 including raw_ok field at line 71)
    - app/config.py (PRICE_DROP_THRESHOLD constant from 03-01; TELEGRAM_CHAT_ID etc.)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 5 (price drop detection flow — data mutation in place)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 6 (re-evaluation dispatch by state: approved/pending/rejected)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 7 (sold/removed detection — raw_ok=False signal from scraper)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pitfalls 1 (RLock reentrancy), 6 (score change confusion — append to notes, don't overwrite), 7 (long API calls under lock — existing limitation, do not change)
  </read_first>
  <behavior>
    - _record_and_check_price_drop(app_data, listing, today_str) is called in place of the bare record_price_in_data call in both branches (dedup-hit and new-listing). Body:
      1. Guard: return early if not (isinstance(listing.price_eur, int) and listing.price_eur > 0)
      2. Read prior history via app_data.get("price_history",{}).get(listing.id, [])
      3. Capture prev_entry = history[-1] if history is non-empty AND its date != today_str (so same-day overwrites do not trigger drop detection)
      4. Call data_store.record_price_in_data(app_data, listing.id, listing.price_eur, today_str) — mutates in place
      5. If prev_entry exists AND prev_entry.price > 0 AND (prev_entry.price - listing.price_eur) / prev_entry.price >= config.PRICE_DROP_THRESHOLD: call _handle_price_drop(app_data, listing, prev_entry.price, listing.price_eur)
      6. Wrap the whole body in try/except Exception: log.exception + return (never-raise per D convention)
    - _handle_price_drop(app_data, listing, prev_price, new_price) dispatches by state:
      1. Build context_prefix = _build_context_prefix(listing, app_data)
      2. Call evaluate_listing(listing, context_prefix) — captured as evaluation dict
      3. Compute pct = round((prev_price - new_price) / prev_price * 100, 1)
      4. Search app_data["properties"] for id match → if found: update entry["score"] and entry["verdict"] from evaluation; append a price-drop note to entry["notes"] (PREPEND, do not overwrite — RESEARCH Pitfall 6): entry["notes"] = f"[Price drop {pct}%, re-scored {entry['score']}/100] " + (entry.get("notes") or ""). Then call telegram_client.send_message with a formatted message per RESEARCH Pattern 6 including listing title, prev_price, new_price, pct, and new score
      5. Else search app_data["pending"] for id match → if found: update entry["score"], entry["verdict"], entry["strengths"], entry["concerns"] from evaluation (D-16 silent update). Also call data_store.write_checklist_ai(listing.id, _whitelist_checklist(evaluation.get("checklist", {}))) to refresh the AI checklist. Do NOT send Telegram.
      6. Else search app_data["rejected"] for id match → if found AND entry.get("rejection_reason") == "price" (D-17): construct a new pending entry as a copy of the rejected entry with rejection_reason and rejected_at removed, score/verdict/strengths/concerns from evaluation, queued_at set to current UTC ISO string, price_drop_requeue_note set to "Previously rejected for price. Price dropped {pct}% to {new_price:,} EUR.". Append to app_data["pending"] and remove the entry from app_data["rejected"] (rebuild the list without it). Also send a Telegram notification via send_message noting the re-queue.
      7. Rejected entries with rejection_reason != "price" are silently untouched (D-17)
      8. Wrap the whole body in try/except Exception: log.exception + return (never-raise)
    - _mark_removed_listings(app_data, batch_dicts) runs after the batch loop (before save_agent_state / save_app_data):
      1. For each item in batch_dicts where item.get("raw_ok", True) is False: extract listing_id via item.get("id") or extract_object_id(item.get("url","")); skip if empty
      2. today_str via date.today().isoformat()
      3. Walk app_data["properties"] AND app_data["pending"]; for any entry whose id matches AND removed is not already true: set entry["removed"] = True and entry["removed_at"] = today_str; log.info with listing id
      4. Wrap in try/except Exception: log.exception + return (never-raise)
    - data_store.get_rejected_by_reason(reason) is a thread-safe reader: with _lock: load_app_data(); return [e for e in data.get("rejected",[]) if e.get("rejection_reason") == reason]
    - process_ingest_batch integrates:
      - Dedup-hit branch: replace the plain data_store.record_price_in_data call with _record_and_check_price_drop(app_data, listing, today_str)
      - New-listing branch: replace the plain record_price_in_data call with _record_and_check_price_drop(app_data, listing, today_str) — same helper covers both paths
      - After the for loop closes and BEFORE save_agent_state: call _mark_removed_listings(app_data, listing_dicts) — pass the ORIGINAL batch dicts (not the deserialized Listings) because raw_ok is on the incoming dict
  </behavior>
  <action>
In app/data_store.py:
1. Add a new helper get_rejected_by_reason(reason) — thread-safe reader following the shape of load_pending (with _lock: load, filter, return). Return a list of matching rejected entries. Return an empty list on any KeyError.

In app/ingest_handler.py:
1. Add a new private helper _record_and_check_price_drop(app_data, listing, today_str). Body per behavior block. Uses data_store.record_price_in_data (already imported via data_store module). Detects same-day overwrite by comparing history[-1]["date"] == today_str BEFORE recording — this avoids self-triggering when the same batch replays. Uses config.PRICE_DROP_THRESHOLD for the threshold constant (imported via config module).
2. Add a new private helper _handle_price_drop(app_data, listing, prev_price, new_price). Body per behavior block. Uses telegram_client.send_message (already imported). Uses evaluate_listing (already imported). Uses _build_context_prefix (already in this module). Uses data_store.write_checklist_ai and _whitelist_checklist (both landed in 03-02).
3. Add a new private helper _mark_removed_listings(app_data, batch_dicts). Body per behavior block. Uses extract_object_id (already imported).
4. Modify process_ingest_batch:
   - In the dedup-hit branch (currently calls record_price_in_data directly per 03-03), replace that call with _record_and_check_price_drop(app_data, listing, today_str). Guard remains: only call when isinstance(listing.price_eur, int) and listing.price_eur > 0 (that guard already lives inside _record_and_check_price_drop, so it can also be dropped from the caller — pick one location for the guard and note the choice).
   - In the new-listing branch (post add_to_pending + write_checklist_ai), replace the record_price_in_data call with _record_and_check_price_drop(app_data, listing, today_str). NOTE: for a newly-seen listing, prior history is empty, so the drop path is a no-op — safe to always call.
   - AFTER the for loop closes and BEFORE data_store.save_agent_state(state), call _mark_removed_listings(app_data, listing_dicts) passing the original input dicts (not the deserialized Listings).
   - The final data_store.save_app_data(app_data) call from 03-03 stays — it now also persists removed markers and any pending-entry re-evaluation updates.
5. Guard evaluate_listing call inside _handle_price_drop with its own try/except (in addition to the outer never-raise wrap) so a network failure during re-evaluation logs but does not corrupt state — return early instead of writing a partial evaluation.
6. Telegram notification format for approved-listing drops: use the text template from RESEARCH Pattern 6: f"Price drop on {listing.title or listing.id}: {prev_price:,} → {new_price:,} EUR (-{pct}%). Re-scored: {new_score}/100." — no emoji required (send_message strips or preserves per existing convention). Do not include images.
7. Telegram notification format for price-rejected re-queue: send_message with a short note like f"Previously price-rejected {listing.title or listing.id} re-queued: {prev_price:,} → {new_price:,} EUR (-{pct}%). Now: {new_score}/100." so Daniel knows about the re-queue.

Cite per D-14, D-15, D-16, D-17, D-18, RESEARCH Patterns 5/6/7, Pitfalls 1/6.
  </action>
  <verify>
    <automated>cd app && python -c "import ingest_handler, data_store; assert callable(ingest_handler._record_and_check_price_drop); assert callable(ingest_handler._handle_price_drop); assert callable(ingest_handler._mark_removed_listings); assert callable(data_store.get_rejected_by_reason); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def _record_and_check_price_drop" app/ingest_handler.py` returns 1
    - `grep -c "def _handle_price_drop" app/ingest_handler.py` returns 1
    - `grep -c "def _mark_removed_listings" app/ingest_handler.py` returns 1
    - `grep -c "def get_rejected_by_reason" app/data_store.py` returns 1
    - `grep -c "_record_and_check_price_drop(app_data" app/ingest_handler.py` returns at least 2 (call in dedup-hit branch + new-listing branch)
    - `grep -c "_mark_removed_listings(app_data" app/ingest_handler.py` returns 1 (single call after the for loop)
    - `grep -c "config.PRICE_DROP_THRESHOLD" app/ingest_handler.py` returns at least 1 (used inside _record_and_check_price_drop)
    - `grep -c "telegram_client.send_message" app/ingest_handler.py` returns at least 1 (used inside _handle_price_drop)
    - Python module compile OK: `cd app && python -c "import py_compile; py_compile.compile('data_store.py', doraise=True); py_compile.compile('ingest_handler.py', doraise=True); print('OK')"` prints OK
    - Full existing test suite still passes: `cd app && python -m pytest tests/ -x -q --ignore=tests/test_price_intelligence.py --ignore=tests/test_eval_quality.py`
    - The automated verify command above prints OK
  </acceptance_criteria>
  <done>Three ingest_handler private helpers implemented following never-raise + RLock-safe patterns; data_store.get_rejected_by_reason helper added; process_ingest_batch invokes _record_and_check_price_drop on both branches and _mark_removed_listings after the loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip remaining 10 Wave 0 test stubs to PASS + Removed badge in frontend</name>
  <files>app/tests/test_price_intelligence.py, app/tests/test_eval_quality.py, app/static/index.html</files>
  <read_first>
    - app/tests/test_price_intelligence.py (remaining 6 xfail stubs after 03-03: test_price_drop_reeval_pending, test_price_drop_below_threshold_no_reeval, test_price_rejected_requeued, test_location_rejected_not_requeued, test_removed_listing_marked, test_removed_listing_marked_pending)
    - app/tests/test_eval_quality.py (remaining 4 xfail stubs after 03-02: test_anchor_injection, test_anchor_skipped_below_threshold, test_district_avg_injected, test_district_avg_omitted_unknown)
    - app/tests/test_pending.py (data_store._lock seeding pattern; monkeypatch lambda for evaluate_listing accepting context_prefix="")
    - app/tests/conftest.py (fixtures)
    - app/static/index.html (renderMain at 763-815 — Removed badge insertion point similar to plan 03-03 Days-on-market injection; buildPendingCard at 1143-1207)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 9 (removed badge — check p.removed === true, red var(--red))
    - .planning/phases/03-ai-quality-price-intelligence/03-VALIDATION.md (per-task verification map rows 03-04-01..03-04-06 for exact test names)
  </read_first>
  <behavior>
    - test_anchor_injection PASSES: seeds properties[] with 3 scored entries; sets Listing.title etc. and calls _build_context_prefix(listing, app_data); asserts return string contains "Anchor 1" and "Anchor 2" and mentions the highest score
    - test_anchor_skipped_below_threshold PASSES: seeds properties[] with just 1 scored entry; _build_context_prefix returns "" for the anchor portion (no "Anchor" substring in output — district line may still appear)
    - test_district_avg_injected PASSES: seeds properties[] with 2 entries in district "Mustamäe" with pricePerSqm 2500 and 2400; listing has district "Mustamäe" via monkeypatched attr or a synthetic dataclass extension; asserts "District price/m² average" substring present in _build_context_prefix output
    - test_district_avg_omitted_unknown PASSES: listing has no district; asserts "District price/m² average" substring NOT in _build_context_prefix output
    - test_price_drop_reeval_pending PASSES: seeds pending[] with a listing at 200000 and seeds price_history with [{date 2026-07-01, price 200000}]; monkeypatches evaluate_listing to return score 85 verdict "Better now" checklist full-7-key; POSTs the same listing at 188000 (>=5% drop); asserts pending entry score updated to 85 AND price_history has 2 entries
    - test_price_drop_below_threshold_no_reeval PASSES: seeds pending[] at 200000 + history at 200000; POSTs at 195000 (2.5% drop, below threshold); asserts pending entry score is UNCHANGED (still original) — evaluate_listing was NOT called for re-eval
    - test_price_rejected_requeued PASSES: seeds rejected[] with rejection_reason "price" for a listing; seeds price_history with 200000; monkeypatches evaluate_listing; POSTs at 185000 (>=5% drop); asserts the entry moved from rejected[] to pending[] with a price_drop_requeue_note
    - test_location_rejected_not_requeued PASSES: seeds rejected[] with rejection_reason "location"; seeds price_history at 200000; POSTs at 180000 (10% drop); asserts the entry is STILL in rejected[] (untouched); pending[] does NOT contain it
    - test_removed_listing_marked PASSES: seeds properties[] with a listing; POSTs the listing with raw_ok False; asserts the properties[] entry has removed True AND removed_at is a YYYY-MM-DD string
    - test_removed_listing_marked_pending PASSES: seeds pending[] with a listing; POSTs the listing with raw_ok False; asserts the pending[] entry has removed True + removed_at
    - renderMain and buildPendingCard append a Removed badge (red text) when the property/entry has removed True — using textContent only
  </behavior>
  <action>
In app/tests/test_price_intelligence.py:
1. Replace the xfail stub bodies for the 6 remaining tests with real implementations. Remove @pytest.mark.xfail from all 6. Each test uses the fixtures (client, tmp_agent_state, mock_send_pending_card, mock_telegram, monkeypatch) as needed.

Common monkeypatch fixture setup at the top of the price-drop tests:
- monkeypatch.setattr(ingest_handler, "evaluate_listing", lambda l, context_prefix="": {"score":85,"verdict":"Better now","strengths":[],"concerns":[],"draft_body":"b","checklist":{"price_per_sqm":"pass","rooms_area":"pass","parking":"unknown","renovation_potential":"unknown","floor":"pass","year_material":"pass","mandatory_extras":"unknown"}})

test_price_drop_reeval_pending: seed pending[] with id "test-1" and price_eur 200000 + a valid queued_at etc. Seed price_history["test-1"] = [{"date":"2026-07-01","price":200000}]. Seed seen_listing_ids with test-1 so the ingest treats it as dedup-hit. POST payload with id test-1, url https://www.kv.ee/test-1.html, title Test, price_eur 188000 (6% drop), rooms 3, area_sqm 60.0, image_count 10, raw_ok True, Authorization Bearer test-token-abc. Reload app_data. Assert data["pending"][0]["score"] == 85. Assert len(data["price_history"]["test-1"]) == 2 with the latest price 188000.

test_price_drop_below_threshold_no_reeval: same setup but POST at 195000 (2.5% drop). Also monkeypatch a Sentinel score for evaluate_listing (score 999) so if it accidentally runs the test can detect it. Reload app_data. Assert data["pending"][0]["score"] equals its ORIGINAL seeded score (NOT 999) — evaluate_listing was NOT called for re-eval. Assert len(data["price_history"]["test-1"]) == 2 (still recorded).

test_price_rejected_requeued: seed rejected[] with id "test-1", rejection_reason "price", rejected_at some ISO. Seed price_history at 200000. Seed seen_listing_ids with test-1. POST at 185000 (7.5% drop). Reload app_data. Assert not any(e["id"] == "test-1" for e in data["rejected"]) — moved out. Assert any(e["id"] == "test-1" for e in data["pending"]) — moved to pending. Assert the new pending entry contains a price_drop_requeue_note key.

test_location_rejected_not_requeued: same as above but rejection_reason "location". Assert the entry REMAINS in rejected[] and pending[] does NOT contain it.

test_removed_listing_marked: seed properties[] with id "test-1". POST payload with id test-1, raw_ok False, url https://www.kv.ee/test-1.html, image_count 0. Reload app_data. Assert the properties[] entry has removed True AND removed_at is a YYYY-MM-DD string (re.match r"^\d{4}-\d{2}-\d{2}$").

test_removed_listing_marked_pending: same but seed pending[] instead of properties[].

In app/tests/test_eval_quality.py:
1. Replace the 4 remaining xfail stub bodies with real implementations. Remove @pytest.mark.xfail from all 4. Each test uses (tmp_agent_state, monkeypatch) — no ingest POST needed, they call _build_context_prefix directly.

test_anchor_injection(tmp_agent_state): under with data_store._lock, seed data["properties"] with 3 entries containing id, name, district "X", score 85 (and 78 and 92), rooms 3, area 60, pricePerSqm 2500, material panel; save. Build a Listing (id foo, url http://x, title "New Listing") — omit district since Listing has no such field. Call ingest_handler._build_context_prefix(listing, data_store.load_app_data()). Assert the returned string contains "Anchor 1" AND "Anchor 2" AND "92" (highest score should appear first since sorted descending).

test_anchor_skipped_below_threshold(tmp_agent_state): seed properties[] with just 1 scored entry (score 85). Call _build_context_prefix as above. Assert "Anchor 1" is NOT in the returned string (fewer than 2 scored = skip anchors per D-02).

test_district_avg_injected(tmp_agent_state): seed properties[] with 2 entries in district "Mustamäe" (pricePerSqm 2500 and 2400). Because Listing dataclass has no district field, we cannot pass a district directly through the standard Listing constructor. TWO strategies allowed: (a) call _build_context_prefix with a plain SimpleNamespace object mimicking Listing that has a district attribute — the helper reads via getattr with default "" per RESEARCH Pitfall 2, so a SimpleNamespace with district set is a valid stand-in; OR (b) monkeypatch a fresh Listing instance with listing.district = "Mustamäe" via object.__setattr__ (dataclass allows attribute setting unless frozen). Prefer strategy (a). Call the helper; assert "District price/m² average" AND "Mustamäe" AND either "2400" or "2450" (average of 2500 and 2400) is in the output.

test_district_avg_omitted_unknown(tmp_agent_state): pass a Listing with no district field (the true Listing dataclass, no monkeypatch). Seed properties[] with entries in district "X". Call helper; assert "District price/m² average" is NOT in output.

In app/static/index.html:
1. In renderMain, after el.innerHTML = ... (line 814) and after the days-on-market injection from 03-03, add: if (p.removed) { var removedBadge = document.createElement("div"); removedBadge.style.color = "var(--red)"; removedBadge.style.fontFamily = "var(--font-mono)"; removedBadge.style.fontSize = "12px"; removedBadge.style.marginTop = "8px"; removedBadge.textContent = "Removed from kv.ee — " + (p.removed_at || "unknown date"); el.querySelector(".head-actions").appendChild(removedBadge); }
2. In buildPendingCard, before the actionRow is appended, add analogous conditional: if (entry.removed) { create a removed badge div with red color + textContent and append to card }
3. Both badge nodes use textContent only — no innerHTML.

Cite per D-14, D-15, D-16, D-17, D-18, INTEL-03.
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_price_intelligence.py tests/test_eval_quality.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && python -m pytest tests/test_price_intelligence.py -v` reports 9 PASSED and 0 FAILED and 0 XFAIL
    - `cd app && python -m pytest tests/test_eval_quality.py -v` reports 6 PASSED and 0 FAILED and 0 XFAIL
    - `grep -c "xfail" app/tests/test_price_intelligence.py` returns 0
    - `grep -c "xfail" app/tests/test_eval_quality.py` returns 0
    - `grep -c "Removed from kv.ee" app/static/index.html` returns at least 2 (renderMain injection + buildPendingCard injection)
    - `grep -c "p.removed" app/static/index.html` returns at least 1
    - `grep -c "entry.removed" app/static/index.html` returns at least 1
    - Full test suite passes: `cd app && python -m pytest tests/ -v` — 0 failures, 0 XFAIL
    - No new innerHTML sites introduced: manual diff review of index.html shows only textContent additions
  </acceptance_criteria>
  <done>All 15 Wave 0 stubs flipped to PASS; Removed badge renders on dossier and pending cards via textContent; entire Phase 3 test surface green.</done>
</task>

</tasks>

<verification>
Plan-level checks after both tasks complete:

1. `cd app && python -m pytest tests/ -v` — every test PASSED, 0 XFAIL, 0 FAILED
2. Coverage of all 7 phase requirements: EVAL-01 (test_anchor_injection + test_anchor_skipped_below_threshold), EVAL-02 (test_checklist_in_response + test_checklist_user_override_preserved), EVAL-03 (test_district_avg_injected + test_district_avg_omitted_unknown), EVAL-04 (4 price-drop tests), INTEL-01 (test_record_price_new + test_record_price_idempotent + test_ingest_records_price_for_known), INTEL-02 (manual browser check of days-on-market), INTEL-03 (test_removed_listing_marked + test_removed_listing_marked_pending)
3. Manual sanity (optional, phase-verify not blocking): trigger an ingest with a real 6% drop on a seeded known listing; observe Telegram notification arrives and pending card score updates
4. Manual browser check: dossier card shows Removed badge on a listing with p.removed True
</verification>

<success_criteria>
- Every phase requirement (EVAL-01, EVAL-02, EVAL-03, EVAL-04, INTEL-01, INTEL-02, INTEL-03) has at least one green automated test — INTEL-02 additionally has a manual browser check per VALIDATION.md
- All 15 Wave 0 stubs PASS with 0 XFAIL remaining
- Price-drop re-evaluation dispatches correctly across approved/pending/rejected-price/rejected-other states
- Removed marker survives round-trip through /api/data and renders in the UI
- Zero regressions in Phase 1 and Phase 2 tests
- No new Python packages installed
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper-client → VPS ingest | raw_ok field is an untrusted boolean signal; default True on missing preserves current behavior |
| VPS → Anthropic (re-eval) | evaluate_listing under drop path holds _lock during a 30-second HTTP call — same latency-window as new-listing path (existing limitation per RESEARCH Pitfall 7) |
| VPS → Telegram | send_message on price drop discloses listing title + prices to Daniel's chat only (existing chat-id boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-13 | Denial of Service | A malicious scraper payload could report raw_ok=False for every listing, silently marking the entire dossier removed | high | mitigate | raw_ok defaults to True on the incoming dict (item.get("raw_ok", True)); the scraper client is auth-gated via Bearer token (Phase 1); mini PC is trusted infrastructure. Additional defense: only mark listings whose id is present in the batch and matches an existing dossier entry — do not create new removed entries |
| T-03-14 | Tampering | Repeated same-listing POST at slightly lower prices could trigger repeated re-evaluations and Telegram spam | medium | mitigate | Same-day overwrite path in _record_and_check_price_drop skips the drop comparison when history[-1].date == today_str, so a same-day re-scrape does not re-trigger. Different-day price re-drops are legitimate and expected |
| T-03-15 | Denial of Service | A very stale price_history entry (>90 days old) could compare against modern price and produce a large drop | low | accept | Bounded by the 90-entry cap in record_price_in_data; typical inter-scrape gap is 2h; edge case reserved for future work |
| T-03-16 | Elevation of Privilege | Re-evaluation could overwrite an approved listing's user-added notes or manual score | medium | mitigate | _handle_price_drop PREPENDS the price-drop tag to notes (does not overwrite); score/verdict are updated per D-15; Telegram notification includes both old and new price so Daniel sees the change (RESEARCH Pitfall 6) |
| T-03-17 | Information Disclosure | Telegram message contains listing title + prices | low | accept | Same threat surface as existing send_pending_card; chat-id guard from Phase 2 applies |
| T-03-18 | Tampering | rejected[] re-queue path could construct a pending entry missing required fields, causing frontend render errors | low | mitigate | new pending entry constructed via dict(rej) (shallow copy) preserves all original fields; only rejection_reason and rejected_at are popped; new fields are strictly additive |
| T-03-SC | Tampering | No new package installs in this plan | low | accept | No additions to requirements.txt |
</threat_model>

<output>
Create `.planning/phases/03-ai-quality-price-intelligence/03-04-SUMMARY.md` when done, following @.claude/gsd-core/templates/summary.md.

Phase 3 gate: after this plan's SUMMARY is committed, run `/gsd-verify-work` for the phase — all 7 requirements have automated coverage plus 1 manual (INTEL-02 days-on-market visual).
</output>
