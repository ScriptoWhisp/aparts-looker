---
phase: 03-ai-quality-price-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/ai_evaluator.py
  - app/ingest_handler.py
  - app/data_store.py
  - app/static/index.html
  - app/tests/test_eval_quality.py
requirements:
  - EVAL-02
autonomous: true
status: complete
completed_at: 2026-07-08
tags:
  - ai
  - checklist
  - schema
  - frontend
must_haves:
  truths:
    - "evaluate_listing() returns a dict whose response schema (setdefault + error-fallback branches) includes a checklist key defaulting to {} (D-06, EVAL-02)"
    - "SYSTEM_PROMPT includes explicit instructions listing the 7 checklist criterion keys (price_per_sqm, rooms_area, parking, renovation_potential, floor, year_material, mandatory_extras) with allowed values pass|fail|unknown (D-06, EVAL-02)"
    - "ingest_handler whitelists the AI-returned checklist to the 7 expected keys and 3 allowed values, then persists it via data_store.write_checklist_ai (RESEARCH Pitfall 3, D-07, D-08)"
    - "write_checklist_ai writes to app_data.checklists[listing_id].ai_checklist with each entry shaped as {result, source:'ai'} and preserves any existing source=='user' entry (D-08, D-09)"
    - "The pending tab card in index.html renders an AI checklist badge strip per listing when checklists[id].ai_checklist exists, using textContent only (EVAL-02, Security V5)"
    - "The two EVAL-02 tests in test_eval_quality.py (test_checklist_in_response, test_checklist_user_override_preserved) are green — xfail decorator removed"
  artifacts:
    - "SYSTEM_PROMPT extended with checklist schema block in app/ai_evaluator.py"
    - "app/data_store.write_checklist_ai(listing_id, checklist) helper"
    - "app/ingest_handler EXPECTED_CHECKLIST_KEYS constant + _whitelist_checklist helper"
    - "buildAiChecklistEl(listingId) JS function in app/static/index.html"
  key_links:
    - "process_ingest_batch calls data_store.write_checklist_ai(listing.id, whitelisted_checklist) AFTER data_store.add_to_pending(pending_entry), inside the outer with data_store._lock — RLock-safe (RESEARCH Pitfall 1)"
    - "Frontend reads state.checklists[id].ai_checklist from the /api/data payload — no backend changes to GET /api/data needed since checklists is already returned"
---

<objective>
Deliver end-to-end structured checklist output: Claude returns a 7-key pass/fail/unknown checklist alongside the existing score/verdict, the VPS whitelists and persists it to app_data.checklists[id].ai_checklist with source="ai", and the pending tab renders a badge strip so Daniel sees the AI-assessed criteria at a glance.

Purpose: Give the pending queue an at-a-glance quality signal beyond a single score, and preload the 11-category post-viewing checklist with everything the AI can pre-fill from listing text.

Output: SYSTEM_PROMPT extension, write_checklist_ai data_store helper, checklist whitelist + persist in ingest_handler, AI badge strip in the pending card, and 2 EVAL-02 stubs flipped from XFAIL to PASSED.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-quality-price-intelligence/03-CONTEXT.md
@.planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md
@.planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md
@.planning/phases/03-ai-quality-price-intelligence/03-VALIDATION.md
@.planning/phases/03-ai-quality-price-intelligence/03-01-SUMMARY.md
@.claude/CLAUDE.md

# Source files the executor MUST read before touching anything
@app/ai_evaluator.py
@app/ingest_handler.py
@app/data_store.py
@app/static/index.html
@app/tests/test_eval_quality.py
@app/tests/test_pending.py
@app/tests/conftest.py
</context>

<artifacts_this_phase_produces>
New symbols in plan 03-02:
- app.ingest_handler.EXPECTED_CHECKLIST_KEYS (module-level frozenset of the 7 canonical criterion keys)
- app.ingest_handler.ALLOWED_CHECKLIST_VALUES (module-level frozenset {"pass","fail","unknown"})
- app.ingest_handler._whitelist_checklist(raw) — private helper returning full 7-key dict
- app.data_store.write_checklist_ai(listing_id, checklist) — new public helper
- buildAiChecklistEl(listingId) JS function in app/static/index.html

Symbols extended in plan 03-02:
- SYSTEM_PROMPT — appended block describing the 7-key checklist schema
- evaluate_listing() — setdefault checklist key in success branch; checklist:{} in error fallback dict
- process_ingest_batch() — writes AI checklist after add_to_pending

Downstream consumers:
- Plan 03-04 re-uses write_checklist_ai when re-evaluating on a price drop for pending entries (D-16)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend evaluate_listing schema + add write_checklist_ai + wire checklist into ingest pipeline</name>
  <files>app/ai_evaluator.py, app/data_store.py, app/ingest_handler.py</files>
  <read_first>
    - app/ai_evaluator.py (SYSTEM_PROMPT f-string at lines 23-56; setdefault block at lines 120-127; error fallback dict at lines 130-138)
    - app/data_store.py (reject_listing at lines 143-166 is the closest shape analog for a threaded write with nested-dict mutation; add_to_pending at lines 109-120)
    - app/ingest_handler.py (process_ingest_batch loop with add_to_pending at line 109 — checklist write goes AFTER add_to_pending)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 8 (checklists[listing_id].ai_checklist sub-structure — CRITICAL: nests inside existing checklists dict, does NOT replace section keys)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pitfall 3 (whitelist the 7 keys + 3 allowed values before writing)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Checklist Criteria — The 7 Text-Assessable Criteria table
    - .planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md app/data_store.py section (Boolean-return helper pattern; write_checklist_ai code shape)
  </read_first>
  <behavior>
    - SYSTEM_PROMPT ends with an explicit instruction block naming the 7 criterion keys and stating that values must be exactly one of pass|fail|unknown and to use unknown when the listing text is silent
    - evaluate_listing() success path calls result.setdefault(checklist_key, {}) alongside the other 7 setdefault calls
    - evaluate_listing() error fallback dict includes a checklist:{} entry
    - ingest_handler defines EXPECTED_CHECKLIST_KEYS as a module-level frozenset of the 7 canonical keys AND ALLOWED_CHECKLIST_VALUES as a frozenset {"pass","fail","unknown"}
    - _whitelist_checklist(raw) returns a dict with exactly the 7 canonical keys — filling missing or invalid entries with "unknown" so callers always get a fully-populated dict
    - process_ingest_batch calls data_store.write_checklist_ai(listing.id, _whitelist_checklist(evaluation.get(checklist_key, {}))) IMMEDIATELY AFTER the existing data_store.add_to_pending(pending_entry) line, still inside the outer with data_store._lock block
    - write_checklist_ai(listing_id, checklist) acquires _lock (RLock, safe to re-enter), navigates data.setdefault(checklists_key,{}).setdefault(listing_id,{}).setdefault(ai_checklist_key,{}) and for each key writes {result, source:'ai'} — BUT preserves any existing entry whose source == user (D-09)
  </behavior>
  <action>
In app/ai_evaluator.py:
1. Append an instruction block to the SYSTEM_PROMPT f-string (which currently ends with the JSON schema block at line 55). Insert BEFORE the closing triple-quote. The block extends the JSON schema example by adding one more field to the schema and one paragraph of instructions. Use the schema fragment from RESEARCH.md Pattern 2 (checklist keys with pass/fail/unknown values). Use the 7 criteria keys documented in RESEARCH.md's Checklist Criteria — The 7 Text-Assessable Criteria table: price_per_sqm, rooms_area, parking, renovation_potential, floor, year_material, mandatory_extras. Instruct the model to use exactly those 7 keys and to fill unknown when the listing text is silent.
2. Add a checklist setdefault call inside the success block (currently lines 120-127) after the should_draft_email setdefault line. Default value is an empty dict.
3. Add a checklist entry with an empty-dict value to the error fallback dict returned in the except block (currently lines 130-138).
4. Do NOT change max_tokens (already 1500 from plan 03-01).

In app/data_store.py:
1. Add write_checklist_ai(listing_id, checklist) as a new function following the shape of reject_listing (with _lock: load-mutate-save). Body per RESEARCH Pattern 4 and PATTERNS.md app/data_store.py section: navigate data.setdefault(checklists_key,{}).setdefault(listing_id,{}).setdefault(ai_checklist_key,{}); for each (key, result) in checklist.items(): if existing entry is a dict with source==user, skip (D-09 preserve); else write a dict containing result and source ai; then call save_app_data(data). Return None (no boolean).

In app/ingest_handler.py:
1. Add module-level constants near LISTING_FIELD_NAMES: EXPECTED_CHECKLIST_KEYS as a frozenset containing the 7 canonical keys listed above; ALLOWED_CHECKLIST_VALUES as a frozenset of pass, fail, unknown.
2. Add a new private helper _whitelist_checklist(raw) returning a dict with all 7 keys — for each canonical key, if raw.get(key) is a string in ALLOWED_CHECKLIST_VALUES use it, otherwise default to unknown. Reject unknown keys silently (do NOT propagate them). Never-raise via try/except returning a fully-default dict of 7 unknowns on error.
3. In process_ingest_batch, IMMEDIATELY after the existing data_store.add_to_pending(pending_entry) call, add a call to data_store.write_checklist_ai passing listing.id and _whitelist_checklist applied to evaluation.get(checklist_key, {}). This runs inside the outer with data_store._lock; RLock reentrancy is safe (RESEARCH Pitfall 1).

Cite per D-06, D-07, D-08, D-09, RESEARCH Pitfall 3. D-10 (Context note) informs the choice of the 7 text-assessable criterion keys and why viewing-only items are not part of ai_checklist — instruct the model to use "unknown" when the listing text is silent, matching D-10's separation of text-assessable vs viewing-only criteria.
  </action>
  <verify>
    <automated>cd app && python -c "import data_store, ingest_handler; assert callable(data_store.write_checklist_ai); assert len(ingest_handler.EXPECTED_CHECKLIST_KEYS)==7 and 'price_per_sqm' in ingest_handler.EXPECTED_CHECKLIST_KEYS; out = ingest_handler._whitelist_checklist({'price_per_sqm':'pass','garbage':'x','floor':'bogus'}); assert out['price_per_sqm']=='pass' and out['floor']=='unknown' and 'garbage' not in out and len(out)==7; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def write_checklist_ai" app/data_store.py` returns 1
    - `grep -c "EXPECTED_CHECKLIST_KEYS" app/ingest_handler.py` returns at least 2 (definition + usage)
    - `grep -c "def _whitelist_checklist" app/ingest_handler.py` returns 1
    - `grep -c "write_checklist_ai" app/ingest_handler.py` returns at least 1 (call site inside process_ingest_batch)
    - `grep -c 'setdefault("checklist"' app/ai_evaluator.py` returns 1 (success branch)
    - `grep -c '"checklist":' app/ai_evaluator.py` returns at least 2 (error fallback dict entry + schema example inside SYSTEM_PROMPT)
    - `grep -c "price_per_sqm" app/ai_evaluator.py` returns at least 1 (in the SYSTEM_PROMPT extension)
    - `grep -c "rooms_area\|renovation_potential\|mandatory_extras" app/ai_evaluator.py` returns at least 3 (all 7 keys named in SYSTEM_PROMPT)
    - Python module import: `cd app && python -c "import ai_evaluator, data_store, ingest_handler; print('OK')"` prints OK
    - The automated verify command above prints OK (whitelisting behavior verified)
    - The full existing test suite still passes: `cd app && python -m pytest tests/ -x -q --ignore=tests/test_eval_quality.py --ignore=tests/test_price_intelligence.py` reports 0 failures (Phase 1 and Phase 2 tests unaffected)
  </acceptance_criteria>
  <done>SYSTEM_PROMPT names the 7 criterion keys; evaluate_listing returns a checklist field in both success and error branches; ingest_handler whitelists and persists AI checklist to app_data.checklists[id].ai_checklist with source ai; user-source entries preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip EVAL-02 tests green and add pending-card AI checklist badge strip</name>
  <files>app/tests/test_eval_quality.py, app/static/index.html</files>
  <read_first>
    - app/tests/test_eval_quality.py (the two Wave 0 xfail stubs test_checklist_in_response and test_checklist_user_override_preserved created in plan 03-01)
    - app/tests/test_pending.py (monkeypatch.setattr(ingest_handler, "evaluate_listing", lambda listing, context_prefix="": {...}) pattern; data_store._lock seeding pattern at lines 209-230)
    - app/tests/conftest.py (fixtures: client, tmp_agent_state, mock_send_pending_card, mock_telegram)
    - app/static/index.html (state object at line 472; loadData at lines 496-509; buildPendingCard at lines 1143-1207; CSS variables --green/--red/--grey/--amber at lines 24-35)
    - .planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md app/static/index.html section (buildAiChecklistEl code shape with LABELS map, textContent-only, no innerHTML)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 9 (AI checklist badge rendering — pass/fail/unknown color mapping)
  </read_first>
  <behavior>
    - test_checklist_in_response: POSTs a listing via /api/ingest with evaluate_listing monkeypatched to return a full 7-key checklist; asserts app_data.checklists[listing_id].ai_checklist contains all 7 keys and each entry is a dict with a "result" string in pass/fail/unknown and source == ai
    - test_checklist_user_override_preserved: seeds an existing entry in checklists[test-1].ai_checklist.parking with source == user and result == fail; then invokes write_checklist_ai(test-1, {parking: pass, floor: pass, ...}); asserts parking entry still has source == user and result == fail while floor got source == ai and result == pass
    - Both tests are decorated WITHOUT @pytest.mark.xfail — they must PASS normally
    - buildPendingCard now appends an AI checklist badge strip element (built by new buildAiChecklistEl helper) between the meta row and the actionRow, when state.checklists[entry.id].ai_checklist exists
    - buildAiChecklistEl uses textContent exclusively (never innerHTML) — Security V5 XSS mitigation
  </behavior>
  <action>
In app/tests/test_eval_quality.py:
1. Replace the xfail stub bodies for test_checklist_in_response and test_checklist_user_override_preserved with real implementations. Remove the @pytest.mark.xfail decorator on ONLY these two tests. Leave the other 4 stubs xfail-strict for later plans to flip.

For test_checklist_in_response, use fixtures (client, tmp_agent_state, mock_send_pending_card, monkeypatch). Monkeypatch ingest_handler.evaluate_listing to a lambda accepting (listing, context_prefix="") that returns a dict containing score 80, verdict "Good", strengths [], concerns [], draft_body "b", and a checklist dict with all 7 canonical keys mapped to "pass". POST a valid listing_payload to /api/ingest (mirror the payload shape in test_pending.py::test_ingest_writes_to_pending — include id "test-1", url, title, price_eur under MAX_PRICE_EUR, rooms >= MIN_ROOMS, area_sqm, image_count >= MIN_IMAGES, raw_ok True) with Authorization Bearer test-token-abc. Then read data_store.load_app_data() and assert data["checklists"]["test-1"]["ai_checklist"] contains all 7 canonical keys; each value is a dict with keys result and source; result is in the pass/fail/unknown set; source equals ai.

For test_checklist_user_override_preserved, use fixtures (tmp_agent_state,). Under with data_store._lock: seed data["checklists"]["test-1"] = {"ai_checklist": {"parking": {"result": "fail", "source": "user"}}} and save. Then call data_store.write_checklist_ai("test-1", {"parking": "pass", "floor": "pass", "price_per_sqm": "unknown", "rooms_area": "pass", "renovation_potential": "unknown", "year_material": "pass", "mandatory_extras": "unknown"}). Reload data and assert data["checklists"]["test-1"]["ai_checklist"]["parking"] equals {"result": "fail", "source": "user"} AND data["checklists"]["test-1"]["ai_checklist"]["floor"] equals {"result": "pass", "source": "ai"}.

In app/static/index.html:
1. Add a new function buildAiChecklistEl(listingId) BEFORE buildPendingCard, following the shape in PATTERNS.md app/static/index.html section. Structure:
   - Read state.checklists[listingId]?.ai_checklist; return null if missing/empty
   - Build a container div with margin-top 8px, display flex, flex-wrap wrap, gap 4px
   - Define a LABELS map: price_per_sqm -> price/m², rooms_area -> rooms/area, parking -> parking, renovation_potential -> reno, floor -> floor, year_material -> year/mat, mandatory_extras -> extras
   - Iterate Object.keys(ai_checklist); for each, read item.result (support both flat string and {result, source} shape); pick a check/cross/question glyph and a background/foreground color from --green-bg/--red-bg/--grey-bg vs --green/--red/--grey
   - Prefix badge textContent with "[AI] " if source == ai; use textContent only, never innerHTML
2. In buildPendingCard, immediately after the meta.textContent = metaParts.join(...) block and BEFORE actionRow is created, call var aiEl = buildAiChecklistEl(entry.id); if (aiEl) card.appendChild(aiEl);
3. Do NOT touch renderMain in this plan — dossier card AI badges land in the same aiEl pattern in plan 03-03 or a follow-up if UI-01 needs them. For this plan only the pending tab gets the strip.

Verify Security V5: use textContent exclusively for all badge text; never string-concatenate into innerHTML.

Cite per D-08, D-09, EVAL-02, RESEARCH Pattern 9.
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_eval_quality.py::test_checklist_in_response tests/test_eval_quality.py::test_checklist_user_override_preserved -v</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && python -m pytest tests/test_eval_quality.py::test_checklist_in_response tests/test_eval_quality.py::test_checklist_user_override_preserved -v` reports 2 PASSED and 0 FAILED
    - `grep -c "xfail" app/tests/test_eval_quality.py` returns exactly 4 (down from 6 — the two EVAL-02 tests no longer decorated)
    - `grep -c "function buildAiChecklistEl" app/static/index.html` returns 1
    - `grep -c "buildAiChecklistEl(entry.id)" app/static/index.html` returns 1 (call site inside buildPendingCard)
    - `grep -c "\.innerHTML" app/static/index.html` count is unchanged from before this task (verify by comparing to git diff — no new innerHTML sites introduced by the AI badge helper)
    - `grep -c "\[AI\]" app/static/index.html` returns at least 1 (badge prefix)
    - The full test suite still passes: `cd app && python -m pytest tests/ -x -q` — the 4 remaining Wave 0 stubs stay XFAIL, all other tests PASS
    - HTML file remains syntactically valid: `python -c "from html.parser import HTMLParser; HTMLParser().feed(open('app/static/index.html').read()); print('OK')"` prints OK (best-effort parse; HTMLParser is lenient)
  </acceptance_criteria>
  <done>Both EVAL-02 tests pass; buildAiChecklistEl renders per-listing badge strip in the pending tab using textContent only; 4 xfail stubs remain for plans 03-03/03-04.</done>
</task>

</tasks>

<verification>
Plan-level checks after both tasks complete:

1. `cd app && python -m pytest tests/ -v` — all Phase 1/2 tests PASS; test_checklist_in_response + test_checklist_user_override_preserved PASS; 4 remaining Wave 0 stubs XFAIL
2. `grep -c "ai_checklist" app/static/index.html` returns at least 1
3. `grep -c "ai_checklist" app/data_store.py` returns at least 1 (inside write_checklist_ai)
4. Manual browser check (optional, plan-gate not blocking): open pending tab in browser; verify AI checklist badges render below the meta line — pass entries green, fail red, unknown grey, all prefixed [AI]
5. No regression: `grep -c "def evaluate_listing" app/ai_evaluator.py` still returns 1 (signature intact)
</verification>

<success_criteria>
- EVAL-02 fully delivered: prompt schema, response schema, whitelist persistence, UI badges
- 2 of 6 test_eval_quality.py stubs flipped to PASS; 4 remain XFAIL for plans 03-03/03-04
- write_checklist_ai preserves user overrides per D-09
- Zero XSS risk introduced: all new DOM writes use textContent
- No new Python packages installed
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic API → VPS | The AI response is untrusted JSON; keys and values must be whitelisted before persistence |
| VPS → Browser | checklist values become DOM text; must use textContent to prevent XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | AI returns non-canonical checklist key or value | medium | mitigate | EXPECTED_CHECKLIST_KEYS + ALLOWED_CHECKLIST_VALUES whitelist in _whitelist_checklist rejects unknown keys and coerces invalid values to unknown before write |
| T-03-05 | Information Disclosure | AI response could hallucinate a criterion name containing user PII from listing text | low | mitigate | Whitelist limits keys to 7 canonical strings; unknown keys are dropped entirely |
| T-03-06 | Elevation of Privilege | AI checklist could overwrite a user-confirmed post-viewing entry | medium | mitigate | write_checklist_ai preserves existing entries with source==user (D-09) |
| T-03-07 | Tampering (XSS) | AI checklist string rendered into DOM | high | mitigate | buildAiChecklistEl uses textContent exclusively — no innerHTML, no template strings written to innerHTML (existing convention at index.html line 1167 and 1182) |
| T-03-SC | Tampering | No new package installs in this plan | low | accept | No additions to requirements.txt |
</threat_model>

<output>
Create `.planning/phases/03-ai-quality-price-intelligence/03-02-SUMMARY.md` when done, following @.claude/gsd-core/templates/summary.md.
</output>
