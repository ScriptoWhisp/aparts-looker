---
phase: 03-ai-quality-price-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/tests/test_eval_quality.py
  - app/tests/test_price_intelligence.py
  - app/ai_evaluator.py
  - app/data_store.py
  - app/ingest_handler.py
  - app/config.py
requirements:
  - EVAL-01
  - EVAL-03
autonomous: true
tags:
  - ai
  - prompt-engineering
  - calibration
  - price-intelligence
must_haves:
  truths:
    - "evaluate_listing(listing, context_prefix) accepts a string prefix argument prepended to the user message content (D-03, EVAL-01)"
    - "When properties[] contains ≥2 entries with a positive integer score, _build_context_prefix() returns a string containing a numbered Anchor block for the top 2-3 scored entries sorted by score descending (D-01, EVAL-01)"
    - "When fewer than 2 scored entries exist, _build_context_prefix() returns an empty string with no error raised (D-02, EVAL-01)"
    - "When ≥1 entry across properties[] + pending[] shares the same district as the listing being evaluated, _build_context_prefix() output contains a district-average line with the district name, sample count, and EUR/m² average (D-04, D-05, EVAL-03)"
    - "When the listing has no district (Listing dataclass has no district field — inferred as empty string), the district-average line is omitted (D-05, EVAL-03)"
    - "The Wave 0 test files test_eval_quality.py and test_price_intelligence.py exist as pytest.xfail stubs so subsequent waves have measurable green/red signals (Nyquist)"
    - "PRICE_DROP_THRESHOLD env var is defined in config.py with default 0.05 for consumption by plan 03-04"
  artifacts:
    - "app/tests/test_eval_quality.py (new, Wave 0 xfail stubs covering EVAL-01/02/03)"
    - "app/tests/test_price_intelligence.py (new, Wave 0 xfail stubs covering EVAL-04/INTEL-01/INTEL-03)"
    - "ai_evaluator.evaluate_listing(listing, context_prefix: str = '') function signature"
    - "ingest_handler._build_context_prefix(listing: Listing, data: dict) -> str private function"
    - "config.PRICE_DROP_THRESHOLD constant"
  key_links:
    - "process_ingest_batch calls _build_context_prefix(listing, app_data) BEFORE evaluate_listing, and passes result as context_prefix kwarg"
    - "SYSTEM_PROMPT stays byte-stable — anchors go into messages[0].content, not the system prompt (D-06 note in CONTEXT)"
---

<objective>
Deliver end-to-end calibration context injection: when a new listing is evaluated, the Claude API receives (1) a numbered anchor block built from the top 2-3 scored properties[] entries, and (2) a single district-average line when other listings share that district. Also lands the Wave 0 test scaffold (xfail stubs) for both new test files so plans 03-02/03/04 have failing-test signals to flip green.

Purpose: Fix the "everything gets 70" problem by grounding Claude's scoring in Daniel's own approved reference points. Also lay the test scaffolding that governs the rest of the phase.

Output: `evaluate_listing()` signature extended, `_build_context_prefix()` implemented in ingest_handler, `PRICE_DROP_THRESHOLD` config var, and two new test files with strict xfail stubs.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ai-quality-price-intelligence/03-CONTEXT.md
@.planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md
@.planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md
@.planning/phases/03-ai-quality-price-intelligence/03-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.claude/CLAUDE.md

# Source files the executor MUST read before touching anything
@app/ai_evaluator.py
@app/ingest_handler.py
@app/data_store.py
@app/config.py
@app/kv_listing_parser.py
@app/tests/conftest.py
@app/tests/test_pending.py
@app/tests/test_ingest.py
</context>

<artifacts_this_phase_produces>
Symbols and files that plans in this phase produce. Downstream plans (03-02/03/04) rely on these being present:

**New symbols in plan 03-01:**
- `app.config.PRICE_DROP_THRESHOLD: float` (module-level constant, default 0.05)
- `app.ai_evaluator.evaluate_listing(listing: Listing, context_prefix: str = "") -> dict` (signature change)
- `app.ingest_handler._build_context_prefix(listing: Listing, data: dict) -> str` (new private helper)

**New files in plan 03-01:**
- `app/tests/test_eval_quality.py` (Wave 0 xfail stubs — covers EVAL-01/02/03)
- `app/tests/test_price_intelligence.py` (Wave 0 xfail stubs — covers EVAL-04/INTEL-01/INTEL-03)

**Downstream consumers:**
- 03-02 flips test_eval_quality xfail stubs green by adding the checklist output
- 03-03 flips test_price_intelligence INTEL-01 stubs green by adding record_price_in_data + get_price_history
- 03-04 flips remaining test_price_intelligence stubs green (drop detection, removed marking)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test scaffolds and PRICE_DROP_THRESHOLD config</name>
  <files>app/tests/test_eval_quality.py, app/tests/test_price_intelligence.py, app/config.py</files>
  <read_first>
    - app/tests/conftest.py (fixture patterns: tmp_agent_state, client, mock_send_pending_card, mock_telegram)
    - app/tests/test_pending.py (Phase 2 test file — mirrors the imports-inside-test, data_store.lock seeding, monkeypatch.setattr(ingest_handler, "evaluate_listing", ...) pattern)
    - app/tests/test_ingest.py (integration test pattern with client fixture)
    - app/config.py (env var pattern with int()/float() cast + default)
    - .planning/phases/03-ai-quality-price-intelligence/03-VALIDATION.md (16-row per-task verification map — every test name below MUST match a row)
    - .planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md (Wave 0 xfail stub pattern at "app/tests/test_eval_quality.py (test, Wave 0 xfail stubs)" section)
  </read_first>
  <behavior>
    - test_eval_quality.py contains xfail stubs with these EXACT names (from VALIDATION.md rows 03-01-01..03-01-04 and 03-02-01..03-02-02): test_anchor_injection, test_anchor_skipped_below_threshold, test_district_avg_injected, test_district_avg_omitted_unknown, test_checklist_in_response, test_checklist_user_override_preserved
    - test_price_intelligence.py contains xfail stubs with these EXACT names (from VALIDATION.md rows 03-03-01..03-03-03 and 03-04-01..03-04-06): test_record_price_new, test_record_price_idempotent, test_ingest_records_price_for_known, test_price_drop_reeval_pending, test_price_drop_below_threshold_no_reeval, test_price_rejected_requeued, test_location_rejected_not_requeued, test_removed_listing_marked, test_removed_listing_marked_pending
    - Each stub uses @pytest.mark.xfail(reason="Wave 0 stub — not yet implemented", strict=True) followed by a body of pytest.fail("not implemented")
    - Each stub function includes a one-line docstring citing the requirement ID (e.g. EVAL-01)
    - config.py gains a PRICE_DROP_THRESHOLD constant loaded via float(os.environ.get("PRICE_DROP_THRESHOLD", "0.05"))
    - Running `cd app && python -m pytest tests/test_eval_quality.py tests/test_price_intelligence.py -v` shows all stubs as XFAIL (expected failure, strict) — NOT XPASS or PASS
  </behavior>
  <action>
Create app/tests/test_eval_quality.py with module docstring: `"""Tests for Phase 03 AI evaluation quality: EVAL-01, EVAL-02, EVAL-03."""`. Import pytest at module scope. Define exactly 6 test functions with the names listed in the behavior block. Each function body is exactly two statements: a docstring line with the requirement ID and a `pytest.fail("not implemented")` line. Decorate each with `@pytest.mark.xfail(reason="Wave 0 stub — not yet implemented", strict=True)` — strict=True is REQUIRED so accidental early implementation surfaces as XPASS failure.

Create app/tests/test_price_intelligence.py with module docstring: `"""Tests for Phase 03 price intelligence: EVAL-04, INTEL-01, INTEL-02, INTEL-03."""`. Same pytest import and same xfail(strict=True) decorator pattern. Define exactly 9 test functions with the names listed in the behavior block.

Add PRICE_DROP_THRESHOLD to app/config.py immediately after CHECK_INTERVAL_HOURS (line 28), following the identical float-cast pattern: `PRICE_DROP_THRESHOLD = float(os.environ.get("PRICE_DROP_THRESHOLD", "0.05"))`. Do NOT touch other lines in config.py.

The stub bodies are intentionally minimal — plans 03-02, 03-03, 03-04 will replace the stub body with the real test implementation and remove the xfail decorator.

Cite per D-14 for PRICE_DROP_THRESHOLD default of 0.05.
  </action>
  <verify>
    <automated>cd app && python -m pytest tests/test_eval_quality.py tests/test_price_intelligence.py -v 2>&1 | grep -E "(XFAIL|PASSED|FAILED|ERROR)" | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && python -m pytest tests/test_eval_quality.py tests/test_price_intelligence.py -v` reports exactly 15 XFAIL results (6 in test_eval_quality + 9 in test_price_intelligence) and 0 PASSED / 0 FAILED / 0 ERROR
    - `grep -c "xfail" app/tests/test_eval_quality.py` returns at least 6
    - `grep -c "xfail" app/tests/test_price_intelligence.py` returns at least 9
    - `grep -c "^def test_" app/tests/test_eval_quality.py` returns exactly 6
    - `grep -c "^def test_" app/tests/test_price_intelligence.py` returns exactly 9
    - `python -c "import sys; sys.path.insert(0, 'app'); import config; assert config.PRICE_DROP_THRESHOLD == 0.05"` exits 0
    - `grep -c "PRICE_DROP_THRESHOLD = float" app/config.py` returns 1
    - Running the full existing suite `cd app && python -m pytest tests/ -x -q` still passes (does not regress Phase 1 or Phase 2 tests)
    - Every test function name in both new files matches a row in .planning/phases/03-ai-quality-price-intelligence/03-VALIDATION.md's per-task verification map
  </acceptance_criteria>
  <done>Two new test files with 15 total xfail(strict=True) stub tests, PRICE_DROP_THRESHOLD added to config.py, full existing test suite still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend evaluate_listing signature and implement _build_context_prefix helper</name>
  <files>app/ai_evaluator.py, app/ingest_handler.py</files>
  <read_first>
    - app/ai_evaluator.py (current evaluate_listing signature at line 66; user message assembly at lines 72-85; API call at lines 98-113; setdefault fallback at lines 120-127; error fallback dict at lines 130-138)
    - app/ingest_handler.py (process_ingest_batch structure at lines 41-133; lock scope at line 51; evaluate_listing call site at line 91)
    - app/data_store.py (properties[] shape at lines 28-43 — includes district field; pending[] entry structure via add_to_pending; RLock at line 26)
    - app/kv_listing_parser.py (Listing dataclass fields at lines 50-71 — CONFIRM Listing has NO district field before implementing)
    - app/config.py (BUYER_PROFILE content — Daniel's 3-4 rooms, 50-80 m² target)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pattern 1 section (anchor injection with never-raise wrapping)
    - .planning/phases/03-ai-quality-price-intelligence/03-RESEARCH.md Pitfall 2 (Listing dataclass lacks district — must getattr with empty default)
    - .planning/phases/03-ai-quality-price-intelligence/03-PATTERNS.md app/ai_evaluator.py section (signature, user_content assembly, max_tokens bump, all setdefault lines)
  </read_first>
  <behavior>
    - evaluate_listing accepts context_prefix as a second parameter with default "" — existing callers (Phase 2 tests) that call evaluate_listing(listing) continue to work
    - When context_prefix is non-empty, it is prepended to listing_summary before being passed as messages[0].content
    - When context_prefix is empty, the exact same request body as before Phase 3 is sent (backward compat)
    - _build_context_prefix(listing, data) returns "" when fewer than 2 properties[] entries have a positive integer score (D-02)
    - When ≥2 scored entries exist, _build_context_prefix returns a string starting with "Here are listings Daniel has previously approved" and containing one paragraph per anchor (top 2-3, sorted by score descending) with title, district, score, rooms, area, price/m² (D-01, D-03)
    - When the listing has a district AND ≥1 entry across properties[]+pending[] matches that district, the output ALSO contains the exact substring "District price/m² average" followed by the district name (D-05, EVAL-03)
    - When the listing has no district (Listing dataclass lacks the field — treat as empty string via getattr) OR no matching entries exist, the district average line is omitted
    - _build_context_prefix NEVER raises — on any exception it logs via log.exception and returns ""
    - The max_tokens value in the API call is 1500 (was 1000) to accommodate the checklist coming in plan 03-02
    - process_ingest_batch (before evaluate_listing call) builds context_prefix from an app_data dict already loaded in-lock, then passes it as evaluate_listing(listing, context_prefix)
  </behavior>
  <action>
In app/ai_evaluator.py:
1. Change evaluate_listing signature to `def evaluate_listing(listing: Listing, context_prefix: str = "") -> dict:`. Docstring adds a sentence noting context_prefix is prepended to the user message for calibration anchors + district avg per D-01/D-04.
2. After the existing `listing_summary = f"""..."""` block, add: `user_content = context_prefix + listing_summary` (a single new line).
3. Change the API call's `"messages": [{"role": "user", "content": listing_summary}]` to use `user_content`.
4. Change the API call's `"max_tokens": 1000` to `"max_tokens": 1500` — reason: plan 03-02 adds ~150 output tokens for the checklist (per RESEARCH Pitfall 4).
5. Do NOT touch SYSTEM_PROMPT in this plan (that lands in 03-02).
6. Do NOT add checklist to the setdefault block or the error fallback dict (that lands in 03-02).

In app/ingest_handler.py:
1. Add a new module-level private function `_build_context_prefix(listing: Listing, data: dict) -> str` above `process_ingest_batch`. Implementation follows RESEARCH.md Pattern 1 exactly:
   - Wrap the entire body in try/except Exception: log.exception + return "" (never-raise per D convention)
   - Read `data.get("properties", [])` and `data.get("pending", [])`
   - Build the anchor list: sort properties[] descending by score, keep entries where isinstance(p.get("score"), int) and p["score"] > 0, take first 3 (D-01)
   - If len < 2: anchor_block = "" (D-02)
   - Otherwise anchor_block is a string starting with `"Here are listings Daniel has previously approved, with their AI scores, as calibration reference:\n\n"` followed by numbered paragraphs "Anchor 1 — {name} ({district})\nScore: {score}/100 | {rooms} rooms, {area} m², {pricePerSqm} EUR/m², material: {material}\n\n" and ending with `"Use these as reference points when scoring the new listing below.\n\n"`
   - District avg: use `getattr(listing, "district", "")` since Listing dataclass has no district field per RESEARCH Pitfall 2. If empty string, district_line = "".
   - Otherwise walk properties[] + pending[] entries, collect price_per_sqm values (checking BOTH keys "pricePerSqm" and "price_per_sqm" since properties[] uses camelCase and pending[] uses snake_case — per RESEARCH.md Pattern 1 code sample) where the entry's district field equals the listing district AND the sqm value is truthy
   - If matching sqm values exist, district_line = `f"District price/m² average ({dist}, from {N} seen listings): {avg:.0f} EUR/m²\n\n"` where avg = sum/len (D-05)
   - Otherwise district_line = ""
   - return anchor_block + district_line
2. Modify process_ingest_batch: BEFORE the `evaluation = evaluate_listing(listing)` line (currently line 91), insert two lines: `app_data = data_store.load_app_data()` and `context_prefix = _build_context_prefix(listing, app_data)`. Then change the evaluate_listing call to `evaluation = evaluate_listing(listing, context_prefix)`. Both new reads happen inside the outer `with data_store._lock:` (RLock is reentrant — safe per RESEARCH Pitfall 1). Do NOT restructure the batch loop.
3. Do NOT add checklist writing yet (that lands in 03-02).

Cite per D-01, D-02, D-04, D-05, D-03 (anchor format wording), RESEARCH Pitfall 2 (Listing lacks district), RESEARCH Pitfall 4 (max_tokens bump).
  </action>
  <verify>
    <automated>cd app && python -c "from ai_evaluator import evaluate_listing; import inspect; sig = inspect.signature(evaluate_listing); assert 'context_prefix' in sig.parameters and sig.parameters['context_prefix'].default == '', f'signature: {sig}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def evaluate_listing(listing: Listing, context_prefix: str = \"\")" app/ai_evaluator.py` returns 1
    - `grep -c "max_tokens\": 1500" app/ai_evaluator.py` returns 1 and `grep -c "max_tokens\": 1000" app/ai_evaluator.py` returns 0
    - `grep -c "user_content = context_prefix + listing_summary" app/ai_evaluator.py` returns 1
    - `grep -c "def _build_context_prefix" app/ingest_handler.py` returns 1
    - `grep -c "_build_context_prefix(listing" app/ingest_handler.py` returns at least 1 (the call site inside process_ingest_batch)
    - `grep -c "evaluate_listing(listing, context_prefix)" app/ingest_handler.py` returns 1 (call site updated)
    - Python-syntax check: `cd app && python -c "import ai_evaluator; import ingest_handler; print('OK')"` prints OK
    - The existing Phase 2 test suite still passes: `cd app && python -m pytest tests/test_pending.py tests/test_ingest.py -v` reports 0 failures — this is the compatibility gate for the signature change (test_pending.py's monkeypatch lambda for evaluate_listing accepts a single positional arg; verify it still works or the Phase 2 tests were already tolerant)
    - New Wave 0 stubs remain XFAIL: `cd app && python -m pytest tests/test_eval_quality.py tests/test_price_intelligence.py -v` reports 15 XFAIL
    - Behavioral check via Python REPL (write to a shell script and run):
      ```
      cd app && python -c "
      import ingest_handler
      from kv_listing_parser import Listing
      listing = Listing(id='x', url='http://kv.ee/x.html', title='Test')
      # No scored properties => empty string
      assert ingest_handler._build_context_prefix(listing, {'properties': [], 'pending': []}) == ''
      # 2 scored properties => non-empty with 'Anchor 1'
      data = {'properties': [{'id': 'a', 'name': 'A', 'district': 'X', 'score': 85, 'rooms': 3, 'area': 60, 'pricePerSqm': 2500, 'material': 'panel'}, {'id': 'b', 'name': 'B', 'district': 'X', 'score': 78, 'rooms': 4, 'area': 70, 'pricePerSqm': 2400, 'material': 'brick'}], 'pending': []}
      out = ingest_handler._build_context_prefix(listing, data)
      assert 'Anchor 1' in out and 'Anchor 2' in out, out
      print('OK')
      "
      ```
      exits 0 with output "OK"
  </acceptance_criteria>
  <done>evaluate_listing accepts context_prefix parameter; _build_context_prefix helper built with never-raise + Listing-district-fallback safety; process_ingest_batch wires them together; max_tokens bumped to 1500; Wave 0 stubs untouched; Phase 1/2 tests still green.</done>
</task>

</tasks>

<verification>
Overall plan-level checks (run after both tasks complete):

1. `cd app && python -m pytest tests/ -v` — full existing suite (Phase 1 + Phase 2) passes; new xfail stubs report as XFAIL (not XPASS)
2. `grep -c "PRICE_DROP_THRESHOLD" app/config.py` returns 1
3. `grep -c "context_prefix" app/ai_evaluator.py` returns at least 2 (signature + user_content line)
4. `grep -c "context_prefix" app/ingest_handler.py` returns at least 2 (helper def + call site)
5. No `ImportError` / `AttributeError` on module import: `cd app && python -c "import config, ai_evaluator, ingest_handler; print('OK')"`
6. Manual sanity — call `evaluate_listing(listing)` (positional only) once from a Python REPL to confirm backward-compat default works.
</verification>

<success_criteria>
- Wave 0 test scaffolds exist and correctly report XFAIL for all 15 stub tests
- evaluate_listing signature extended with backward-compat default context_prefix=""
- _build_context_prefix follows never-raise pattern, produces anchor block per D-01/D-02/D-03, produces district avg line per D-04/D-05, tolerates missing Listing.district
- process_ingest_batch reads app_data + builds prefix + passes to evaluate_listing before the API call
- PRICE_DROP_THRESHOLD constant available for plan 03-04
- No regression in Phase 1 or Phase 2 tests
- Zero new packages installed (per RESEARCH — all stdlib + existing deps)
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraper-client → VPS ingest | Listing payloads arrive from the mini PC over HTTPS with Bearer auth; already validated in Phase 1 — no new boundary in this plan |
| VPS → Anthropic API | context_prefix content constructed from Daniel's own stored properties[] entries; anchor titles come from Daniel-approved listings only |
| env → config | PRICE_DROP_THRESHOLD is a numeric env var cast via float() with a hardcoded default |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | _build_context_prefix reads properties[]/pending[] and formats an f-string into the Claude prompt | low | mitigate | Anchor content sourced from Daniel's own approved listings only — no external adversarial input crosses this path. Never-raise wrapper prevents malformed data from breaking evaluation. |
| T-03-02 | Denial of Service | Anchor block token bloat if properties[] contains 100+ entries | low | accept | Top-3 slice caps anchor count structurally; token growth is bounded (~200 tokens per RESEARCH A5) |
| T-03-03 | Tampering | PRICE_DROP_THRESHOLD env var could be set to a non-numeric value | low | mitigate | float() cast raises ValueError at import time — fail-fast at container start is preferable to silent misbehavior |
| T-03-SC | Tampering | No new package installs in this plan | low | accept | RESEARCH.md confirms no new dependencies; requirements.txt unchanged |
</threat_model>

<output>
Create `.planning/phases/03-ai-quality-price-intelligence/03-01-SUMMARY.md` when done, following @.claude/gsd-core/templates/summary.md.
</output>
