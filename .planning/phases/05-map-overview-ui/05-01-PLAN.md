---
phase: 05-map-overview-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/config.py
  - app/data_store.py
  - app/kv_listing_parser.py
  - app/tests/test_geocoding.py
autonomous: true
requirements:
  - MAP-01
  - MAP-04
  - MAP-05
  - MAP-06
user_setup:
  - service: OpenRouteService
    why: "Commute isochrone (Plan 02) + per-listing driving times (Plan 02) — API key not needed at this Plan; noted here for the phase because ORS_API_KEY constant is introduced now"
    env_vars:
      - name: ORS_API_KEY
        source: "Sign up at https://openrouteservice.org — free tier, no billing card. Copy the API key from the developer dashboard and add ORS_API_KEY=<key> to .env. Also add it to docker-compose.yml environment section."
must_haves:
  truths:
    - "kv_listing_parser.Listing dataclass exposes lat: Optional[float] and lng: Optional[float] fields (default None), preserving byte-identical parity between mini PC scraper-client copy and VPS copy per Phase 1 D-05"
    - "config.ORS_API_KEY, config.BOLT_HQ_LAT (=59.4203), config.BOLT_HQ_LNG (=24.7205) are importable module-level constants"
    - "data_store.load_app_data() adds setdefault for lat, lng, commute_minutes on every entry in properties[] and pending[] (per-entry migration) so existing app_data.json files load without KeyError"
    - "data_store._pending_to_property() carries over lat, lng, commute_minutes from the source pending entry to the new properties entry at approval time"
    - "data_store.update_listing_coords(listing_id, lat, lng, commute_minutes) mutates the matching entry in properties[] or pending[] and returns True; returns False if not found"
    - "data_store.get_listing_coords(listing_id) returns {'lat': float|None, 'lng': float|None, 'commute_minutes': int|None} for a listing_id; returns {'lat': None, 'lng': None, 'commute_minutes': None} on miss"
    - "kv_listing_parser.fetch_listing() attempts to extract coordinates from raw resp.text using at least three fallback regex patterns and sets Listing.lat / Listing.lng when a valid pair is found"
    - "Existing tests in app/tests/ still pass — no regression on Listing dataclass instantiation, no regression on data_store.load_app_data() or _pending_to_property behaviour"
  artifacts:
    - "app/config.py — updated with ORS_API_KEY (default ''), BOLT_HQ_LAT, BOLT_HQ_LNG"
    - "app/data_store.py — updated load_app_data() with per-entry setdefault; new update_listing_coords() and get_listing_coords() helpers; _pending_to_property carries lat/lng/commute_minutes"
    - "app/kv_listing_parser.py — Listing dataclass extended with lat and lng; new COORD_*_RE module-level regexes; fetch_listing() attempts coord extraction from resp.text"
    - "app/tests/test_geocoding.py — Wave 0 tests: coord regex probes, Listing.lat/lng roundtrip, data_store per-entry migration, update_listing_coords/get_listing_coords"
  key_links:
    - "kv_listing_parser.Listing.lat/lng → dataclasses.asdict() → ingest_handler pending_entry → data_store.add_to_pending() → app_data.json pending[] entry (verified by fact that ingest_handler already uses **dataclasses.asdict(listing), so lat/lng flow automatically once on the dataclass)"
    - "Existing pending/properties JSON entries → load_app_data setdefault → all consumers see lat/lng/commute_minutes as None until later plans populate them"
    - "_pending_to_property carry-over → approval preserves lat/lng/commute_minutes into properties[] so map pins remain stable across approve action"
tags:
  - phase-5
  - data-model
  - geocoding
---

<objective>
Establish the Phase 5 data-model foundation: extend the Listing dataclass, config, and data_store with the lat/lng/commute_minutes fields and helpers that Plans 02, 03, and 04 depend on. Also add best-effort coordinate extraction from kv.ee listing HTML (D-03 primary path). Ships behind the existing pipeline so nothing breaks if the regex probes miss — Plan 02's Nominatim fallback will fill the gaps.

Purpose: Every subsequent plan in Phase 5 (ORS commute, district heat zones, map rendering) reads lat/lng and commute_minutes off pending[]/properties[] entries. Without this migration, all downstream work would fail on KeyError against pre-migration app_data.json files.

Output: Extended Listing dataclass (byte-identical between scraper-client and VPS per Phase 1 constraint), migrated data_store schema, coordinate-extraction attempt in fetch_listing, config constants for ORS + Bolt HQ, and a Wave 0 pytest scaffold.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-map-overview-ui/05-CONTEXT.md
@.planning/phases/05-map-overview-ui/05-RESEARCH.md
@.planning/phases/05-map-overview-ui/05-PATTERNS.md
@app/config.py
@app/data_store.py
@app/kv_listing_parser.py
@app/tests/conftest.py
</context>

<phase_goal>
The overview page will show every dossier apartment on an interactive Tallinn map with score-coloured pins, price/m² heat zones by district, a 20-minute commute isochrone from Veerenni 28, and a detail side panel with commute time — plus a comparison view for 2–4 listings. This plan lays the data foundation that makes all of that possible.
</phase_goal>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — app/tests/test_geocoding.py</name>
  <files>app/tests/test_geocoding.py</files>
  <read_first>
    - app/tests/conftest.py (fixtures: tmp_agent_state, client)
    - app/tests/test_pending.py (patterns for data_store tests using tmp_agent_state)
    - app/data_store.py (current load_app_data, _pending_to_property, add_to_pending)
    - app/kv_listing_parser.py (Listing dataclass, fetch_listing structure)
    - .planning/phases/05-map-overview-ui/05-RESEARCH.md sections 1, 9, 10 (coord patterns, Listing shape, data_store shape)
    - .planning/phases/05-map-overview-ui/05-PATTERNS.md (setdefault migration pattern, helper function pattern)
  </read_first>
  <behavior>
    - test_listing_dataclass_has_lat_lng: instantiate Listing(id="x", url="y") — assert listing.lat is None and listing.lng is None (must exist as attributes, default None)
    - test_load_app_data_migrates_missing_lat_lng: seed app_data.json with a pending entry containing only {"id": "abc", "title": "T"} (no lat, lng, commute_minutes); call data_store.load_app_data(); assert loaded entry has lat is None, lng is None, commute_minutes is None
    - test_load_app_data_preserves_existing_coords: seed app_data.json with a pending entry {"id": "abc", "lat": 59.4, "lng": 24.7, "commute_minutes": 15}; call load_app_data(); assert values are preserved unchanged
    - test_update_listing_coords_pending: seed pending entry; call data_store.update_listing_coords("abc", 59.42, 24.72, 18); assert returns True and next load_app_data() shows the values on the pending entry
    - test_update_listing_coords_properties: seed properties entry; call update_listing_coords; assert values written on properties entry
    - test_update_listing_coords_not_found: call update_listing_coords("missing", 59.42, 24.72, 18); assert returns False
    - test_get_listing_coords_hit: seed entry with coords; call get_listing_coords; assert dict {'lat': 59.42, 'lng': 24.72, 'commute_minutes': 18}
    - test_get_listing_coords_miss: assert {'lat': None, 'lng': None, 'commute_minutes': None} for unknown id
    - test_pending_to_property_carries_coords: build pending entry dict with lat=59.4, lng=24.7, commute_minutes=15; call data_store._pending_to_property(entry); assert returned prop dict has "lat": 59.4, "lng": 24.7, "commute_minutes": 15
    - test_coord_regex_json_ld_pattern: feed a synthetic HTML string containing `"latitude": 59.4203, "longitude": 24.7205` to the module-level COORD_LAT_RE / COORD_LNG_RE (or a dedicated _extract_coords helper if the plan introduces one); assert both patterns match and yield the expected floats
    - test_coord_regex_script_var_pattern: feed HTML containing `"lat":59.4203,"lng":24.7205` (compact JSON no spaces); assert extraction returns (59.4203, 24.7205)
    - test_coord_regex_data_attribute_pattern: feed HTML containing `<div id="map" data-lat="59.4203" data-lng="24.7205">`; assert extraction succeeds
    - test_coord_regex_no_match_returns_none: feed HTML with no coord-shaped tokens; assert extraction returns (None, None) — never raises
  </behavior>
  <action>
    Create `app/tests/test_geocoding.py` following the pattern of `app/tests/test_pending.py`. Import fixtures `tmp_agent_state` from conftest.py (indirectly — pytest auto-discovers). Use the same style: `import json`, `from pathlib import Path`, seed `tmp_path / "app_data.json"` with json.dump, then call `data_store.load_app_data()` or the helper under test.

    The 13 tests listed in `<behavior>` are ALL initially expected to FAIL (RED) — they exercise APIs that Tasks 2, 3, 4 will introduce. Naming convention: snake_case test functions starting with `test_`. Use pytest's built-in tmp_path fixture (already used elsewhere via tmp_agent_state).

    For the regex probe tests, import the module-level regex constants directly (e.g. `from kv_listing_parser import COORD_LAT_RE, COORD_LNG_RE`) or the helper function name introduced in Task 4 — whichever the plan settles on. The test file MUST resolve the coord probe function name that Task 4 will export. Convention: expose a private function `kv_listing_parser._extract_coords(html: str) -> tuple[Optional[float], Optional[float]]` and test it directly; this keeps the test isolated from BeautifulSoup + HTTP.

    Do NOT run pytest yet — running the empty scaffold will collect-error because the target functions don't exist. Task 2/3/4 will create them, then Task 5 (verify) runs the full test file.
  </action>
  <verify>
    <automated>test -f app/tests/test_geocoding.py &amp;&amp; grep -c 'def test_' app/tests/test_geocoding.py</automated>
  </verify>
  <acceptance_criteria>
    - File `app/tests/test_geocoding.py` exists
    - `grep -c 'def test_' app/tests/test_geocoding.py` returns 13 (matches the 13 behavior tests listed above)
    - Test names include `test_listing_dataclass_has_lat_lng`, `test_load_app_data_migrates_missing_lat_lng`, `test_update_listing_coords_pending`, `test_update_listing_coords_not_found`, `test_get_listing_coords_hit`, `test_get_listing_coords_miss`, `test_pending_to_property_carries_coords`, `test_coord_regex_json_ld_pattern`, `test_coord_regex_script_var_pattern`, `test_coord_regex_data_attribute_pattern`, `test_coord_regex_no_match_returns_none` (grep each name)
    - File uses `import data_store` (not `from data_store import *`) — enforces module-qualified access
    - File imports Listing from kv_listing_parser
  </acceptance_criteria>
  <done>Wave 0 test scaffold in place. All 13 tests defined but expected to fail until Tasks 2, 3, 4 land. No production code yet altered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: config.py — add ORS_API_KEY, BOLT_HQ_LAT, BOLT_HQ_LNG</name>
  <files>app/config.py</files>
  <read_first>
    - app/config.py (existing env-var pattern lines 8–33, hard-coded constant pattern lines 19–21)
    - .planning/phases/05-map-overview-ui/05-PATTERNS.md section "app/config.py" (env var pattern + hard-coded constants pattern)
    - .planning/phases/05-map-overview-ui/05-RESEARCH.md section 11 (config.py analysis)
  </read_first>
  <action>
    In `app/config.py`, add these three module-level constants:

    1. **After** the existing line `ANTHROPIC_MODEL = os.environ.get(...)`, add:
       `ORS_API_KEY = os.environ.get("ORS_API_KEY", "")` — env-var pattern matching all existing token env vars (default empty string, no exception if missing per D-06 and RESEARCH Pitfall 5).

    2. **After** the `SMTP_PORT = 465` line (which is the last hard-coded constant), add two hard-coded float constants:
       `BOLT_HQ_LAT = 59.4203  # Veerenni 28, Tallinn (Bolt HQ) — Leaflet [lat, lng] order`
       `BOLT_HQ_LNG = 24.7205  # ORS/GeoJSON uses [lng, lat] order — see RESEARCH Pitfall 2`

    Constants are hard-coded per D-06 (they are known real-world coordinates, not deployment config). Do NOT add them as env vars.

    Do NOT modify any other line in config.py. Do NOT add try/except around env.get (matches all existing conventions). Do NOT log the ORS_API_KEY value anywhere in the module (never-log-secrets convention).
  </action>
  <verify>
    <automated>python3 -c "import sys; sys.path.insert(0, 'app'); import config; assert hasattr(config, 'ORS_API_KEY'); assert config.BOLT_HQ_LAT == 59.4203; assert config.BOLT_HQ_LNG == 24.7205; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^ORS_API_KEY = os.environ.get("ORS_API_KEY"' app/config.py` returns 1
    - `grep -c '^BOLT_HQ_LAT = 59.4203' app/config.py` returns 1
    - `grep -c '^BOLT_HQ_LNG = 24.7205' app/config.py` returns 1
    - `python3 -c "import sys; sys.path.insert(0,'app'); import config; print(config.ORS_API_KEY, config.BOLT_HQ_LAT, config.BOLT_HQ_LNG)"` prints `  59.4203 24.7205` (empty string for ORS_API_KEY when env unset)
    - Number of lines in `app/config.py` increased by exactly 3 relative to before this task (no other edits)
  </acceptance_criteria>
  <done>ORS_API_KEY env-var import + Bolt HQ coord constants are importable; existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: data_store.py — per-entry migration + update_listing_coords + get_listing_coords + _pending_to_property carry-over</name>
  <files>app/data_store.py</files>
  <read_first>
    - app/data_store.py (load_app_data lines 83–92, add_to_pending lines 144–155, approve_listing lines 164–175, _pending_to_property lines 204–240)
    - .planning/phases/05-map-overview-ui/05-PATTERNS.md section "app/data_store.py" (setdefault migration, load-modify-save, _pending_to_property extension)
    - .planning/phases/05-map-overview-ui/05-RESEARCH.md section 8 (data_store shape, thread-safety notes)
    - app/tests/test_geocoding.py (Wave 0 tests — implementation must satisfy them)
  </read_first>
  <action>
    Make three additions inside `app/data_store.py`:

    **(a) Extend `load_app_data()` with per-entry setdefault** — after the existing 6 `data.setdefault(...)` lines and BEFORE `return data`, iterate all entries in `properties` and `pending` and setdefault their `lat`, `lng`, `commute_minutes` to None. Use:
    - Loop over `data.get("properties", []) + data.get("pending", [])`
    - For each entry: `entry.setdefault("lat", None)`, `entry.setdefault("lng", None)`, `entry.setdefault("commute_minutes", None)`
    - This is a per-entry migration — zero-downtime for existing installs (matches D-11 pattern from Phase 3).
    - Keep the existing `with _lock:` — the whole function stays inside the lock.

    **(b) Add two new module-level functions** after `get_approved_listing()` and before `load_agent_state()`:

    - `update_listing_coords(listing_id: str, lat: float, lng: float, commute_minutes: Optional[int]) -> bool`
      - Acquires `_lock`, calls `load_app_data()`, finds the entry with matching id in `data["properties"] + data["pending"]` (search properties first then pending; do NOT create new entries), updates the three fields on the matched entry, calls `save_app_data(data)`, returns True. Returns False if not found. Follows the same load-modify-save pattern as `add_to_pending`. Signature parameter type: use `Optional[int]` for commute_minutes (the value may legitimately be None while lat/lng are known — see Task 4). Docstring: "Update lat/lng/commute_minutes on a properties or pending entry. Returns True if found and updated, False otherwise. Thread-safe (acquires _lock)."

    - `get_listing_coords(listing_id: str) -> dict`
      - Acquires `_lock`, calls `load_app_data()`, searches properties first then pending for id match. Returns a dict `{"lat": entry.get("lat"), "lng": entry.get("lng"), "commute_minutes": entry.get("commute_minutes")}` on hit. On miss returns `{"lat": None, "lng": None, "commute_minutes": None}`. Never raises — wrap in try/except returning the all-None dict on error. Docstring: "Return coord dict for a listing_id. Returns all-None dict on miss or error. Thread-safe."

    **(c) Extend `_pending_to_property()`** — inside the `prop = { ... }` dict literal, add three new keys AFTER the existing `"draft_subject": entry.get("draft_subject", ""),` line and before the closing `}`:
    - `"lat": entry.get("lat"),`
    - `"lng": entry.get("lng"),`
    - `"commute_minutes": entry.get("commute_minutes"),`

    Do NOT change the existing signature or lock structure. Do NOT introduce try/except around the update functions except for `get_listing_coords` (guaranteed safe reader per D-16 never-raise pattern for readers). `update_listing_coords` follows the raise-on-catastrophic pattern of `add_to_pending` (no explicit try/except — only file I/O errors bubble up, which is desired for callers to see).

    IMPORTANT: Do NOT delete or reorder any existing lines in `load_app_data()` — only append the per-entry loop. Do NOT modify existing setdefaults. Do NOT modify `approve_listing()`, `reject_listing()`, or other helpers.
  </action>
  <verify>
    <automated>cd app &amp;&amp; python3 -m pytest tests/test_geocoding.py::test_load_app_data_migrates_missing_lat_lng tests/test_geocoding.py::test_load_app_data_preserves_existing_coords tests/test_geocoding.py::test_update_listing_coords_pending tests/test_geocoding.py::test_update_listing_coords_properties tests/test_geocoding.py::test_update_listing_coords_not_found tests/test_geocoding.py::test_get_listing_coords_hit tests/test_geocoding.py::test_get_listing_coords_miss tests/test_geocoding.py::test_pending_to_property_carries_coords -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def update_listing_coords' app/data_store.py` returns 1
    - `grep -c 'def get_listing_coords' app/data_store.py` returns 1
    - `grep -c 'entry.setdefault("lat", None)' app/data_store.py` returns 1
    - `grep -c 'entry.setdefault("lng", None)' app/data_store.py` returns 1
    - `grep -c 'entry.setdefault("commute_minutes", None)' app/data_store.py` returns 1
    - `grep -c '"lat": entry.get("lat")' app/data_store.py` returns 1 (inside _pending_to_property)
    - `grep -c '"lng": entry.get("lng")' app/data_store.py` returns 1
    - `grep -c '"commute_minutes": entry.get("commute_minutes")' app/data_store.py` returns 1
    - `cd app && python3 -m pytest tests/test_geocoding.py -k "not coord_regex and not listing_dataclass" -x -q` — all 8 data_store-related tests from test_geocoding.py pass
    - `cd app && python3 -m pytest tests/test_pending.py tests/test_price_intelligence.py tests/test_ingest.py -q` — existing test suites still pass (regression check)
  </acceptance_criteria>
  <done>data_store.py has per-entry migration, two new coord helpers, and _pending_to_property carries the new fields. All eight data-store tests in test_geocoding.py pass. No regression in existing suites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: kv_listing_parser.py — Listing.lat/lng fields + _extract_coords helper + fetch_listing integration</name>
  <files>app/kv_listing_parser.py</files>
  <read_first>
    - app/kv_listing_parser.py (existing regex block lines 31–47, Listing dataclass lines 50–71, fetch_listing lines 93–174)
    - .planning/phases/05-map-overview-ui/05-PATTERNS.md section "app/kv_listing_parser.py" (regex naming, dataclass extension, extraction pattern)
    - .planning/phases/05-map-overview-ui/05-RESEARCH.md section 1 (kv.ee coordinate extraction — three fallback patterns)
    - app/tests/test_geocoding.py (regex tests — implementation must satisfy them)
  </read_first>
  <action>
    Make three additions inside `app/kv_listing_parser.py`:

    **(a) Add module-level regex constants** — after the existing `BROKER_RE = ...` line (line ~47) and before `@dataclass`, add:

    Four probe regexes covering the three assumed kv.ee coord embedding shapes plus JSON-LD:
    - `COORD_LAT_JSONLD_RE` — matches `"latitude"` JSON key: `re.compile(r'"latitude"\s*:\s*(-?\d+\.\d+)')`
    - `COORD_LNG_JSONLD_RE` — matches `"longitude"` JSON key: `re.compile(r'"longitude"\s*:\s*(-?\d+\.\d+)')`
    - `COORD_LAT_SCRIPT_RE` — matches script-var short form: `re.compile(r'["\']lat["\']\s*:\s*(-?\d+\.\d+)')`
    - `COORD_LNG_SCRIPT_RE` — matches script-var short form: `re.compile(r'["\']ln[gG]["\']\s*:\s*(-?\d+\.\d+)')` (matches "lng" or "lnG"; kv.ee is Estonian/likely uses "lng" but be liberal)
    - `COORD_LAT_DATA_RE` — matches HTML data attribute: `re.compile(r'data-lat=["\'](-?\d+\.\d+)["\']')`
    - `COORD_LNG_DATA_RE` — matches HTML data attribute: `re.compile(r'data-lng=["\'](-?\d+\.\d+)["\']')`

    All regexes must be module-level UPPERCASE_RE constants matching the existing naming convention (PRICE_RE, ROOMS_RE, ...).

    **(b) Extend the Listing dataclass** — inside the `@dataclass class Listing:` block (currently ending with `raw_ok: bool = True`), append two new fields as the LAST two:
    - `lat: Optional[float] = None`
    - `lng: Optional[float] = None`

    The two fields go at the bottom of the dataclass to preserve field order for existing serialised entries (dataclasses.asdict + ** unpacking in ingest_handler are order-agnostic, but appending is conventional for schema evolution). Type annotation: `Optional[float]`. Default: `None`. This preserves byte-identical parity with the scraper-client copy of this file (per Phase 1 D-05) — the scraper-client repo must receive the same edit, but that is outside this VPS-side plan's scope.

    **(c) Add a private `_extract_coords()` helper** — after the `_to_float()` function (line ~85), add:

    ```
    def _extract_coords(html: str) -> tuple[Optional[float], Optional[float]]:
        """Best-effort lat/lng extraction from raw HTML (not BeautifulSoup text).

        Probes three fallback patterns in order — JSON-LD structured data, script var,
        HTML data attribute — and returns the first successful match. Never raises
        (returns (None, None) on any failure). Lat/lng are validated within Estonia
        bounding box (57.5-59.7 lat, 21.7-28.2 lng) to reject false-positive matches
        of unrelated decimals in the same page.
        """
    ```

    Function body (prose specification — implementer writes the code):
    - Attempt Pattern 1 (JSON-LD): search `html` with COORD_LAT_JSONLD_RE and COORD_LNG_JSONLD_RE; if both match, capture floats.
    - Attempt Pattern 2 (script var): if Pattern 1 failed, search with COORD_LAT_SCRIPT_RE and COORD_LNG_SCRIPT_RE.
    - Attempt Pattern 3 (data attribute): if Pattern 2 failed, search with COORD_LAT_DATA_RE and COORD_LNG_DATA_RE.
    - After any successful pair: validate lat in [57.5, 59.7] and lng in [21.7, 28.2]. If out of range, discard both and try the next pattern.
    - On any exception, return (None, None).
    - On no match, return (None, None).

    **(d) Wire _extract_coords into fetch_listing** — after the existing `og_desc` block (line ~172) and BEFORE the final `return listing`, add:
    - Call `lat, lng = _extract_coords(resp.text)` — note: resp.text (raw HTML), NOT the `text` variable (soup.get_text()) — coords live in script blocks that BeautifulSoup strips.
    - If both `lat is not None and lng is not None`: `listing.lat = lat; listing.lng = lng`
    - No log statement, no try/except (already inside the outer try/except at the top of fetch_listing which returns raw_ok=False on any request failure; extraction runs after resp is proven valid).

    IMPORTANT: Do NOT modify the existing regex block or extraction sequence (price, rooms, area, ...). Only APPEND. Do NOT change the Listing dataclass field order for the existing fields — append lat/lng after raw_ok.
  </action>
  <verify>
    <automated>cd app &amp;&amp; python3 -m pytest tests/test_geocoding.py::test_listing_dataclass_has_lat_lng tests/test_geocoding.py::test_coord_regex_json_ld_pattern tests/test_geocoding.py::test_coord_regex_script_var_pattern tests/test_geocoding.py::test_coord_regex_data_attribute_pattern tests/test_geocoding.py::test_coord_regex_no_match_returns_none -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'lat: Optional\[float\] = None' app/kv_listing_parser.py` returns 1
    - `grep -c 'lng: Optional\[float\] = None' app/kv_listing_parser.py` returns 1
    - `grep -c 'def _extract_coords' app/kv_listing_parser.py` returns 1
    - `grep -c 'COORD_LAT_JSONLD_RE' app/kv_listing_parser.py` returns >= 2 (definition + use in _extract_coords)
    - `grep -c 'COORD_LNG_JSONLD_RE' app/kv_listing_parser.py` returns >= 2
    - `grep -c 'COORD_LAT_SCRIPT_RE' app/kv_listing_parser.py` returns >= 2
    - `grep -c 'COORD_LNG_SCRIPT_RE' app/kv_listing_parser.py` returns >= 2
    - `grep -c 'COORD_LAT_DATA_RE' app/kv_listing_parser.py` returns >= 2
    - `grep -c 'COORD_LNG_DATA_RE' app/kv_listing_parser.py` returns >= 2
    - `grep -c '_extract_coords(resp.text)' app/kv_listing_parser.py` returns 1 (used inside fetch_listing)
    - `cd app && python3 -m pytest tests/test_geocoding.py::test_listing_dataclass_has_lat_lng -x -q` passes
    - `cd app && python3 -m pytest tests/test_geocoding.py -k coord_regex -x -q` — all 4 regex probe tests pass
    - `cd app && python3 -m pytest tests/test_listing_contract.py -q` — existing dataclass contract test still passes (regression)
  </acceptance_criteria>
  <done>Listing dataclass exposes lat/lng, kv_listing_parser has three coord probe patterns with Estonia-bounding-box validation, fetch_listing populates lat/lng when patterns match. All 5 regex + dataclass tests pass. No regression in existing kv_listing_parser tests.</done>
</task>

<task type="auto">
  <name>Task 5: Full Wave 0 test file — green run</name>
  <files>app/tests/test_geocoding.py</files>
  <read_first>
    - app/tests/test_geocoding.py
    - app/data_store.py (post-Task-3)
    - app/kv_listing_parser.py (post-Task-4)
    - app/config.py (post-Task-2)
  </read_first>
  <action>
    Run the full test_geocoding.py file. All 13 tests must be green. No implementation changes in this task — this is the end-to-end verification that Tasks 1–4 landed a coherent slice.

    If any test fails: diagnose against the acceptance criteria of the responsible task (Task 2 = config, Task 3 = data_store, Task 4 = kv_listing_parser) and fix in the appropriate source file — do NOT modify the test file to make it pass. The test file is the contract; source files satisfy it.

    Also run the full existing test suite as a regression sanity check.
  </action>
  <verify>
    <automated>cd app &amp;&amp; python3 -m pytest tests/test_geocoding.py -v &amp;&amp; python3 -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && python3 -m pytest tests/test_geocoding.py -v` — 13 passed, 0 failed
    - `cd app && python3 -m pytest tests/ -q` — full suite exits 0 (no regressions in test_pending, test_price_intelligence, test_ingest, test_eval_quality, test_heartbeat, test_listing_contract)
    - No new pytest warnings introduced (compare warning count to pre-Task-1 baseline mentally)
  </acceptance_criteria>
  <done>Wave 0 for Plan 01 is fully green. Downstream plans (02, 03, 04) can rely on the data model in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kv.ee HTML → parser | Untrusted HTML content is regex-matched for coord floats; malformed values could crash parsing |
| pending/properties entry → downstream consumers | New lat/lng/commute_minutes fields flow into JSON serialisation and the /api/data response |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | _extract_coords bounding-box validation | low | mitigate | Estonia bounding box check (lat 57.5–59.7, lng 21.7–28.2) rejects garbage decimals from HTML that happen to match the pattern but land outside Estonia (e.g. pixel coords, prices, IDs). Ensures downstream ORS calls never hit an unrelated country. Implemented in Task 4. |
| T-05-02 | DoS | _extract_coords regex on adversarial HTML | low | accept | All 6 regexes are anchored on short prefix tokens (`"latitude":`, `data-lat=`, ...) with bounded fractional-digit matches (`\d+\.\d+`). No catastrophic backtracking risk. The outer fetch_listing already has a request timeout, bounding total exposure. |
| T-05-03 | Info Disclosure | ORS_API_KEY logging | high | mitigate | ORS_API_KEY constant added to config.py but never referenced in a log statement in this plan. Convention from Phase 1 D-07 (INGEST_TOKEN) applies: NEVER log the value. Confirmed by grep gate in Plan 02 threat model. |
| T-05-04 | Tampering | Nominatim fallback address input | medium | mitigate | Deferred to Plan 02 — this plan does not accept user-controlled addresses. Coordinate extraction consumes kv.ee HTML which is already trusted enough to run through the existing regex parser. |
</threat_model>

<verification>
1. Every task's automated verify command passes.
2. `cd app && python3 -m pytest tests/ -q` exits 0 (no regressions across the six pre-existing test files).
3. `python3 -c "import sys; sys.path.insert(0,'app'); import config; import data_store; from kv_listing_parser import Listing, _extract_coords; l=Listing(id='x', url='y'); assert l.lat is None; assert data_store.get_listing_coords('missing') == {'lat': None, 'lng': None, 'commute_minutes': None}; print('smoke OK')"` — smoke test passes without exception.
4. Manual (not blocking): open `app/data/app_data.json` (if present) — verify data_store.load_app_data() applied per-entry setdefault to any existing pending/properties entry on next load (visible as lat/lng/commute_minutes keys with null values).
</verification>

<success_criteria>
- Every listed acceptance criterion above green.
- No exception raised during `python3 -c "from app.data_store import load_app_data; print(len(load_app_data().get('properties', [])))"`-style smoke import.
- Plan 02 can begin: it will `import config` and read config.ORS_API_KEY, config.BOLT_HQ_LAT, config.BOLT_HQ_LNG, and call data_store.update_listing_coords() without any further scaffolding.
</success_criteria>

<artifacts_this_phase_produces>
This plan (05-01) produces these symbols that later plans consume:

| Symbol | Kind | Consumer |
|--------|------|----------|
| `config.ORS_API_KEY` | str constant (env-loaded) | Plan 02 (_fetch_commute_minutes, /api/refresh-isochrone) |
| `config.BOLT_HQ_LAT` | float constant = 59.4203 | Plan 02, Plan 04 (mini-map centre) |
| `config.BOLT_HQ_LNG` | float constant = 24.7205 | Plan 02, Plan 04 (mini-map centre) |
| `kv_listing_parser.Listing.lat` | Optional[float] dataclass field | Plan 02 (ORS Matrix input), Plan 04 (map pin) |
| `kv_listing_parser.Listing.lng` | Optional[float] dataclass field | Plan 02, Plan 04 |
| `kv_listing_parser._extract_coords(html)` | private helper | Plan 02 could re-use if it decides to re-extract; primary consumer is fetch_listing which is already wired |
| `kv_listing_parser.COORD_*_RE` | 6 regex constants | Available for reuse; consumed indirectly via _extract_coords |
| `data_store.update_listing_coords(id, lat, lng, commute_minutes)` | bool-returning helper | Plan 02 (Nominatim backfill), Plan 02 (ORS matrix result write) |
| `data_store.get_listing_coords(id)` | dict-returning reader | Available for reuse; primary reader is /api/data (which already returns full entries) |
| `data_store.load_app_data()` per-entry setdefault | migration behaviour | Plan 02, 03, 04 all rely on lat/lng/commute_minutes being non-KeyError on every entry |
| `data_store._pending_to_property()` carry-over | approval-time preservation | Plan 04 (map pins remain stable when Daniel approves a pending listing) |
| Entry-level `lat`, `lng`, `commute_minutes` keys in app_data.json properties[] / pending[] | JSON schema fields | Plan 02 writes them; Plan 03 reads district for heat zones; Plan 04 reads all three for map + detail panel |
| `app/tests/test_geocoding.py` | 13-test scaffold | Plan 02 will add more tests here (Nominatim backfill, ORS integration) |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-map-overview-ui/05-01-SUMMARY.md` when done. Summary must include: which coord regex patterns matched on the first real kv.ee listing test (if executor ran a live probe via the Playwright session — optional, and only if ORS_API_KEY was also configured for a full smoke test), or a note that live probing is deferred to Plan 02's Nominatim fallback path.
</output>
