---
phase: 05-map-overview-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/ingest_handler.py
  - app/main.py
  - app/static/isochrone.geojson
  - app/tests/test_ors_api.py
  - app/tests/test_commute.py
autonomous: true
requirements:
  - MAP-04
  - MAP-05
  - MAP-06
user_setup:
  - service: OpenRouteService
    why: "MAP-04 20-minute driving isochrone from Veerenni 28; MAP-06 per-listing commute time via ORS Matrix API"
    env_vars:
      - name: ORS_API_KEY
        source: "Sign up at https://openrouteservice.org (free tier, no billing card). Copy the API key from the developer dashboard. Add ORS_API_KEY=<key> to your .env file and to the docker-compose.yml `app` service `environment:` block. Restart the app container. Without this key, commute_minutes stays null on every entry and the isochrone endpoint returns an error — non-blocking, the map still renders per RESEARCH Pitfall 5."
    dashboard_config: []
must_haves:
  truths:
    - "When ORS_API_KEY is configured, `POST /api/refresh-isochrone` fetches the 20-min driving-car isochrone from ORS for Veerenni 28 (config.BOLT_HQ_LNG=24.7205, config.BOLT_HQ_LAT=59.4203) and writes app/static/isochrone.geojson"
    - "When ORS_API_KEY is configured, incoming listings with lat/lng populate get their commute_minutes filled by an ORS Matrix call before the pending entry is saved"
    - "When ORS_API_KEY is empty, ORS calls skip silently — commute_minutes stays None, no exception raised (RESEARCH Pitfall 5)"
    - "`POST /api/geocode-backfill` iterates properties[] + pending[], calls Nominatim for entries with lat is None or lng is None (with 1.1 sec sleep between calls per Nominatim usage policy), writes lat/lng via data_store.update_listing_coords; returns {ok: true, geocoded: N, skipped: M}"
    - "`GET /api/isochrone` returns the contents of app/static/isochrone.geojson as JSON with content-type application/geo+json; returns an empty FeatureCollection if the file is missing rather than 404"
    - "All new backend endpoints follow the never-raise convention: any Nominatim/ORS/file error is logged via log.exception and returns a structured error dict — no unhandled exception propagates"
    - "The ORS API integration uses [lng, lat] order at the API boundary and comments the order at every ORS call site (RESEARCH Pitfall 2)"
    - "ORS_API_KEY value is NEVER logged (grep confirms zero occurrences of `log.*ORS_API_KEY` in modified files)"
  artifacts:
    - "app/ingest_handler.py — new private helpers _fetch_commute_minutes(lat, lng) and _geocode_with_nominatim(query); ORS matrix call wired into process_ingest_batch after pending entry write"
    - "app/main.py — three new endpoints: GET /api/isochrone, POST /api/refresh-isochrone, POST /api/geocode-backfill (all registered BEFORE the StaticFiles mount line)"
    - "app/static/isochrone.geojson — placeholder empty FeatureCollection committed initially; overwritten on first successful /api/refresh-isochrone call"
    - "app/tests/test_ors_api.py — unit tests for _fetch_commute_minutes (mocked ORS), POST /api/refresh-isochrone (mocked ORS), GET /api/isochrone (file-serving)"
    - "app/tests/test_commute.py — integration tests: process_ingest_batch with lat/lng populates commute_minutes; ORS_API_KEY empty skips the call; /api/geocode-backfill mocked Nominatim path"
  key_links:
    - "process_ingest_batch loop → after data_store.add_to_pending → look up lat/lng from listing → _fetch_commute_minutes(listing.lat, listing.lng) → mutate app_data['pending'] entry in place → save_app_data at loop end (matches _handle_price_drop in-place pattern)"
    - "Frontend fetch /api/isochrone → falls back to embedded empty FeatureCollection if backend absent (Plan 04 owns the frontend consumer)"
    - "geocode-backfill background thread pattern (matches check-now line 78) → prevents blocking the HTTP response when 20+ Nominatim calls with 1.1s sleep run serially"
tags:
  - phase-5
  - ors-integration
  - nominatim
  - backend-endpoints
---

<objective>
Wire OpenRouteService (isochrone + matrix commute times) and Nominatim (fallback geocoding) into the VPS backend so listings gain commute_minutes and lat/lng data. Deliver three new FastAPI endpoints and extend the ingest pipeline to compute commute time at ingest time. Also serve a static isochrone.geojson placeholder so the frontend has something to render even before the first /api/refresh-isochrone call.

Purpose: Plan 04 (frontend) needs commute_minutes on every entry and an isochrone GeoJSON to render the 20-min driving polygon. This plan produces the data; Plan 04 consumes it.

Output: Two new private helpers in ingest_handler (ORS matrix + Nominatim fallback), three new admin endpoints in main.py, a placeholder GeoJSON file, and pytest coverage for the ORS/Nominatim mock paths.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-map-overview-ui/05-CONTEXT.md
@.planning/phases/05-map-overview-ui/05-RESEARCH.md
@.planning/phases/05-map-overview-ui/05-PATTERNS.md
@.planning/phases/05-map-overview-ui/05-01-PLAN.md
@app/config.py
@app/data_store.py
@app/ingest_handler.py
@app/main.py
@app/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffolds — test_ors_api.py + test_commute.py</name>
  <files>app/tests/test_ors_api.py, app/tests/test_commute.py</files>
  <read_first>
    - app/tests/conftest.py (client fixture with INGEST_TOKEN monkeypatch, tmp_agent_state)
    - app/tests/test_ingest.py (mock_telegram + mock_send_pending_card + process_ingest_batch invocation pattern)
    - app/tests/test_price_intelligence.py (fixture composition for full ingest cycles)
    - .planning/phases/05-map-overview-ui/05-RESEARCH.md sections 2, 3, 10 (ORS + Nominatim + ingest_handler)
    - .planning/phases/05-map-overview-ui/05-PATTERNS.md section "app/ingest_handler.py" (never-raise, in-place mutation)
    - app/ingest_handler.py (current process_ingest_batch structure)
  </read_first>
  <behavior>
    ### test_ors_api.py (5 tests)
    - test_fetch_commute_minutes_success: monkeypatch requests.post to return a 200 response whose json() is `{"durations": [[null, 720.5]]}`; call ingest_handler._fetch_commute_minutes(59.42, 24.72); assert returns 12 (720.5 / 60 rounded, max(1, round(720.5/60)) = 12)
    - test_fetch_commute_minutes_null_duration: monkeypatch to return `{"durations": [[null, null]]}`; assert _fetch_commute_minutes returns None
    - test_fetch_commute_minutes_empty_api_key: monkeypatch config.ORS_API_KEY to ""; assert _fetch_commute_minutes returns None WITHOUT calling requests.post (assert the requests.post mock had zero calls)
    - test_fetch_commute_minutes_http_error: monkeypatch requests.post to raise requests.RequestException; assert _fetch_commute_minutes returns None (never raises)
    - test_refresh_isochrone_endpoint: use the `client` fixture; monkeypatch requests.post to return 200 with a valid GeoJSON `{"type": "FeatureCollection", "features": [{"type": "Feature", "geometry": {"type": "Polygon", "coordinates": [[[24.6, 59.35],[24.85, 59.35],[24.85, 59.5],[24.6, 59.5],[24.6, 59.35]]]}, "properties": {}}]}`; monkeypatch config.ORS_API_KEY to "test-key"; POST /api/refresh-isochrone; assert response.status_code == 200, response.json() == {"ok": True}, and that app/static/isochrone.geojson on disk contains the returned FeatureCollection
    - test_isochrone_get_serves_file: after test_refresh_isochrone_endpoint (or seed file directly), GET /api/isochrone; assert 200 and response.json()["type"] == "FeatureCollection"
    - test_isochrone_get_missing_file: unlink app/static/isochrone.geojson if present; GET /api/isochrone; assert 200 and response.json() == {"type": "FeatureCollection", "features": []}

    ### test_commute.py (6 tests)
    - test_process_ingest_batch_populates_commute_minutes: use tmp_agent_state + mock_telegram + mock_send_pending_card; monkeypatch config.ORS_API_KEY to "test-key"; monkeypatch ingest_handler._fetch_commute_minutes to return 15 (bypass real HTTP); monkeypatch evaluate_listing to return {"score": 80, "verdict": "OK", "checklist": {}, "strengths":[], "concerns":[]}; feed a listing dict with lat=59.42, lng=24.72 through process_ingest_batch; assert the resulting pending entry in app_data.json has commute_minutes == 15
    - test_process_ingest_batch_no_coords_skips_ors: same setup as above but listing dict without lat/lng; assert commute_minutes is None and _fetch_commute_minutes mock had 0 calls
    - test_process_ingest_batch_empty_ors_key: same setup but config.ORS_API_KEY = ""; listing has lat/lng; assert commute_minutes is None and no _fetch_commute_minutes call was actually made (or _fetch_commute_minutes was called and short-circuited to None — either is acceptable; test that final commute_minutes on pending entry is None)
    - test_geocode_with_nominatim_success: monkeypatch requests.get to return 200 with json `[{"lat": "59.4280671", "lon": "24.7471453", "display_name": "..."}]`; call _geocode_with_nominatim("Liivalaia 7, Tallinn"); assert returns (59.4280671, 24.7471453)
    - test_geocode_with_nominatim_empty_response: monkeypatch to return 200 with json `[]`; assert returns (None, None)
    - test_geocode_backfill_endpoint: use client; seed app_data.json with two pending entries — one with lat/lng already set (should be skipped) and one without; monkeypatch _geocode_with_nominatim to return (59.42, 24.72); monkeypatch time.sleep to no-op (via monkeypatch on ingest_handler.time.sleep); monkeypatch _fetch_commute_minutes to return 15; POST /api/geocode-backfill; assert response.status_code == 200, response.json()["ok"] is True, response.json()["geocoded"] == 1, response.json()["skipped"] == 1; verify app_data.json now has lat=59.42 on the previously-empty entry
  </behavior>
  <action>
    Create two test files following the pattern of `app/tests/test_ingest.py` and `app/tests/test_price_intelligence.py`:

    **app/tests/test_ors_api.py** — imports: `pytest`, `import config`, `import ingest_handler`, `import requests`, `from unittest.mock import MagicMock, patch`. Uses `client` and `tmp_agent_state` fixtures from conftest.py. Uses monkeypatch for `config.ORS_API_KEY`, `requests.post` (both in-module reference `ingest_handler.requests.post` and inside main.py), and `time.sleep` (where relevant).

    **app/tests/test_commute.py** — imports similar to test_ors_api.py, plus `mock_telegram` and `mock_send_pending_card` fixtures. Uses monkeypatch for `ingest_handler.evaluate_listing`, `ingest_handler._fetch_commute_minutes`, `ingest_handler._geocode_with_nominatim`.

    ALL tests initially FAIL (RED). Tasks 2, 3, 4 will implement the target functions and endpoints; Task 5 runs the full suite.

    Naming rules: snake_case, test_ prefix, imports at module level, use pytest fixtures via parameter injection (no autouse). Do NOT touch conftest.py — new fixtures for this plan are inlined into the test file if needed. For the `client` fixture to correctly monkeypatch config.ORS_API_KEY, add `monkeypatch.setattr(config, "ORS_API_KEY", "test-key")` inside each test that needs a set key (do NOT extend the client fixture).
  </action>
  <verify>
    <automated>test -f app/tests/test_ors_api.py &amp;&amp; test -f app/tests/test_commute.py &amp;&amp; grep -c 'def test_' app/tests/test_ors_api.py &amp;&amp; grep -c 'def test_' app/tests/test_commute.py</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - `grep -c 'def test_' app/tests/test_ors_api.py` returns 7 (5 _fetch_commute_minutes/isochrone tests + 2 GET tests)
    - `grep -c 'def test_' app/tests/test_commute.py` returns 6
    - Both files import `ingest_handler` at module level
    - Both files use existing conftest fixtures (client, tmp_agent_state, mock_telegram, mock_send_pending_card) — verified by grep for fixture names in function signatures
    - Neither file modifies conftest.py (git diff conftest.py returns empty)
  </acceptance_criteria>
  <done>Wave 0 scaffolds created. 13 tests total defined but currently red (RED). Tasks 2–5 will turn them green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingest_handler.py — _fetch_commute_minutes + _geocode_with_nominatim helpers</name>
  <files>app/ingest_handler.py</files>
  <read_first>
    - app/ingest_handler.py (existing helpers _whitelist_checklist lines 54–70, _record_and_check_price_drop lines 144–181, imports at top)
    - .planning/phases/05-map-overview-ui/05-PATTERNS.md section "app/ingest_handler.py" (private helper never-raise pattern, ORS_API_KEY guard)
    - .planning/phases/05-map-overview-ui/05-RESEARCH.md section 2 (ORS Matrix API request/response shape) and section 3 (Nominatim shape + User-Agent policy)
    - app/config.py (post-Task-2 of Plan 01: ORS_API_KEY, BOLT_HQ_LAT, BOLT_HQ_LNG)
    - app/tests/test_ors_api.py and app/tests/test_commute.py (expected behavior)
  </read_first>
  <action>
    Make two additions inside `app/ingest_handler.py`:

    **(a) Add `import time` and `import requests` if not already at the top** — the current file imports `dataclasses`, `logging`, `dataclasses.fields`, `datetime`. Add `import time` and `import requests` in the standard-library and third-party sections respectively, preserving alphabetical ordering.

    **(b) Add `_fetch_commute_minutes(lat, lng) -> Optional[int]`** — a new private helper placed AFTER `_whitelist_checklist` (line ~70) and BEFORE `_build_context_prefix` (line ~73). Signature: `def _fetch_commute_minutes(lat: float, lng: float) -> Optional[int]:`.

    Behavior specification:
    - Docstring: "Call ORS Matrix API for driving minutes from Veerenni 28 (config.BOLT_HQ_LAT/LNG) to one listing at (lat, lng). Returns None on failure (never-raise). Returns None if config.ORS_API_KEY is empty (soft skip)."
    - Guard: if `not config.ORS_API_KEY: return None` (soft skip, no exception).
    - Build payload dict:
      - `"locations": [[config.BOLT_HQ_LNG, config.BOLT_HQ_LAT], [lng, lat]]` — inline comment `# [lon, lat] order per ORS/GeoJSON convention; DO NOT swap. See RESEARCH Pitfall 2.`
      - `"sources": [0]`
      - `"destinations": [1]`
      - `"metrics": ["duration"]`
    - Headers: `{"Authorization": f"Bearer {config.ORS_API_KEY}", "Content-Type": "application/json"}`
    - HTTP: `requests.post("https://api.openrouteservice.org/v2/matrix/driving-car", json=payload, headers=headers, timeout=10)` — wrapped in try/except Exception. Call `resp.raise_for_status()` after.
    - Parse: `duration_secs = resp.json()["durations"][0][0]`. If `duration_secs is None`, return None. Otherwise return `max(1, round(duration_secs / 60))`.
    - On any exception: `log.exception("ORS matrix call failed for lat=%s lng=%s — skipping commute_minutes", lat, lng)` and return None.
    - NEVER log the ORS_API_KEY value or the payload dict (which contains the URL but not the key — safe). NEVER log the Authorization header contents.

    **(c) Add `_geocode_with_nominatim(query: str) -> tuple[Optional[float], Optional[float]]`** — placed immediately after `_fetch_commute_minutes`. Signature: `def _geocode_with_nominatim(query: str) -> tuple[Optional[float], Optional[float]]:`.

    Behavior specification:
    - Docstring: "Geocode an address string via Nominatim. Returns (lat, lng) tuple on success or (None, None) on failure. Never raises. Caller is responsible for sleeping 1.1 seconds between successive calls per Nominatim usage policy (RESEARCH section 3)."
    - No API key needed (Nominatim is unauthenticated).
    - Build headers: `{"User-Agent": "ApartsLooker/1.0 daniel.tjulinov@gmail.com"}` — NON-NEGOTIABLE per Nominatim usage policy (RESEARCH Pitfall 4). The exact contact email is on record for the project.
    - HTTP: `requests.get("https://nominatim.openstreetmap.org/search", params={"q": query, "format": "json", "limit": 1}, headers=headers, timeout=10)`. Wrap in try/except Exception. Call `resp.raise_for_status()` after.
    - Parse: `results = resp.json()`. If not `results` (empty list) or not isinstance(results, list): return `(None, None)`. Else take `results[0]` and return `(float(results[0]["lat"]), float(results[0]["lon"]))`.
    - On any exception: `log.exception("Nominatim geocode failed for query=%s", query)` and return `(None, None)`.

    **(d) Wire _fetch_commute_minutes into process_ingest_batch** — inside the for-loop of process_ingest_batch, AFTER the existing `data_store.add_to_pending(pending_entry)` call (line ~393) and `data_store.write_checklist_ai(...)` and `_send_card(...)` and the app_data reload (line ~412), BUT BEFORE the `_record_and_check_price_drop(app_data, listing, today_str)` call (line ~425), add:

    ```
    # Phase 5 (MAP-06): compute driving commute time from Veerenni 28 to this listing.
    # Coords come from kv_listing_parser (D-03) or Nominatim backfill (D-04).
    # In-place mutation of app_data['pending'] entry mirrors _handle_price_drop pattern
    # (RESEARCH section 10) — do NOT call update_listing_coords here (would re-enter _lock).
    if listing.lat is not None and listing.lng is not None:
        commute_mins = _fetch_commute_minutes(listing.lat, listing.lng)
        for entry in app_data.get("pending", []):
            if entry.get("id") == listing.id:
                entry["lat"] = listing.lat
                entry["lng"] = listing.lng
                entry["commute_minutes"] = commute_mins
                break
    ```

    Note: the pending_entry itself was built via `**dataclasses.asdict(listing)` (line ~380) so it already has `lat`/`lng` keys populated from the Listing dataclass. The mutation above is defensive re-assertion + commute_minutes injection. `commute_mins` may be None (either because ORS_API_KEY empty or the API call failed) — that is fine, the entry gets None which matches the migrated default.

    IMPORTANT: Do NOT modify _handle_price_drop, _record_and_check_price_drop, or any other function beyond adding the two helpers and the in-loop wiring above. Do NOT change the outer with-lock structure.
  </action>
  <verify>
    <automated>cd app &amp;&amp; python3 -m pytest tests/test_ors_api.py::test_fetch_commute_minutes_success tests/test_ors_api.py::test_fetch_commute_minutes_null_duration tests/test_ors_api.py::test_fetch_commute_minutes_empty_api_key tests/test_ors_api.py::test_fetch_commute_minutes_http_error tests/test_commute.py::test_geocode_with_nominatim_success tests/test_commute.py::test_geocode_with_nominatim_empty_response tests/test_commute.py::test_process_ingest_batch_populates_commute_minutes tests/test_commute.py::test_process_ingest_batch_no_coords_skips_ors tests/test_commute.py::test_process_ingest_batch_empty_ors_key -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def _fetch_commute_minutes' app/ingest_handler.py` returns 1
    - `grep -c 'def _geocode_with_nominatim' app/ingest_handler.py` returns 1
    - `grep -c 'https://api.openrouteservice.org/v2/matrix/driving-car' app/ingest_handler.py` returns 1
    - `grep -c 'https://nominatim.openstreetmap.org/search' app/ingest_handler.py` returns 1
    - `grep -c 'ApartsLooker/1.0' app/ingest_handler.py` returns 1 (User-Agent for Nominatim)
    - `grep -c '_fetch_commute_minutes(listing.lat, listing.lng)' app/ingest_handler.py` returns 1 (used inside process_ingest_batch)
    - `grep -c 'if not config.ORS_API_KEY' app/ingest_handler.py` returns 1 (soft skip inside _fetch_commute_minutes)
    - Zero occurrences of `log\..*ORS_API_KEY` in the file — `grep -c 'log\..*ORS_API_KEY' app/ingest_handler.py` returns 0
    - 9 of 13 tests pass: 4 test_ors_api ORS matrix tests + 2 Nominatim tests + 3 process_ingest_batch tests
    - `cd app && python3 -m pytest tests/test_ingest.py tests/test_pending.py tests/test_price_intelligence.py -q` — no regression
  </acceptance_criteria>
  <done>ORS matrix and Nominatim helpers exist, ingest pipeline populates commute_minutes end-to-end. 9 of 13 Plan-02 tests green. Endpoint-level tests still red (Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: main.py — GET /api/isochrone + POST /api/refresh-isochrone + POST /api/geocode-backfill</name>
  <files>app/main.py, app/static/isochrone.geojson</files>
  <read_first>
    - app/main.py (existing endpoint patterns: sync get_data line 61, async put_data line 66, background-thread check_now line 75, JSON body parse line 100, HTTPException 404 line 96, StaticFiles mount line 174)
    - .planning/phases/05-map-overview-ui/05-PATTERNS.md section "app/main.py" (all four patterns)
    - .planning/phases/05-map-overview-ui/05-RESEARCH.md section 2 (ORS isochrone request/response) and section 3 (Nominatim)
    - app/ingest_handler.py (post-Task-2: _fetch_commute_minutes, _geocode_with_nominatim)
    - app/data_store.py (post-Plan-01: update_listing_coords, load_app_data)
  </read_first>
  <action>
    Two file edits:

    **File 1: `app/static/isochrone.geojson`** — create this file with placeholder content:
    ```
    {"type": "FeatureCollection", "features": []}
    ```
    Exactly this one-line JSON, no trailing newline mandatory. This ensures GET /api/isochrone never 404s before the first refresh call.

    **File 2: `app/main.py`** — add three new endpoints. All three MUST be declared BEFORE the `app.mount("/", StaticFiles(...))` line (currently line 174; this line must remain last). Add them AFTER the existing `create_draft_endpoint` handler and BEFORE the `# Static frontend last` comment.

    Also add `import json`, `import os`, `import time` if not already imported at the top (currently the file imports `logging`, `threading`, `Optional`, `fastapi`, `fastapi.responses`, `fastapi.security`, `fastapi.staticfiles`, `config`, `data_store`, `gmail_client`, `ingest_handler`, `scheduler`). Add `import requests` for the ORS isochrone call. Preserve grouping.

    **Endpoint 1: `GET /api/isochrone`** — synchronous, no auth (behind Caddy basicauth). Behavior:
    - Read the file at path `os.path.join(os.path.dirname(__file__), "static", "isochrone.geojson")` — use absolute path to avoid cwd surprises.
    - If file missing or JSON parse fails: return `{"type": "FeatureCollection", "features": []}` (log warning once via log.warning, do NOT log the file path repeatedly on each request).
    - Return the parsed JSON content directly (FastAPI auto-serialises).
    - Wrap the file read in try/except Exception; return the empty FeatureCollection dict on any error.
    - Response content-type is left as FastAPI default `application/json` (browsers/Leaflet accept it as valid GeoJSON — no special content-type header needed for JSON-shaped GeoJSON).

    **Endpoint 2: `POST /api/refresh-isochrone`** — synchronous handler that calls ORS then writes the file. Behavior:
    - Check `if not config.ORS_API_KEY: return {"ok": False, "error": "ORS_API_KEY not configured"}` (200 status, structured error — never raise).
    - Build payload: `{"locations": [[config.BOLT_HQ_LNG, config.BOLT_HQ_LAT]], "range": [1200], "range_type": "time"}` with inline comment `# [lon, lat] order per ORS convention; 1200 sec = 20 min per MAP-04`.
    - Headers: `{"Authorization": f"Bearer {config.ORS_API_KEY}", "Content-Type": "application/json"}`.
    - HTTP: `requests.post("https://api.openrouteservice.org/v2/isochrones/driving-car", json=payload, headers=headers, timeout=15)` wrapped in try/except Exception.
    - On success (raise_for_status passes): `geojson = resp.json()`. Write to file: open the absolute path from Endpoint 1 for writing with UTF-8 encoding and `json.dump(geojson, f)`. Return `{"ok": True}`.
    - On failure: log.exception("ORS isochrone refresh failed"), return `{"ok": False, "error": "ORS call failed"}` (200 status; structured error).
    - NEVER log ORS_API_KEY.

    **Endpoint 3: `POST /api/geocode-backfill`** — starts a background thread (matches check_now pattern) so the HTTP response returns immediately. Behavior:
    - Body: no request body needed. Returns `{"ok": True, "message": "Backfill running in background — see logs"}`.
    - The background function (define as a module-level `_run_geocode_backfill()` inside main.py — or import it from a new location; keeping it in main.py is simplest):
      - Acquires `data_store._lock`.
      - Loads app_data.
      - Iterates through `data.get("properties", []) + data.get("pending", [])`.
      - For each entry where `entry.get("lat") is None or entry.get("lng") is None`:
        - Build query from `entry.get("name") or entry.get("title")` — prefer `name` (properties) then fall back to `title` (pending). Append `", Tallinn"` if not already present.
        - If query is empty/None, skip.
        - Call `lat, lng = ingest_handler._geocode_with_nominatim(query)`.
        - If both non-None: also call `commute_mins = ingest_handler._fetch_commute_minutes(lat, lng)` (may be None), then mutate the entry in place: `entry["lat"] = lat; entry["lng"] = lng; entry["commute_minutes"] = commute_mins`.
        - `time.sleep(1.1)` between Nominatim calls (Nominatim usage policy).
      - After the loop: `data_store.save_app_data(data)`.
      - Wrap the whole background function body in try/except Exception with `log.exception(...)`; never propagate exceptions out of the background thread.
    - Endpoint 3 itself just starts the thread: `threading.Thread(target=_run_geocode_backfill, daemon=True).start()`.

    Note: because tests need synchronous behavior to inspect the result, ALSO expose a synchronous test-friendly variant: make `_run_geocode_backfill` a module-level function so tests can call it directly. Additionally, when the caller passes `?sync=1` in the query string, run the function inline (not in a thread) and return `{"ok": True, "geocoded": N, "skipped": M}` where N = count of successful Nominatim hits and M = count of entries that already had coords. Signature: `@app.post("/api/geocode-backfill")` `def geocode_backfill(sync: int = 0)`. If sync=1: call `_run_geocode_backfill()` inline (returns a dict `{"geocoded": N, "skipped": M}`) and return `{"ok": True, **result}`. Otherwise: spawn thread and return `{"ok": True, "message": ...}`.

    Update `_run_geocode_backfill` to return `{"geocoded": N, "skipped": M}` after the loop (so the sync branch returns useful stats). The counter increments each iteration. If NO Nominatim mock is in place and the real Nominatim is called in tests, the test's monkeypatch of `ingest_handler._geocode_with_nominatim` covers this.

    IMPORTANT: All three endpoints declared BEFORE `app.mount("/", StaticFiles(...))` (line 174). Do NOT reorder the mount. Do NOT change any existing endpoint.
  </action>
  <verify>
    <automated>cd app &amp;&amp; python3 -m pytest tests/test_ors_api.py::test_refresh_isochrone_endpoint tests/test_ors_api.py::test_isochrone_get_serves_file tests/test_ors_api.py::test_isochrone_get_missing_file tests/test_commute.py::test_geocode_backfill_endpoint -x -q</automated>
  </verify>
  <acceptance_criteria>
    - File `app/static/isochrone.geojson` exists and contains `{"type": "FeatureCollection", "features": []}` (verify via `python3 -c "import json; d=json.load(open('app/static/isochrone.geojson')); assert d == {'type': 'FeatureCollection', 'features': []}"`)
    - `grep -c '@app.get("/api/isochrone")' app/main.py` returns 1
    - `grep -c '@app.post("/api/refresh-isochrone")' app/main.py` returns 1
    - `grep -c '@app.post("/api/geocode-backfill")' app/main.py` returns 1
    - `grep -c 'https://api.openrouteservice.org/v2/isochrones/driving-car' app/main.py` returns 1
    - `grep -c 'def _run_geocode_backfill' app/main.py` returns 1
    - The `app.mount("/", StaticFiles(...))` line is still the LAST non-blank line in the file — verified by `tail -5 app/main.py | grep -c 'app.mount'` returning 1
    - Zero occurrences of `log\..*ORS_API_KEY` — `grep -c 'log\..*ORS_API_KEY' app/main.py` returns 0
    - All 4 endpoint tests pass in `cd app && python3 -m pytest tests/test_ors_api.py::test_refresh_isochrone_endpoint tests/test_ors_api.py::test_isochrone_get_serves_file tests/test_ors_api.py::test_isochrone_get_missing_file tests/test_commute.py::test_geocode_backfill_endpoint -q`
    - `cd app && python3 -m pytest tests/ -q` — full suite passes
  </acceptance_criteria>
  <done>Three admin endpoints live, placeholder GeoJSON committed, geocode-backfill runs async by default and sync when `?sync=1` for tests. All 13 Plan-02 tests green. Zero regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live smoke test — refresh isochrone + backfill</name>
  <what-built>
    - _fetch_commute_minutes (ORS Matrix) and _geocode_with_nominatim (Nominatim) helpers in ingest_handler.py
    - GET /api/isochrone, POST /api/refresh-isochrone, POST /api/geocode-backfill in main.py
    - Placeholder isochrone.geojson served as a static file
    - New commute_minutes field populated on every incoming pending entry when ORS_API_KEY is set and the listing has coords
  </what-built>
  <how-to-verify>
    Do this manually on the DEPLOYED VPS (46.62.152.9) via the web UI + curl. This is NOT a local check.

    1. Set ORS_API_KEY in .env on the VPS: SSH in, edit /path/to/.env, add `ORS_API_KEY=<your key>`. Restart the app container: `docker-compose restart app`.
    2. Run: `curl -X POST https://<your-vps-domain>/api/refresh-isochrone -u <basicauth-user>:<basicauth-pass>`. Expected response: `{"ok": true}`. If `{"ok": false, "error": "..."}` — check that ORS_API_KEY is correctly loaded (see container env).
    3. Run: `curl https://<your-vps-domain>/api/isochrone -u ...`. Expected: a JSON FeatureCollection with one Feature whose geometry is a Polygon. The coordinates should trace roughly the drivable area within 20 min of Veerenni 28. If you get `{"type": "FeatureCollection", "features": []}` (the placeholder), step 2 did not persist the file — investigate.
    4. Run: `curl -X POST 'https://<your-vps-domain>/api/geocode-backfill?sync=1' -u ...` (note the sync=1 query param so you see stats inline). Expected: `{"ok": true, "geocoded": N, "skipped": M}` where N + M = total pending + properties entries with a name/title.
    5. Verify by fetching `curl https://<your-vps-domain>/api/data -u ... | jq '.pending[0] | {id, lat, lng, commute_minutes}'`. Expected: real float values for lat/lng and an integer for commute_minutes (or null if the ORS matrix call failed for that specific entry).
    6. Trigger a fresh scrape: `curl -X POST https://<your-vps-domain>/api/check-now -u ...`. Wait ~2 minutes. Check the Telegram notification for a new pending listing OR check `/api/data` for a fresh pending entry. Expected: new entries should have commute_minutes populated automatically (assuming the mini PC scraper's Listing dataclass also has lat/lng from Plan 01 — this requires the scraper-client copy of kv_listing_parser.py to be updated in parallel; see the "IMPORTANT" note below).

    IMPORTANT: Per Phase 1 D-05, `kv_listing_parser.py` is byte-identical between VPS and the mini PC scraper-client repo. Plan 01 modified the VPS copy. Before step 6 you must ALSO push the same change (Listing dataclass + _extract_coords + regexes) to the scraper-client repo and rebuild the scraper's Docker image. If you skipped the scraper-client update, incoming listings will not have lat/lng and step 6 will show commute_minutes as null. In that case, either update the scraper-client (recommended) or rely on the geocode-backfill endpoint for coord population (fallback per D-04).
  </how-to-verify>
  <resume-signal>Type "approved" once /api/refresh-isochrone returns ok, /api/isochrone returns a real polygon, /api/geocode-backfill?sync=1 reports geocoded > 0, and at least one pending entry in /api/data has commute_minutes set. Type "skip" if ORS_API_KEY is not yet available and you want to defer live smoke — Plan 04 will still function with empty data but will show no isochrone and no commute badges.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VPS backend → OpenRouteService API | Outbound HTTPS with ORS_API_KEY bearer token in header; response is untrusted JSON |
| VPS backend → Nominatim API | Outbound HTTPS, no auth, response is untrusted JSON |
| Client → `/api/refresh-isochrone` and `/api/geocode-backfill` | Both admin endpoints protected only by Caddy basicauth (matches other admin endpoints); no per-request auth check inside FastAPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Info Disclosure | ORS_API_KEY in logs | high | mitigate | Never-log convention. Task 2 and Task 3 acceptance criteria include `grep -c 'log\..*ORS_API_KEY' == 0` gates. No log statement anywhere in ingest_handler.py or main.py references config.ORS_API_KEY. Error messages return generic "ORS call failed" strings. |
| T-05-12 | Tampering | Malformed ORS JSON response (missing durations key) | medium | mitigate | try/except Exception around resp.json() and durations access in _fetch_commute_minutes returns None on any KeyError/TypeError. Isochrone endpoint's parse is guarded by try/except returning generic error. |
| T-05-13 | DoS | /api/geocode-backfill run without sync=1 by attacker (spawns thread each call) | low | accept | Behind Caddy basicauth. Multiple concurrent backfills would all acquire data_store._lock serially — no race. Slight CPU/Nominatim rate waste per call but bounded by 1.1s sleep. Acceptable per single-user scale. |
| T-05-14 | SSRF | /api/geocode-backfill URL construction from listing name | medium | mitigate | `_geocode_with_nominatim` builds the URL from a fixed base + `params={"q": query}` — requests library URL-encodes the query string. No user-controlled URL scheme/host possible. |
| T-05-15 | Tampering | Malformed GeoJSON written to isochrone.geojson | low | mitigate | POST /api/refresh-isochrone writes the exact ORS response. If ORS returns malformed data, the file is malformed BUT the frontend consumer in Plan 04 must catch the parse error and fall back to no-overlay (RESEARCH Pitfall 6). Backend does not double-validate ORS response shape — this is a WAF-level control on ORS side. |
| T-05-16 | Rate limit violation | Nominatim policy (1 req/sec) | medium | mitigate | Backfill endpoint sleeps 1.1 sec between calls (see `time.sleep(1.1)`). No batching. User-Agent header is required per policy and is included: `ApartsLooker/1.0 daniel.tjulinov@gmail.com` (RESEARCH Pitfall 4). |
</threat_model>

<verification>
1. All 13 tests in test_ors_api.py + test_commute.py pass: `cd app && python3 -m pytest tests/test_ors_api.py tests/test_commute.py -v`
2. Full test suite passes: `cd app && python3 -m pytest tests/ -q`
3. Zero log-leak of ORS_API_KEY: `grep -rn 'log\..*ORS_API_KEY' app/ingest_handler.py app/main.py` returns 0 matches
4. Endpoint registration ordering intact: `grep -n '@app\.\(get\|post\|put\)' app/main.py | tail -5` — the last `@app.*` decorator is above the `app.mount(...)` line
5. Live smoke (Task 4 checkpoint) confirms refresh + backfill + a fresh ingest produces commute_minutes
</verification>

<success_criteria>
- Every acceptance criterion in Tasks 1, 2, 3 met
- Task 4 checkpoint approved (or skipped with explicit user note)
- app/static/isochrone.geojson committed to git with placeholder content
- Plan 03 can begin: it will fetch /api/isochrone and consume commute_minutes on entries via the existing /api/data endpoint
</success_criteria>

<artifacts_this_phase_produces>
| Symbol | Kind | Consumer |
|--------|------|----------|
| `ingest_handler._fetch_commute_minutes(lat, lng)` | private helper | ingest_handler.process_ingest_batch, main._run_geocode_backfill |
| `ingest_handler._geocode_with_nominatim(query)` | private helper | main._run_geocode_backfill |
| `main._run_geocode_backfill()` | module function (background-thread target + sync path) | POST /api/geocode-backfill |
| `GET /api/isochrone` | HTTP endpoint | Plan 04 frontend fetch for Leaflet GeoJSON overlay |
| `POST /api/refresh-isochrone` | HTTP endpoint | Manual admin operation (curl or button — Plan 04 may add a UI button) |
| `POST /api/geocode-backfill` | HTTP endpoint | Manual admin operation (one-time for existing listings without coords) |
| `app/static/isochrone.geojson` | static file | GET /api/isochrone and (indirectly) Leaflet frontend |
| `commute_minutes` populated on every new pending entry | JSON field | Plan 04 detail panel commute badge |
| `commute_minutes` populated on backfilled entries | JSON field | Plan 04 map + detail panel |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-map-overview-ui/05-02-SUMMARY.md` when done. Include: (a) whether Task 4 live smoke was completed on VPS, (b) commute_minutes hit rate — how many of the ~20 existing listings ended up with a non-null commute_minutes after geocode-backfill?, (c) any ORS quota concerns observed.
</output>
