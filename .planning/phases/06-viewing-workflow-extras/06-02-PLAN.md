---
phase: 06-viewing-workflow-extras
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - app/main.py
  - app/static/index.html
  - app/tests/test_viewing_workflow.py
autonomous: true
requirements: [VIEW-01, VIEW-02]
must_haves:
  truths:
    - "POST /api/entry/{id}/schedule-viewing accepts JSON body {scheduled_at: <UTC ISO>} and sets entry.status='viewing_scheduled' (D-01, D-10, VIEW-01)"
    - "POST /api/entry/{id}/schedule-viewing returns 400 for malformed ISO strings; 404 if entry not in properties[] (D-10)"
    - "POST /api/entry/{id}/mark-viewed sets entry.status='viewed' when transitioning from viewing_scheduled (D-03, D-09, VIEW-02)"
    - "Detail-panel shows 'Schedule viewing' button when entry.status=='approved' (D-10)"
    - "Detail-panel shows 'Mark viewed' button when entry.status=='viewing_scheduled' AND now >= entry.scheduled_at (D-09, D-10)"
    - "Datetime picker is native <input type='datetime-local'> with default value today at 17:00 local time (D-10, Specific Ideas)"
    - "Browser converts datetime-local to UTC ISO via new Date(input.value).toISOString() before POST (Pitfall 1)"
    - "VIEW-02 is satisfied by the 'Mark viewed' state transition + the pre-existing web-UI FULL_CHECKLIST (D-08); no Telegram post-viewing dialog"
    - "After 'Mark viewed', the existing FULL_CHECKLIST UI on the detail panel is accessible for Daniel to fill (D-09)"
  artifacts:
    - "app/main.py has new POST /api/entry/{listing_id}/schedule-viewing endpoint"
    - "app/main.py has new POST /api/entry/{listing_id}/mark-viewed endpoint"
    - "app/main.py has helper _find_entry_any(app_data, listing_id) if not already present"
    - "app/static/index.html has 'Schedule viewing' and 'Mark viewed' buttons in the detail-panel action row"
    - "app/static/index.html has scheduleViewingClick(id) and markViewedClick(id) JS handlers"
    - "app/tests/test_viewing_workflow.py has 4 GREEN tests: test_schedule_viewing_sets_status, test_invalid_iso_returns_400, test_z_suffix_parses, test_mark_viewed_flips_status"
  key_links:
    - "Frontend 'Schedule viewing' click -> POST /api/entry/{id}/schedule-viewing -> data_store.set_viewing_scheduled (from Plan 06-01)"
    - "Frontend 'Mark viewed' click -> POST /api/entry/{id}/mark-viewed -> data_store.mark_viewed (from Plan 06-01)"
    - "Detail-panel button visibility conditional on entry.status (state -> UI affordance)"
    - "This is the VIEW-01 + VIEW-02 vertical slice: after this plan Daniel can schedule + mark viewings entirely from the web UI"
---

<objective>
VIEW-01 + VIEW-02 vertical slice. After this plan, Daniel can (a) click "Schedule viewing" on an approved listing, pick a datetime, and see the listing transition to `status="viewing_scheduled"`; (b) after the scheduled time passes, click "Mark viewed" and see the listing transition to `status="viewed"`, at which point the existing FULL_CHECKLIST UI on the detail panel is his post-viewing form (VIEW-02 per D-08).

Purpose: complete the state-machine round-trip end-to-end. No AI, no external HTTP — pure state transitions wired frontend↔backend. This is the simplest slice that delivers observable user value.

Output:
- Two new FastAPI POST endpoints on `main.py`.
- Two new buttons + click handlers on the detail panel.
- 4 GREEN integration tests in `test_viewing_workflow.py` replacing the Plan 06-01 skips.

Artifacts this phase produces:
- FastAPI endpoint `POST /api/entry/{listing_id}/schedule-viewing` accepting `{scheduled_at: str}` body.
- FastAPI endpoint `POST /api/entry/{listing_id}/mark-viewed` (empty body).
- JS function `scheduleViewingClick(listingId)` (per 06-RESEARCH.md § Verified pattern for the browser-side datetime plumbing).
- JS function `markViewedClick(listingId)`.
- DOM elements: `<button id="schedule-viewing-btn-{id}">`, `<input type="datetime-local" id="scheduled-at-input-{id}">`, `<button id="mark-viewed-btn-{id}">` inside the detail-panel action row for each entry.

VIEW-02 note: Per D-08, VIEW-02 (post-viewing checklist) is reinterpreted as the existing web-UI FULL_CHECKLIST — NO new Telegram code path is added by this plan. The "Mark viewed" transition is the entire new backend surface for VIEW-02.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-viewing-workflow-extras/06-CONTEXT.md
@.planning/phases/06-viewing-workflow-extras/06-RESEARCH.md
@.planning/phases/06-viewing-workflow-extras/06-PATTERNS.md
@.planning/phases/06-viewing-workflow-extras/06-VALIDATION.md
@.planning/phases/06-viewing-workflow-extras/06-01-SUMMARY.md
@.claude/CLAUDE.md
@app/main.py
@app/data_store.py
@app/static/index.html
@app/tests/conftest.py
@app/tests/test_pending.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add POST /api/entry/{id}/schedule-viewing and /mark-viewed endpoints to main.py</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py — existing `POST /api/pending/{listing_id}/approve` (main.py:96-102) and `POST /api/pending/{listing_id}/reject` (main.py:104-113) are the exact canonical shape per 06-PATTERNS.md § app/main.py MOD § Primary analog.
    - app/main.py — existing `POST /api/check-now` (main.py:79-83) — daemon-thread spawn pattern (will be used by Plan 06-03 for brief generation; this plan's endpoints do NOT need a background thread, they are pure state flips).
    - 06-PATTERNS.md § app/main.py (MOD, 4 endpoints) — endpoint-by-endpoint pattern assignment table.
    - 06-RESEARCH.md § Pattern 1: Cloning the pending-endpoint shape for state transitions — full draft including the ISO parse guard.
    - 06-RESEARCH.md § Pitfall 1 — datetime-local produces a naive string; backend must accept UTC ISO (browser sends `.toISOString()`) and reject anything datetime.fromisoformat cannot decode.
    - 06-CONTEXT.md § D-10 — POST endpoint semantics and D-09 — Mark viewed availability rules.
  </read_first>
  <acceptance_criteria>
    - `pytest app/tests/test_viewing_workflow.py::test_schedule_viewing_sets_status -x` passes GREEN
    - `pytest app/tests/test_viewing_workflow.py::test_invalid_iso_returns_400 -x` passes GREEN
    - `pytest app/tests/test_viewing_workflow.py::test_z_suffix_parses -x` passes GREEN
    - `pytest app/tests/test_viewing_workflow.py::test_mark_viewed_flips_status -x` passes GREEN
    - `curl -X POST http://localhost:8000/api/entry/nonexistent-id/schedule-viewing -d '{"scheduled_at": "2026-08-01T15:00:00Z"}' -H 'content-type: application/json'` returns 404 (post-boot behaviour).
    - `curl -X POST http://localhost:8000/api/entry/any-id/schedule-viewing -d '{"scheduled_at": "not-an-iso"}' -H 'content-type: application/json'` returns 400.
  </acceptance_criteria>
  <action>
    In `app/main.py`, after the existing `POST /api/pending/{listing_id}/reject` handler (around main.py:113), append two new endpoints. Both follow the shape from 06-PATTERNS.md § app/main.py MOD § Primary analog (reject_pending) and use `HTTPException` for error paths.

    (a) `POST /api/entry/{listing_id}/schedule-viewing`:
        - Async handler (`async def schedule_viewing(listing_id: str, request: Request)`).
        - Parses body: `body = await request.json()`. Validates: `if not isinstance(body, dict): raise HTTPException(400, "Body must be JSON object")` (mirrors cost_override pattern per 06-PATTERNS.md).
        - Extracts `scheduled_at = body.get("scheduled_at", "")`.
        - ISO parse guard (Pitfall 1): try `datetime.fromisoformat(scheduled_at.replace("Z", "+00:00"))`. On ValueError or AttributeError raise `HTTPException(400, "Invalid scheduled_at ISO string")`.
        - Calls `data_store.set_viewing_scheduled(listing_id, scheduled_at)` (from Plan 06-01).
        - If returns False raise `HTTPException(404, "Listing not found in properties")`.
        - Returns `{"ok": True, "message": "Scheduled"}`.
        - Do NOT spawn a background thread in this plan — Plan 06-03 will add the brief-generation thread. Leave a `# Plan 06-03 will wire brief_generator here` comment marker to make the extension point explicit.

    (b) `POST /api/entry/{listing_id}/mark-viewed`:
        - Async handler (`async def mark_viewed_endpoint(listing_id: str)`). No body required.
        - Calls `data_store.mark_viewed(listing_id)` (from Plan 06-01).
        - If returns False raise `HTTPException(400, "Listing not found or not in viewing_scheduled state")` — the helper returns False on both "not found" AND "wrong current status" per D-03 guard.
        - Returns `{"ok": True, "message": "Marked as viewed"}`.

    Import `from datetime import datetime` at the top of main.py if not already present. Do NOT touch existing endpoints.
  </action>
  <verify>
    <automated>cd /Users/danieltjulinov/PycharmProjects/aparts-looker && python -c "from fastapi.testclient import TestClient; import sys; sys.path.insert(0, 'app'); import main; c = TestClient(main.app); r = c.post('/api/entry/nonexistent/schedule-viewing', json={'scheduled_at': '2026-08-01T15:00:00Z'}); assert r.status_code == 404, f'expected 404 got {r.status_code}'; r2 = c.post('/api/entry/nonexistent/schedule-viewing', json={'scheduled_at': 'bad-iso'}); assert r2.status_code == 400, f'expected 400 got {r2.status_code}'; r3 = c.post('/api/entry/nonexistent/mark-viewed'); assert r3.status_code == 400, f'expected 400 got {r3.status_code}'; print('OK')"</automated>
  </verify>
  <done>
    Two new FastAPI endpoints registered with the shape from 06-RESEARCH.md § Pattern 1. Both return the standard {"ok": True, ...} envelope on success, 404/400 on failure paths per D-10.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Schedule/Mark-viewed buttons + datetime picker to the detail panel in index.html</name>
  <files>app/static/index.html</files>
  <read_first>
    - app/static/index.html — inspect the detail-panel action row shape (where existing "Approve"/"Reject"/"Draft email" buttons live for pending entries). The Phase 5 redesign may or may not be merged; if merged, model after `_buildCostOfOwnership`'s Edit UI (per 06-PATTERNS.md § detail-panel.js MOD).
    - 06-PATTERNS.md § app/static/index.html (MOD, CSS) — CSS class conventions (`.coo-card` -> `.brief-card`, `.ku-card`) using `--text`, `--text-muted`, `--font-mono` variables.
    - 06-RESEARCH.md § Verified pattern for the browser-side datetime plumbing — full `scheduleViewingClick(listingId)` JS draft, executor should copy verbatim.
    - 06-CONTEXT.md § D-09, D-10 — button placement and visibility rules.
    - 06-RESEARCH.md § Specific Ideas — "Mark viewed" appearance: "enabled once scheduled_at is in the past; before that time it's greyed out with a tooltip: 'Available from {scheduled_at}'". Default picker value: today at 17:00 local.
    - .claude/CLAUDE.md § Conventions — vanilla JS + CSS only, textContent for all DOM writes, no innerHTML for AI/user data (XSS safety).
  </read_first>
  <acceptance_criteria>
    - `grep -c 'scheduleViewingClick' app/static/index.html` returns >= 2 (function definition + invocation).
    - `grep -c 'markViewedClick' app/static/index.html` returns >= 2.
    - `grep -c 'type="datetime-local"' app/static/index.html` returns >= 1.
    - Manual browser check (added to phase-level manual-verification queue): scheduling a viewing shows an inline datetime picker; submitting POSTs UTC ISO; reload shows the "Mark viewed" button greyed out until scheduled time passes.
  </acceptance_criteria>
  <action>
    In `app/static/index.html`, inside the detail-panel rendering JS block (find the existing action row where Approve/Reject buttons are rendered for pending entries, or where the top of the detail-panel main pane begins for approved entries), add:

    (a) NEW HTML/JS: a per-entry action row for approved entries. Rendered conditionally on `entry.status || "approved"` (Pitfall 6 default per 06-RESEARCH.md § Sidebar glyph decision):

        - If `entry.status === "approved"`: render a "Schedule viewing" button that on click reveals an inline `<input type="datetime-local">` with default value = today at 17:00 local (compute in JS: `var d = new Date(); d.setHours(17, 0, 0, 0); input.value = d.toISOString().slice(0, 16);`) and a "Confirm" button that calls `scheduleViewingClick(entry.id)`.

        - If `entry.status === "viewing_scheduled"`: render a "Mark viewed" button. Disabled + greyed out if `now < new Date(entry.scheduled_at)`. Tooltip when disabled: `"Available from " + new Date(entry.scheduled_at).toLocaleString()` per 06-RESEARCH.md § Specific Ideas.

        - If `entry.status === "viewed"`: render a static "Viewed on {date}" label (no button); the existing FULL_CHECKLIST UI (which is already accessible on the same detail panel per D-09) is the entire post-viewing surface — DO NOT add any new checklist plumbing.

    (b) NEW JS functions at global scope (or attached to `window` per existing convention):

        - `scheduleViewingClick(listingId)` — implementation per 06-RESEARCH.md § Verified pattern for the browser-side datetime plumbing (COPY VERBATIM). Reads `#scheduled-at-input-<listingId>` value, converts via `new Date(input.value).toISOString()`, POSTs `{scheduled_at: utcIso}` to `/api/entry/{id}/schedule-viewing`. On `.ok=true` calls existing `loadData()` (or the equivalent state-refresh function on this codebase) to re-render.

        - `markViewedClick(listingId)` — POSTs empty body to `/api/entry/{id}/mark-viewed`. On success calls `loadData()`. On failure surface an error toast via existing `window.showToast` if defined (else `console.error`).

    (c) CSS (only if needed): a small `.viewing-action-row` block mirroring the existing button-row class. Use existing CSS custom properties (`--text`, `--text-muted`, `--brand`, `--danger` — grep the file for currently-defined variables and reuse). No new colours.

    Idempotence: if the executor finds the detail-panel structure has moved to `app/static/js/detail-panel.js` post-Plan-05-04, apply the same changes to the appropriate file. The action's semantics are "add these buttons + handlers to whatever file currently renders the detail panel". Do NOT split the change across index.html + detail-panel.js unnecessarily — pick the file that currently owns the detail-panel render code.
  </action>
  <verify>
    <automated>cd /Users/danieltjulinov/PycharmProjects/aparts-looker && (grep -cE 'scheduleViewingClick|markViewedClick' app/static/index.html app/static/js/detail-panel.js 2>/dev/null | awk -F: '{s+=$2} END {if (s>=4) print "OK"; else {print "FAIL count="s; exit 1}}')</automated>
  </verify>
  <done>
    Two new buttons ("Schedule viewing" + "Mark viewed") with correct visibility rules per D-10/D-09, native datetime-local picker, and two new global JS click handlers exist. Handlers POST correctly-shaped JSON to the two endpoints from Task 1 and refresh the UI on success.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fill 4 test_viewing_workflow.py cases + add setdefault-driven integration test</name>
  <files>app/tests/test_viewing_workflow.py</files>
  <read_first>
    - app/tests/test_viewing_workflow.py — the skeleton file from Plan 06-01 with the four `pytest.skip(...)` bodies to replace.
    - app/tests/test_pending.py — existing `test_ingest_writes_to_pending` (test_pending.py:6-53) is the canonical shape per 06-PATTERNS.md § app/tests/test_viewing_workflow.py NEW; existing `test_send_pending_card_buttons` (test_pending.py:70-100) shows how to mock requests.post with MagicMock.
    - app/tests/conftest.py — `client` (FastAPI TestClient), `tmp_agent_state` (temp app_data.json + agent_state.json paths) fixtures.
    - 06-VALIDATION.md § Per-Task Verification Map — exact expected behaviour per test row.
  </read_first>
  <acceptance_criteria>
    - `pytest app/tests/test_viewing_workflow.py::test_schedule_viewing_sets_status -x` passes GREEN
    - `pytest app/tests/test_viewing_workflow.py::test_invalid_iso_returns_400 -x` passes GREEN
    - `pytest app/tests/test_viewing_workflow.py::test_z_suffix_parses -x` passes GREEN
    - `pytest app/tests/test_viewing_workflow.py::test_mark_viewed_flips_status -x` passes GREEN
    - No test in the file is skipped (except tests belonging to Plans 06-03/06-04 which are still `pytest.skip("Filled by Plan 06-03")` / `pytest.skip("Filled by Plan 06-04")`).
  </acceptance_criteria>
  <action>
    Replace the four `pytest.skip(...)` bodies in `app/tests/test_viewing_workflow.py` with real implementations:

    (a) `test_schedule_viewing_sets_status(client, tmp_agent_state, monkeypatch)`:
        - Import `data_store`.
        - Seed properties[]: use `data_store.save_app_data({"properties": [{"id": "abc", "price": 100000}], ...})` (or directly write JSON) — set the entry's status to "approved" implicitly via load_app_data setdefault after seeding.
        - POST to `/api/entry/abc/schedule-viewing` with `json={"scheduled_at": "2026-08-15T15:00:00+00:00"}`.
        - Assert `resp.status_code == 200`.
        - `data = data_store.load_app_data()` and assert `data["properties"][0]["status"] == "viewing_scheduled"` and `data["properties"][0]["scheduled_at"] == "2026-08-15T15:00:00+00:00"`.

    (b) `test_invalid_iso_returns_400(client, tmp_agent_state)`:
        - Seed a properties[] entry (same as above).
        - POST with `json={"scheduled_at": "not-an-iso-string"}` and assert `resp.status_code == 400`.
        - Assert entry status is still "approved" (unchanged).

    (c) `test_z_suffix_parses(client, tmp_agent_state)`:
        - Seed a properties[] entry.
        - POST with `json={"scheduled_at": "2026-08-15T15:00:00Z"}` (Z suffix — needs the `.replace("Z", "+00:00")` path exercised).
        - Assert `resp.status_code == 200` and `data["properties"][0]["scheduled_at"] == "2026-08-15T15:00:00Z"` (stored verbatim; the endpoint validates but doesn't rewrite).

    (d) `test_mark_viewed_flips_status(client, tmp_agent_state)`:
        - Seed a properties[] entry with `status="viewing_scheduled"` and `scheduled_at="2026-01-01T00:00:00+00:00"` (past).
        - POST to `/api/entry/abc/mark-viewed`.
        - Assert `resp.status_code == 200`, `data["properties"][0]["status"] == "viewed"`, and viewing_history has an entry with `action == "viewed"`.
        - Also assert: seed a fresh entry with `status="approved"`, POST mark-viewed, expect `resp.status_code == 400` (invalid transition per D-03).

    Do NOT touch the other two skips (`test_regenerate_brief`, `test_refresh_ku`) — those belong to Plans 06-03 and 06-04.
  </action>
  <verify>
    <automated>cd /Users/danieltjulinov/PycharmProjects/aparts-looker && python -m pytest app/tests/test_viewing_workflow.py::test_schedule_viewing_sets_status app/tests/test_viewing_workflow.py::test_invalid_iso_returns_400 app/tests/test_viewing_workflow.py::test_z_suffix_parses app/tests/test_viewing_workflow.py::test_mark_viewed_flips_status -x -q</automated>
  </verify>
  <done>
    Four tests pass GREEN. `pytest app/tests/test_viewing_workflow.py -q` shows 4 passed + 2 skipped (the remaining Plan 06-03/06-04 skips).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->API | User-submitted `scheduled_at` string (must be validated per V5 Input Validation) |
| API->persistence | State-transition writes flow through data_store._lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | scheduled_at input | medium | mitigate | datetime.fromisoformat guard rejects malformed input with HTTP 400 (V5 ASVS); tested by test_invalid_iso_returns_400. |
| T-06-04 | Denial of Service | mark-viewed brute-force loops | low | accept | State-transition helper returns False cheaply; caller is single-user behind basic auth. |
| T-06-05 | Repudiation | state transitions unaudited | low | mitigate | viewing_history[] append-only log records every scheduled + viewed transition with UTC timestamps (D-04). |
</threat_model>

<verification>
- `pytest app/tests/test_viewing_workflow.py -q` shows 4 passed, 2 skipped.
- `pytest app/tests -x -q` shows no regressions in prior test files.
- Manual browser: on a properties[] entry, "Schedule viewing" button appears; picker defaults to today 17:00 local; submit POSTs UTC ISO; sidebar refresh shows the entry now in viewing_scheduled state; "Mark viewed" button appears greyed until scheduled_at is past, then clickable.
</verification>

<success_criteria>
- Two new FastAPI endpoints on main.py, both following the reject_pending shape.
- Frontend has visible Schedule/Mark-viewed buttons with correct visibility.
- Native `datetime-local` picker in use (no new deps).
- All four Plan 06-02 tests in test_viewing_workflow.py pass green.
- No regressions in the existing test suite.
</success_criteria>

<output>
Create `.planning/phases/06-viewing-workflow-extras/06-02-SUMMARY.md` when done.
</output>
