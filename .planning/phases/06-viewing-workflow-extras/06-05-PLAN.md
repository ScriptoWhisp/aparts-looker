---
phase: 06-viewing-workflow-extras
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified:
  - app/static/index.html
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VIEW-01, VIEW-02, VIEW-03, ENRICH-01, EXPORT-01]
must_haves:
  truths:
    - "Sidebar item shows a status glyph — 📅 for viewing_scheduled, ✓ for viewed, blank for approved (Specific Ideas + Pitfall 6 default)"
    - "Sidebar glyph is XSS-safe: added via textContent, not innerHTML"
    - "Removed listings (INTEL-03 raw_ok=False) still show the removed indicator regardless of status — removed wins over viewed glyph (Open Question 4)"
    - "EXPORT-01 is documented as descoped in ROADMAP.md and REQUIREMENTS.md, with a link back to 06-CONTEXT.md § Deferred Ideas explaining rationale (D-14)"
    - "Full pytest suite (pytest app/tests) passes GREEN with no skips remaining in the three new Phase 6 test files"
    - "All five phase requirement IDs have documented Wave 4 status in the traceability table: VIEW-01/02/03/ENRICH-01 COMPLETE, EXPORT-01 DEFERRED"
  artifacts:
    - "app/static/index.html — sidebar-item status glyph rendering via statusGlyph(entry) helper"
    - ".planning/ROADMAP.md — Phase 6 Success Criteria row 5 (PDF export) marked DEFERRED with rationale link"
    - ".planning/REQUIREMENTS.md — EXPORT-01 marked as Deferred with source-of-deferral note"
    - "Test suite green: pytest app/tests -q shows zero skips in Phase 6 test files"
  key_links:
    - "sidebar-item render (existing) -> statusGlyph(entry) -> textContent-only DOM write"
    - "EXPORT-01 deferral traceability: REQUIREMENTS.md -> ROADMAP.md -> 06-CONTEXT.md § Deferred Ideas -> D-14"
    - "This plan is the polish + governance close-out of Phase 6"
---

<objective>
Wave 4 polish + governance. Adds the last piece of user-facing UI (sidebar status glyphs so Daniel can skim the sidebar and see which listings have viewings scheduled), formally deferrs EXPORT-01 with proper roadmap + requirements traceability, and confirms the full test suite is green.

Purpose: (a) make status changes visible from the sidebar so Daniel doesn't have to open every listing to remember which ones are scheduled; (b) close the requirements audit by moving EXPORT-01 from "Pending in Phase 6" to "Deferred" with an explicit rationale linked to the discussion decision D-14; (c) confirm no regressions across the whole suite before phase sign-off.

Output:
- Sidebar-item status glyph via new `statusGlyph(entry)` helper.
- ROADMAP.md updated with EXPORT-01 deferral note.
- REQUIREMENTS.md traceability row for EXPORT-01 updated to Deferred.
- Verified full test suite green.

Artifacts this phase produces:
- JS function `statusGlyph(entry)` (per 06-RESEARCH.md § Verified pattern for the browser-side datetime plumbing lines 991-997).
- Sidebar-item render updated to append glyph to the meta line.
- ROADMAP.md § Phase 6 § Success Criteria — row 5 (PDF export) marked with deferral rationale.
- REQUIREMENTS.md § Traceability table — EXPORT-01 status flipped to "Deferred (post-Phase-6)".

EXPORT-01 is intentionally listed in this plan's `requirements` frontmatter along with the other four IDs so the requirements-coverage gate sees it — but the plan's job for EXPORT-01 is DEFERRAL, not implementation. This is documented explicitly per the planning-context prompt: "Add EXPORT-01 to the `<deferred>` block in one of the PLAN.md files (or a stand-alone note in the phase directory) with the rationale."
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-viewing-workflow-extras/06-CONTEXT.md
@.planning/phases/06-viewing-workflow-extras/06-RESEARCH.md
@.planning/phases/06-viewing-workflow-extras/06-PATTERNS.md
@.planning/phases/06-viewing-workflow-extras/06-VALIDATION.md
@.planning/phases/06-viewing-workflow-extras/06-01-SUMMARY.md
@.planning/phases/06-viewing-workflow-extras/06-02-SUMMARY.md
@.planning/phases/06-viewing-workflow-extras/06-03-SUMMARY.md
@.planning/phases/06-viewing-workflow-extras/06-04-SUMMARY.md
@.claude/CLAUDE.md
@app/static/index.html
</context>

<deferred>
## Deferred: EXPORT-01 (PDF export)

Per D-14 in `.planning/phases/06-viewing-workflow-extras/06-CONTEXT.md § Deferred Ideas`, EXPORT-01 (Dossier PDF export) was dropped from Phase 6 during the discuss-phase conversation. Rationale from CONTEXT.md:

> Dropped from Phase 6 in discussion. Reconsider later if Daniel needs to hand documents to a bank / advisor. If revisited: single-listing dossier is the highest-value scope (title + score + verdict + cost card + price history + checklist snapshot + brief).

This plan documents the deferral in ROADMAP.md and REQUIREMENTS.md so the traceability tables stay accurate. No implementation code is written for EXPORT-01.

Follow-up: if Daniel decides to revisit PDF export post-Phase-6, add a new phase (Phase 6.1 or v2) with a single plan targeting per-listing PDF snapshot (WeasyPrint or ReportLab, ~200-line Python module + a single new GET endpoint).
</deferred>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebar-item status glyph rendering</name>
  <files>app/static/index.html</files>
  <read_first>
    - app/static/index.html — find the sidebar-item render function (grep for `sidebar` or `si-` prefix — patterns section 06-PATTERNS.md § app/static/js/detail-panel.js MOD references `_buildSidebarItem` at detail-panel.js:120-129, but on the current pre-Plan-05-04 codebase this lives in index.html; grep for the actual location).
    - 06-RESEARCH.md § Verified pattern for the browser-side datetime plumbing lines 991-997 — the exact `statusGlyph(entry)` function to copy verbatim.
    - 06-RESEARCH.md § Pitfall 6 — default `entry.status || "approved"`; pending[] entries do not have viewing state and should receive no glyph.
    - 06-RESEARCH.md § Open Question 4 — precedence: `removed` (INTEL-03) wins over `viewed` — if `entry.removed` is truthy, no viewing glyph is added (an existing removed indicator already covers this case).
    - .claude/CLAUDE.md § Conventions — textContent only, no innerHTML for DOM writes.
    - 06-CONTEXT.md § Specifics — sidebar glyph icons: 📅 for viewing_scheduled, ✓ for viewed.
  </read_first>
  <acceptance_criteria>
    - `grep -c 'statusGlyph' app/static/index.html` returns >= 2 (function definition + invocation site).
    - `grep -c 'viewing_scheduled' app/static/index.html` returns >= 1 (used in statusGlyph).
    - Sidebar test (manual browser check): a listing in viewing_scheduled state shows a 📅 in the sidebar meta line; a viewed one shows ✓; a plain approved one shows nothing; a removed one shows only the existing removed indicator (no viewing glyph).
    - No `innerHTML` usage added — all glyph writes use `textContent` or `appendChild`.
  </acceptance_criteria>
  <action>
    In `app/static/index.html`, add a new global JS function `statusGlyph(entry)` at the same scope as other sidebar-related helpers. Implementation (per 06-RESEARCH.md verbatim, with the removed-precedence guard added per Open Question 4):

    - `var status = entry.status || "approved";` (Pitfall 6 default).
    - If `entry.removed`: return "" (existing removed indicator wins per Open Question 4 — do NOT overlay a viewed checkmark on a sold-and-removed listing).
    - If `status === "viewing_scheduled"`: return "📅".
    - If `status === "viewed"`: return "✓".
    - Otherwise return "".

    Locate the sidebar-item render function (grep for the code that builds sidebar list items — likely a function using `document.createElement("li")` or similar, appending price/score/name to a container per existing pattern). Append the glyph to the sidebar item's meta line using textContent: create a `<span class="si-status-glyph">` element, set its `.textContent = statusGlyph(entry)`, and append it AFTER the existing meta content (price/score line).

    Add a small CSS block for `.si-status-glyph { margin-left: 6px; opacity: 0.75; }` in the existing `<style>` block. No colour changes.

    Do NOT modify any pending[]-sidebar rendering — pending entries never have viewing state, and the null-default `entry.status || "approved"` ensures `statusGlyph` returns "" for them.
  </action>
  <verify>
    <automated>cd /Users/danieltjulinov/PycharmProjects/aparts-looker && grep -c 'statusGlyph' app/static/index.html | awk '$0 >= 2 {print "OK"; exit 0} {print "FAIL count="$0; exit 1}'</automated>
  </verify>
  <done>
    Sidebar-item render appends a status glyph via textContent-only. Precedence rules per Open Question 4 respected (removed > viewed > viewing_scheduled > approved).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md and REQUIREMENTS.md — mark EXPORT-01 as Deferred</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 6: Viewing Workflow & Extras — current success criteria row 5 (PDF export).
    - .planning/REQUIREMENTS.md § Traceability — current row for EXPORT-01 (Phase 6 / Pending).
    - .planning/phases/06-viewing-workflow-extras/06-CONTEXT.md § Deferred Ideas — the D-14 rationale to cite.
  </read_first>
  <acceptance_criteria>
    - `grep -c 'EXPORT-01' .planning/REQUIREMENTS.md` shows the row is present (unchanged count).
    - `grep 'EXPORT-01.*Deferred' .planning/REQUIREMENTS.md` matches (row status is Deferred).
    - `grep -c 'DEFERRED\|deferred' .planning/ROADMAP.md` count increases by at least 1 (new marker on Phase 6 row 5).
    - `grep 'PDF' .planning/ROADMAP.md` still finds the row (not deleted, just annotated).
  </acceptance_criteria>
  <action>
    (a) In `.planning/ROADMAP.md`, find the Phase 6 Success Criteria block. Replace the row `5. Daniel can export the full dossier as a PDF suitable for sharing with a bank or advisor.` with:

    `5. ~~Daniel can export the full dossier as a PDF suitable for sharing with a bank or advisor.~~ *(DEFERRED in Phase 6 discussion — see 06-CONTEXT.md § Deferred Ideas, D-14. Track in v2 or a follow-up phase if needed.)*`

    Also update the phase's tentative plan list to remove `06-05: PDF export`. Keep the actual 5 plans (`06-01` through `06-05`) that this planning run produced.

    (b) In `.planning/REQUIREMENTS.md`, find the row `| EXPORT-01 | Phase 6 | Pending |` in the Traceability table. Replace with:

    `| EXPORT-01 | Phase 6 (deferred) | Deferred |`

    Add a footnote or in-place annotation on the EXPORT-01 checkbox row (the `- [ ] **EXPORT-01**: Dossier can be exported as a PDF (for bank/advisor review)` line): add `*(Deferred in Phase 6 discussion — see `.planning/phases/06-viewing-workflow-extras/06-CONTEXT.md` § Deferred Ideas, D-14.)*` on the same line.

    Do NOT delete the requirement row — deferral is a status change, not removal. The row stays discoverable for any future phase that picks it up.
  </action>
  <verify>
    <automated>cd /Users/danieltjulinov/PycharmProjects/aparts-looker && grep -q 'EXPORT-01.*Deferred' .planning/REQUIREMENTS.md && grep -qE 'DEFERRED.*discussion|Deferred in Phase 6' .planning/ROADMAP.md && echo OK || (echo "FAIL: deferral markers missing"; exit 1)</automated>
  </verify>
  <done>
    ROADMAP.md marks the PDF-export success-criteria row as DEFERRED with a link to CONTEXT.md D-14. REQUIREMENTS.md traceability table has EXPORT-01 marked Deferred. Both changes are surgical (Edit-not-Write).
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm full pytest suite is green — Phase 6 acceptance gate</name>
  <files>app/tests/</files>
  <read_first>
    - 06-VALIDATION.md § Sampling Rate — "Before /gsd-verify-work: Full suite must be green".
    - 06-VALIDATION.md § Per-Task Verification Map — check that all 15 rows now map to a GREEN test.
  </read_first>
  <acceptance_criteria>
    - `pytest app/tests -q` shows all tests passed (or clearly-documented pre-existing failures unrelated to Phase 6).
    - Zero tests in `test_viewing_workflow.py`, `test_brief_generator.py`, `test_ku_lookup.py`, or the new cases in `test_data_store.py` are `SKIPPED` or `XFAIL`.
    - `pytest app/tests --collect-only 2>/dev/null | grep -cE 'test_(viewing_workflow|brief_generator|ku_lookup|setdefault_status_legacy|set_viewing_scheduled_missing|reschedule_appends_history|save_ku_preserves_manual|mark_viewed_transitions_from_scheduled)'` returns >= 15.
  </acceptance_criteria>
  <action>
    Run the full test suite from the repository root:

    `cd /Users/danieltjulinov/PycharmProjects/aparts-looker && python -m pytest app/tests -v --tb=short`

    Confirm:
    1. No tests failed.
    2. No tests in the four Phase 6-owned files are skipped or xfailed.
    3. If any tests fail, DO NOT proceed — instead investigate and either (a) fix the fault, (b) file a follow-up task in the summary if it's clearly out-of-scope for Phase 6 acceptance.

    Update the Phase 6 SUMMARY.md (created via <output> below) to include the exact final test count (e.g., "Phase 6 close: 74 + 15 = 89 tests, all green").
  </action>
  <verify>
    <automated>cd /Users/danieltjulinov/PycharmProjects/aparts-looker && python -m pytest app/tests -q --tb=line 2>&1 | tail -3 | grep -E 'passed|failed'</automated>
  </verify>
  <done>
    Full suite is green. Phase 6 acceptance gate passes. Ready for `/gsd-verify-work`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sidebar rendering | User-facing text via textContent only (no innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-14 | Tampering | Sidebar glyph XSS | low | mitigate | statusGlyph returns a fixed set of literal Unicode strings; textContent write; no user input in the glyph value. |
</threat_model>

<verification>
- `pytest app/tests -q` all green.
- ROADMAP.md Phase 6 row 5 shows DEFERRED annotation.
- REQUIREMENTS.md traceability shows EXPORT-01 status = Deferred.
- Manual browser: sidebar items in different states show correct glyphs (📅, ✓, blank).
</verification>

<success_criteria>
- Sidebar rendering surfaces status via statusGlyph().
- EXPORT-01 formally deferred in ROADMAP + REQUIREMENTS with D-14 citation.
- All Phase 6 tests green with zero skips remaining.
- No regressions in prior test files.
</success_criteria>

<output>
Create `.planning/phases/06-viewing-workflow-extras/06-05-SUMMARY.md` when done. Include:
- Final test count (before + after).
- List of Phase 6 sign-off items with checkmarks.
- Confirmation of D-14 deferral traceability.
</output>
