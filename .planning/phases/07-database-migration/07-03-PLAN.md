---
phase: 07-database-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - app/migrate_from_json.py
  - app/entrypoint.sh
  - app/tests/test_migration.py
autonomous: true
requirements:
  - DB-04

must_haves:
  truths:
    - "`python migrate_from_json.py` invoked twice in a row against the same `app_data.json` produces the same DB row count (idempotency via `db.merge` + file-rename guard per RESEARCH § Pitfall 5)"
    - "After successful migration, `app/data/app_data.json` is renamed to `app/data/app_data.json.pre-pg7` (one-week rollback insurance per Claude's discretion cutover strategy)"
    - "Legacy `entry['ku']['manual']` notes survive the migration to the JSONB `ku` column (Pitfall 7 guard)"
    - "Every unmapped key on a legacy entry lands in the `extras` JSONB — no data silently dropped (RESEARCH Assumption A2)"
    - "Wave 0 tests `test_migration.py::test_idempotent_rerun` and `::test_preserves_ku_manual` turn GREEN"
    - "`entrypoint.sh` now invokes `python migrate_from_json.py` after `alembic upgrade head` and before `uvicorn`"
    - "Container start on a fresh volume with legacy `app_data.json` mounted at `/app/data/` produces log lines: `Migrating legacy JSON if present...` → `migrate_from_json: N rows found; inserting into listings table` → `Migrated OK. Renamed app_data.json → app_data.json.pre-pg7` → `Starting uvicorn...`"
  artifacts:
    - "app/migrate_from_json.py — one-shot idempotent JSON→DB loader (~100 lines)"
    - "app/entrypoint.sh (modified) — placeholder line replaced with real `python migrate_from_json.py` invocation"
    - "app/tests/test_migration.py (from Wave 0) — GREEN"
  key_links:
    - "`db.merge()` upserts by primary key — safe to re-run before OR after the file rename (double-guard)"
    - "Rename is `os.replace(SOURCE, BACKUP)` — POSIX-atomic; matches settings_store atomic-write pattern"
    - "`entrypoint.sh` step order — Alembic MUST run before migrate_from_json (schema must exist before data goes in)"
    - "`_LEGACY_ALIASES` from main.py:197 MUST be applied on the read path so legacy `name` / `price` / `pricePerSqm` / `area` / `year` land in the new column names"
---

<objective>
Wave 3 — One-shot idempotent migration script + entrypoint wiring + Wave 0 migration tests turned GREEN.

Purpose: Cut existing production data over from `app_data.json` to Postgres on first deploy of Phase 7. Runs at container start after Alembic, before uvicorn. Safe to re-run — `db.merge()` upserts by primary key; on success, source file is renamed to `.pre-pg7` (one-week rollback insurance per D-Discretion cutover strategy).

Output: A runnable Python module + entrypoint change that flips the cutover for the running production system without downtime beyond the deploy's standard container-restart window. Wave 0 migration tests turn GREEN.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-database-migration/07-CONTEXT.md
@.planning/phases/07-database-migration/07-RESEARCH.md
@.planning/phases/07-database-migration/07-00-SUMMARY.md
@.planning/phases/07-database-migration/07-01-SUMMARY.md
@.planning/phases/07-database-migration/07-02-SUMMARY.md

@app/data_store.py
@app/models.py
@app/entrypoint.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write app/migrate_from_json.py — idempotent one-shot loader</name>
  <files>app/migrate_from_json.py</files>
  <behavior>
    - CLI entry point: `python migrate_from_json.py` (from `app/` dir) — exits 0 on success, 1 on any failure (bad JSON, DB error).
    - Idempotency guard 1: if `config.APP_DATA_FILE` does not exist (already renamed to `.pre-pg7`), log `"No <path> — nothing to migrate (already ran, or fresh install)"` and exit 0.
    - Idempotency guard 2: every insert uses `db.merge(Listing(**fields))` — safe to run before OR after the rename because merge upserts by PK.
    - On successful commit: `os.replace(SOURCE, BACKUP)` where `BACKUP = SOURCE + ".pre-pg7"`. If rename fails (permissions), log and exit 0 anyway (subsequent runs are safe via merge).
    - Never logs secrets: never logs `config.DATABASE_URL`, `POSTGRES_PASSWORD`. Log lines include listing IDs and counts only.
    - Legacy field aliases applied per `_LEGACY_ALIASES` from main.py:197 (name→title, price→price_eur, pricePerSqm→price_per_sqm, area→area_sqm, year→year_built, notes→description or per current app-level mapping — read main.py:197 for the truth).
    - Unknown keys on an entry (any key not in `Listing.__table__.columns`) land in the `extras` JSONB — nothing is silently dropped.
    - Per-listing checklist / price_history from top-level `data['checklists'][listing_id]` and `data['price_history'][listing_id]` land in the corresponding JSONB column on the row.
    - Status column: preserve `entry.get("status")` if present (Phase 6 wrote `status` on properties[] entries); default to "approved" for `properties[]` if not set, "pending" for `pending[]`, "rejected" for `rejected[]`. Clamp `properties[]` `status` to `{"approved","viewing_scheduled","viewed"}` (invalid → "approved").
    - Preserves `ku.manual` — per RESEARCH § Pitfall 7 the migration script must copy `entry["ku"]` as-is (the `ku` JSONB will contain both `auto` and `manual` subkeys).
  </behavior>
  <read_first>
    - .planning/phases/07-database-migration/07-RESEARCH.md § Code Examples § migrate_from_json.py (idempotent one-shot loader) — canonical shape to copy
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 5 (Migration script re-runs and duplicates rows — the multi-guard convention)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 7 (must preserve ku.manual through the migration)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Assumptions Log A2 (unknown keys land in extras — nothing lost)
    - app/data_store.py (from Wave 2 — `_dict_to_listing_fields` is close in spirit but tuned for `save_app_data`; the migration script's `_entry_to_row` should reuse `_LEGACY_ALIASES` logic but land in the merge idiom)
    - app/main.py:197 (_LEGACY_ALIASES — the authoritative map)
    - app/config.py (APP_DATA_FILE constant location + DATA_DIR)
    - app/models.py (Listing column list — every legacy key must be mapped or spilled to `extras`)
  </read_first>
  <action>
    Create `app/migrate_from_json.py` per RESEARCH § Code Examples § migrate_from_json.py verbatim shape.

    Discipline points:
    - Module docstring per CLAUDE.md convention: explain idempotency (two guards), safe re-run semantics, one-week rollback via rename, and that this script runs ONCE per deploy from `entrypoint.sh` (Task 2 wires it).
    - Set up its own logger: `log = logging.getLogger("migrate_from_json")`; `logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(name)s: %(message)s")` — so the container logs show the migration progress with timestamps.
    - Imports: `json`, `logging`, `os`, `sys`, `SQLAlchemyError`, `config`, `db.SessionLocal`, `models.Listing`. NO import of `data_store` (would create a circular / undesirable dependency; the migration owns its own DB writes via `db.merge`).
    - `SOURCE = config.APP_DATA_FILE`; `BACKUP = SOURCE + ".pre-pg7"`.
    - `_entry_to_row(entry: dict, forced_status: str) -> Listing`:
      1. Apply `_LEGACY_ALIASES` (verbatim from main.py:197) — for each `(old, new)` pair, if `old in entry` and `new not in entry` or `not entry.get(new)`, set `entry[new] = entry[old]`. Do NOT delete `old` — keep for the `extras` spill.
      2. `known = {c.name for c in Listing.__table__.columns}`; `fields = {k: v for k, v in entry.items() if k in known}`; `extras = {k: v for k, v in entry.items() if k not in known}`.
      3. `fields.setdefault("id", entry.get("id") or "unknown")` — an entry without id is a data bug; give it a placeholder so the migration does not crash on a bad row.
      4. `fields["status"] = forced_status`.
      5. `fields["extras"] = extras`.
      6. Coerce Nones on JSONB list/dict columns per RESEARCH example (`viewing_history`, `price_history`, `strengths`, `concerns`, `risks` → `[]`; `cost_of_ownership`, `checklist`, `score_breakdown`, `ai_checklist_fills`, `extras` → `{}`). This matches SQLAlchemy JSONB semantics — `None` is a valid JSONB value but the compat shim assumes list/dict.
      7. Return `Listing(**fields)`.
    - `main() -> int`:
      1. If not `os.path.exists(SOURCE)`: log "no file" + return 0.
      2. Read JSON with `try/except (json.JSONDecodeError, OSError)`; log + return 1 on parse failure. Do NOT rename on parse failure (source may still be recoverable).
      3. Extract `properties`, `pending`, `rejected`, `checklists`, `price_history` (each defaulting to `[]` or `{}`).
      4. Log the count of each list.
      5. Within a single `SessionLocal()` context: iterate each list, build rows via `_entry_to_row`, hot-fill `row.checklist = checklists.get(row.id, {}) or {}` and `row.price_history = price_hist.get(row.id, []) or []`, then `db.merge(row)`.
      6. `db.commit()` once at the end.
      7. On success, `os.replace(SOURCE, BACKUP)`. Log the rename. If rename fails (`OSError`): log via `log.exception` and return 0 anyway (subsequent runs are safe via merge — per Pitfall 5 double-guard).
      8. On SQLAlchemyError inside the migration block: log via `log.exception` + return 1 (transaction auto-rolled back by the context manager).
    - `if __name__ == "__main__": sys.exit(main())`.

    For the `properties[]` list, clamp status per Behavior:
    - If `entry.get("status") in ("approved","viewing_scheduled","viewed")` → use as-is.
    - Else → default to `"approved"`.
    For `pending[]` → force `"pending"`.
    For `rejected[]` → force `"rejected"`.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker/app && test -f migrate_from_json.py && python -c 'import migrate_from_json; assert callable(migrate_from_json.main); assert callable(migrate_from_json._entry_to_row)' && grep -q 'db.merge' migrate_from_json.py && grep -q 'os.replace' migrate_from_json.py && grep -q '.pre-pg7' migrate_from_json.py && ! grep -qE 'DATABASE_URL|POSTGRES_PASSWORD' migrate_from_json.py && echo OK"</automated>
  </verify>
  <done>`app/migrate_from_json.py` exists; `main()` is idempotent (guard 1: file existence; guard 2: db.merge upsert; guard 3: rename after success); `_entry_to_row` maps legacy aliases + spills unknown keys to `extras`; no secret ever printed via `grep -qE 'DATABASE_URL|POSTGRES_PASSWORD'`; Wave 0 tests `test_migration.py::test_idempotent_rerun` and `::test_preserves_ku_manual` pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire migrate_from_json.py into entrypoint.sh (replace placeholder line)</name>
  <files>app/entrypoint.sh</files>
  <behavior>
    - `entrypoint.sh` executes in this exact order:
      1. `pg_isready` loop until Postgres accepts connections.
      2. `alembic upgrade head`.
      3. `python migrate_from_json.py` — Wave 3 replaces the Wave 1 placeholder echo.
      4. `exec uvicorn main:app --host 0.0.0.0 --port 8000`.
    - `set -e` remains at top of the script — any failure in steps 1-3 stops the container (container restarts via `restart: unless-stopped`; the failure is visible in `docker compose logs app`).
    - Log lines from the migration script (routed via Python logging) appear inline in `docker compose logs app`.
  </behavior>
  <read_first>
    - app/entrypoint.sh (from Wave 1 — the placeholder echo line to replace)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pattern 5 (Container entrypoint — the exact bash body)
    - .planning/phases/07-database-migration/07-CONTEXT.md § canonical_refs § Deployment (`.github/workflows/deploy.yml` runs `docker compose up -d --build` on push to main — this script MUST survive re-runs, which it does via Task 1's idempotency guards)
  </read_first>
  <action>
    Edit `app/entrypoint.sh` — a two-line change:
    - Replace the placeholder line `echo "Migrating legacy JSON if present (idempotent)..."` with:
      ```
      echo "Migrating legacy JSON if present (idempotent)..."
      python migrate_from_json.py
      ```
    - Do NOT change any other line. Do NOT reorder steps. Do NOT add error-handling around the python invocation — the script exits 0 on no-op / success, 1 on hard failure; `set -e` at the top of entrypoint.sh does the right thing (container restarts on 1).
    - Do NOT invoke the script with a full path — it lives at `/app/migrate_from_json.py` in the container image; the Dockerfile sets `WORKDIR /app`; the plain `python migrate_from_json.py` works.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker && grep -q '^python migrate_from_json.py$' app/entrypoint.sh && grep -q 'alembic upgrade head' app/entrypoint.sh && grep -q 'exec uvicorn' app/entrypoint.sh && grep -c '^echo\\|^python\\|^alembic\\|^exec\\|^set\\|^until' app/entrypoint.sh && grep -q '^WORKDIR /app$' app/Dockerfile && echo OK"</automated>
  </verify>
  <done>`entrypoint.sh` now invokes `python migrate_from_json.py` after `alembic upgrade head` and before `exec uvicorn`; step order unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app container startup → `/app/data/app_data.json` | Docker volume mount; readable/writable by the app process only |
| migrate_from_json.py → Postgres | Same private network as `data_store.py` writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-01 | Tampering | Duplicate rows on migration re-run | high | mitigate | Two guards: (1) file existence check (SOURCE gone after first successful run); (2) `db.merge` upsert by PK (safe even without the rename). RESEARCH § Pitfall 5. Wave 0 test `test_idempotent_rerun` is the regression guard. |
| T-07-03-02 | Tampering | Loss of `ku.manual` notes during migration | high | mitigate | `_entry_to_row` copies `entry["ku"]` as-is (a full dict — includes `manual` subkey if present). Wave 0 test `test_preserves_ku_manual` is the guard. |
| T-07-03-03 | Information Disclosure | Backup file (`app_data.json.pre-pg7`) exposure | low | accept | File stays inside the app container's Docker volume; unreachable from network; same sensitivity as the DB rows themselves. RESEARCH § Security Domain. |
| T-07-03-04 | Denial of Service | Concurrent Alembic + migrate_from_json on multi-pod deploy | low | accept | Only one deploy target; only one app container. Assumption A4 in RESEARCH documents this. Not applicable. |
| T-07-03-05 | Information Disclosure | Migration script logs DATABASE_URL / secrets | high | mitigate | Script never references `DATABASE_URL` or `POSTGRES_PASSWORD` directly; verify greps for these strings. Log lines include listing IDs and counts only. |
| T-07-03-06 | Tampering | Unknown legacy key silently dropped | medium | mitigate | Every unmapped key lands in `extras` JSONB per `_entry_to_row` step 2/5. Nothing lost. RESEARCH § Assumption A2. |
</threat_model>

<verification>
1. Static: `bash -n app/entrypoint.sh` — no syntax errors.
2. Unit: `cd app && pytest tests/test_migration.py -x` — both tests GREEN (test_idempotent_rerun + test_preserves_ku_manual).
3. Manual dry-run: `cd app && cp data/app_data.json /tmp/app_data.json.bak; python migrate_from_json.py` (from a shell inside the container). Confirms rows appear in Postgres; confirms `data/app_data.json` renamed to `data/app_data.json.pre-pg7`; a SECOND `python migrate_from_json.py` invocation exits 0 with `"No <path> — nothing to migrate"`.
4. Fresh boot: `docker compose down && docker compose up -d --build`. `docker compose logs app` shows migration lines in order after Alembic. `curl http://127.0.0.1:8000/api/pending` and `curl http://127.0.0.1:8000/api/health` return valid JSON.
5. Full-suite regression: `cd app && pytest -x`. Every test should pass. If any test in Waves 4/5 scope fails now (e.g. `test_ingest.py` due to `_lock` semantics under stress), that is expected — Waves 4/5 will address it. Log those failures as expected in the SUMMARY.
</verification>

<success_criteria>
- `migrate_from_json.py` runs idempotently, upserts every legacy entry, renames source file on success.
- `ku.manual` survives migration (Pitfall 7 guard passes).
- `entrypoint.sh` invokes the script between Alembic and uvicorn.
- Wave 0 test file `test_migration.py` — GREEN (was RED at Wave 0).
- Container starts cleanly on both a fresh volume and a volume with legacy `app_data.json`.
- No secret ever appears in the script or its log output.
</success_criteria>

<output>
Create `.planning/phases/07-database-migration/07-03-SUMMARY.md` when done.
</output>
