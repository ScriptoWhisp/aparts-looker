---
phase: 07-database-migration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/requirements.txt
  - app/tests/conftest.py
  - app/tests/test_db_smoke.py
  - app/tests/test_models.py
  - app/tests/test_alembic.py
  - app/tests/test_migration.py
autonomous: false
requirements: []
# NOTE: RED test scaffolds do NOT satisfy requirements — they define the contract that
# Waves 1-3 must satisfy. Requirement claims live on the implementing wave, per the
# gap-closure convention. DB-01 satisfied by 07-01 (Wave 1). DB-02 satisfied by 07-01.
# DB-03 satisfied by 07-01 (Alembic baseline). DB-04 satisfied by 07-03 (migration).
# DB-07 satisfied by 07-01 (pytest-postgresql wired end-to-end via first GREEN run).
user_setup:
  - service: postgres
    why: "Wave 1 adds a Postgres service to docker-compose that reads POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB from .env at container start. Deploy will fail with `psycopg.OperationalError: fe_sendauth: no password supplied` if these are missing when Wave 1 lands on the VPS."
    env_vars:
      - name: POSTGRES_USER
        source: "Set to 'aparts' in .env on VPS (any value is fine — no dashboard)"
      - name: POSTGRES_PASSWORD
        source: "Generate locally: `openssl rand -base64 32`; paste into VPS /opt/aparts-looker/.env. Never commit."
      - name: POSTGRES_DB
        source: "Set to 'aparts_looker' in .env on VPS"

must_haves:
  truths:
    - "Running `cd app && pytest tests/test_db_smoke.py tests/test_models.py tests/test_alembic.py tests/test_migration.py -x` produces 4 RED failures (import errors OK — files do not yet exist for db, models, alembic, migrate_from_json)"
    - "The `db_session` fixture in conftest.py boots a per-session Postgres via pytest-postgresql, opens a savepoint per test, and rolls back on teardown"
    - "app/requirements.txt lists sqlalchemy>=2.0.51, alembic>=1.18.5, psycopg[binary]>=3.3.4, pytest-postgresql>=6.0.0"
    - "The user has confirmed POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB are set in /opt/aparts-looker/.env on the VPS BEFORE Wave 1 lands"
  artifacts:
    - "app/requirements.txt (updated)"
    - "app/tests/conftest.py (adds db_session fixture, keeps existing tmp_agent_state)"
    - "app/tests/test_db_smoke.py (RED: covers DB-01)"
    - "app/tests/test_models.py (RED: covers DB-02)"
    - "app/tests/test_alembic.py (RED: covers DB-03)"
    - "app/tests/test_migration.py (RED: covers DB-04)"
  key_links:
    - "pytest-postgresql fixture wiring — db_session must monkeypatch db.SessionLocal so future data_store tests use the same rolled-back transaction"
    - "requirements.txt drift — VPS `pip install -r requirements.txt` must succeed on `python:3.12-slim` for Wave 1 to even boot"
---

<objective>
Wave 0 — RED phase test scaffolds + pytest-postgresql fixture wiring + deploy-prerequisite checkpoint.

Purpose: Land failing tests that pin down the contract for Waves 1-3 (per D-02 SQLAlchemy 2.x, D-03 hybrid schema, D-04 status ENUM, D-05 VARCHAR PK). Add pytest-postgresql so the whole test suite eventually runs against real Postgres (per Claude's discretion in CONTEXT.md — matches production dialect for JSONB and ENUM). Block the merge on the user confirming POSTGRES_* env vars are set on the VPS — Wave 1 docker-compose changes would otherwise break the deploy.

Output: 4 RED test files, updated requirements.txt, extended conftest.py with a `db_session` fixture bound to a per-test transaction that always rolls back, and a human-verify checkpoint that the user has staged the Postgres secrets on the VPS.
</objective>

<note>
RED scaffolds define the contract for later waves; requirement satisfaction is claimed by the implementing wave, not this scaffold wave. Hence `requirements: []` in this plan's frontmatter. Wave 1 (07-01) claims DB-01/DB-02/DB-03/DB-06/DB-07; Wave 3 (07-03) claims DB-04. If plan-checker flags Phase 7 as missing DB-07 coverage, verify 07-01's `requirements:` field includes it — Wave 0 is intentionally requirement-free by design.
</note>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-database-migration/07-CONTEXT.md
@.planning/phases/07-database-migration/07-RESEARCH.md

@app/tests/conftest.py
@app/requirements.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Deploy prerequisite — confirm POSTGRES_* set in VPS .env</name>
  <files>(no repo file changes — human-verify checkpoint only)</files>
  <what-built>Nothing built yet. This is a pre-flight guard so Wave 1's docker-compose Postgres service does not crash the container on first deploy. The GitHub Actions workflow runs `git pull && docker compose up -d --build` on push to `main`; the Postgres service will read `${POSTGRES_USER}`, `${POSTGRES_PASSWORD}`, `${POSTGRES_DB}` from `/opt/aparts-looker/.env`, and the app container's `depends_on: postgres: condition: service_healthy` will hang if authentication fails.</what-built>
  <action>Follow the how-to-verify steps below; this task modifies no repo files — it exists to gate the merge on operator action on the VPS.</action>
  <how-to-verify>
    1. SSH into the VPS: `ssh root@46.62.152.9`
    2. Open the env file: `nano /opt/aparts-looker/.env`
    3. Ensure three lines exist (add if missing — do NOT commit the values anywhere):
       - `POSTGRES_USER=aparts`
       - `POSTGRES_PASSWORD=<paste output of `openssl rand -base64 32` run locally>`
       - `POSTGRES_DB=aparts_looker`
    4. Save the file. Exit SSH.
    5. Confirm here that the three keys exist in the VPS .env.

    Проверьте, что три строки POSTGRES_* добавлены в /opt/aparts-looker/.env на VPS до того, как Wave 1 попадёт в main. Иначе деплой упадёт при первом `docker compose up`.
  </how-to-verify>
  <verify><human-check>Operator confirms via SSH that POSTGRES_USER / POSTGRES_PASSWORD / POSTGRES_DB are all present in /opt/aparts-looker/.env before the merge lands.</human-check></verify>
  <done>Operator has responded "approved" and the three env keys are present in the VPS .env file.</done>
  <resume-signal>Type "approved" once .env is updated on the VPS, or describe blocker.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Extend requirements.txt with SQLAlchemy 2.x + Alembic + psycopg3 + pytest-postgresql</name>
  <files>app/requirements.txt</files>
  <behavior>
    - After edit: `pip install -r app/requirements.txt` on `python:3.12-slim` installs all four new packages without native build errors (psycopg[binary] ships prebuilt wheels).
    - Existing lines unchanged; only appended.
  </behavior>
  <read_first>
    - app/requirements.txt (current lines — do not remove any)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Standard Stack / Installation (per D-02 the exact pinned versions)
  </read_first>
  <action>
    Append four lines to app/requirements.txt (keep existing lines above, do NOT reorder):

    ```
    sqlalchemy>=2.0.51
    alembic>=1.18.5
    psycopg[binary]>=3.3.4
    pytest-postgresql>=6.0.0
    ```

    Notes for the executor:
    - `psycopg[binary]` (per D-02 driver research) — pulls the precompiled wheel; no libpq-dev needed in the Dockerfile.
    - Do NOT switch `psycopg2-binary` in (research verdict: psycopg3 is the forward-looking choice; psycopg2-binary is the escape hatch documented in Assumption A1 only if psycopg3 wheels ever fail).
    - Do NOT bump other pinned versions in the same commit — keep the delta minimal.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker && grep -q '^sqlalchemy>=2.0.51$' app/requirements.txt && grep -q '^alembic>=1.18.5$' app/requirements.txt && grep -q '^psycopg\[binary\]>=3.3.4$' app/requirements.txt && grep -q '^pytest-postgresql>=6.0.0$' app/requirements.txt && echo OK"</automated>
  </verify>
  <done>All four lines present in app/requirements.txt; existing lines untouched; no line ordering churn.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend conftest.py with a db_session fixture backed by pytest-postgresql</name>
  <files>app/tests/conftest.py</files>
  <behavior>
    - Existing fixtures (`tmp_agent_state`, `client`, `mock_telegram`, `mock_send_pending_card`, `mock_gmail`) unchanged and continue to work — many Phase 6 tests still need `tmp_agent_state` for `settings.json` / `agent_state.json` (both stay on disk per D-Discretion).
    - New `db_session` fixture yields a SQLAlchemy `Session` bound to a per-test transaction that always rolls back (BEGIN + SAVEPOINT + ROLLBACK) — no truncate, no leakage between tests.
    - `db.SessionLocal` is monkeypatched inside the fixture so any code path that calls `data_store.*` under this fixture uses the same connection as the test's `db_session`.
    - Fixture is safe to import even when `app/db.py` and `app/models.py` do not yet exist (Wave 1 adds them) — the fixture body imports them lazily; tests that reference `db_session` will fail with a clean ImportError, which is the RED behaviour Wave 0 wants.
  </behavior>
  <read_first>
    - app/tests/conftest.py (all — preserve every existing fixture verbatim)
    - .planning/phases/07-database-migration/07-RESEARCH.md § pytest-postgresql fixture for rollback isolation (canonical shape to copy)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 8 (why rollback-per-savepoint over truncate)
  </read_first>
  <action>
    Append (do NOT edit any existing fixture) two new module-level fixture factory calls and a `db_session` fixture, mirroring the canonical shape in RESEARCH.md § pytest-postgresql fixture.

    Behavior points the executor must preserve:
    - Session-scoped `postgresql_proc_fixture = factories.postgresql_proc(port=None)` (auto port so parallel test runs do not collide).
    - Function-scoped `postgresql_db_fixture = factories.postgresql("postgresql_proc_fixture")`.
    - `db_session(postgresql_db_fixture, monkeypatch)` fixture that:
      1. Builds `postgresql+psycopg://user@host:port/dbname` from `postgresql_db_fixture.info` (per D-02 psycopg3 driver — matches production DSN scheme so a query passing under test is guaranteed to parse under production).
      2. `engine = create_engine(conn_str)`; `Base.metadata.create_all(engine)` — creates the schema directly from `models.py` (skipping Alembic in tests per RESEARCH § Testing strategy).
      3. Opens a connection, starts a transaction, binds a `sessionmaker(bind=connection, expire_on_commit=False)` to it.
      4. `monkeypatch.setattr(db_module, "SessionLocal", TestSession)` so `data_store.*` in-fixture calls share the connection.
      5. Yields the session.
      6. Teardown: `session.close()`; `trans.rollback()`; `connection.close()`; `engine.dispose()`.

    Import discipline (per CLAUDE.md layered imports):
    - `from pytest_postgresql import factories` at module top.
    - The `db_session` body imports `db` and `Base` lazily via `import db as db_module; from db import Base` inside the fixture — Wave 1 creates these modules; Wave 0 tests intentionally fail to import them (RED).

    Do NOT delete or refactor `tmp_agent_state`. Tests touching `settings.json` / `agent_state.json` (which stay on filesystem per D-Discretion) continue to use it.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker && grep -q 'from pytest_postgresql import factories' app/tests/conftest.py && grep -q 'def db_session' app/tests/conftest.py && grep -q 'postgresql_proc_fixture' app/tests/conftest.py && grep -q 'tmp_agent_state' app/tests/conftest.py && echo OK"</automated>
  </verify>
  <done>New `db_session` fixture added; existing `tmp_agent_state` / `client` / mocks unchanged; factories import present; fixture body imports `db` / `Base` lazily so Wave 1 can land the modules without touching conftest again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write 4 RED test scaffolds — smoke, models, alembic, migration</name>
  <files>
    app/tests/test_db_smoke.py,
    app/tests/test_models.py,
    app/tests/test_alembic.py,
    app/tests/test_migration.py
  </files>
  <behavior>
    Every test in these four files MUST fail at the current commit (RED). They pin down the contract Wave 1-3 must satisfy — per D-02 (SQLAlchemy 2.x), D-03 (hybrid schema JSONB), D-04 (status ENUM), D-05 (VARCHAR PK).

    Required test cases per file (each must exist as a `def test_*` function; each must fail at Wave 0 either with ImportError, ModuleNotFoundError, or AssertionError):

    - test_db_smoke.py (covers DB-01):
      1. `test_engine_connects(db_session)` — imports `db`, asserts `db.engine.dialect.name == "postgresql"` and `db_session.execute(text("SELECT 1")).scalar() == 1`. Fails now because `db` module does not exist.

    - test_models.py (covers DB-02):
      2. `test_listing_roundtrip(db_session)` — creates a `Listing(id="test-1", url="https://kv.ee/...", price_eur=200000, area_sqm=60.0, rooms=3, district="Mustamäe", status="pending", cost_of_ownership={"a": 1}, viewing_history=[{"e": 1}])`, `db_session.add(row); db_session.flush()`, then `fetched = db_session.get(Listing, "test-1")`, asserts every scalar and every JSONB blob round-trips (including `fetched.cost_of_ownership == {"a": 1}` and `fetched.viewing_history == [{"e": 1}]`).
      3. `test_status_enum_rejects_invalid(db_session)` — attempts `Listing(id="test-2", status="not_a_real_status")`, `db_session.add(row); db_session.flush()`, expects `sqlalchemy.exc.DataError` (Postgres raises on invalid enum). Fails now because model does not exist.
      4. `test_jsonb_mutation_requires_reassign(db_session)` — creates row with `viewing_history=[{"e": 1}]`, commits (savepoint), reopens via `db_session.get`, mutates in place `.append(...)`, commits again, reopens, asserts the second event is MISSING (documents Pitfall 1 convention). Then reassigns whole list, commits, reopens, asserts second event NOW present.

    - test_alembic.py (covers DB-03):
      5. `test_alembic_head_matches_metadata(postgresql_db_fixture)` — points Alembic at the pytest-postgresql instance via `DATABASE_URL` env var, runs `command.upgrade(alembic_cfg, "head")` from the `alembic` package, then uses `alembic.migration.MigrationContext.configure(...).compare_metadata(Base.metadata)` — asserts the returned diff is empty. Fails now because `app/alembic/` does not exist.

    - test_migration.py (covers DB-04):
      6. `test_idempotent_rerun(db_session, tmp_path, monkeypatch)` — writes a minimal `app_data.json` (2 properties, 1 pending, 1 rejected) to `tmp_path`, monkeypatches `config.APP_DATA_FILE` to that path, runs `migrate_from_json.main()`, asserts 4 rows in the DB with matching IDs. Runs `main()` a SECOND time from a fresh conftest state, asserts row count still exactly 4 (idempotency via `db.merge` per RESEARCH § Pitfall 5). Fails now because `migrate_from_json.py` does not exist.
      7. `test_preserves_ku_manual(db_session, tmp_path, monkeypatch)` — writes `app_data.json` with `properties[0].ku = {"auto": {"reg_code": "12345"}, "manual": "meeting notes from Feb"}`, runs migration, asserts DB row's `ku` dict has `manual == "meeting notes from Feb"` (Pitfall 7 lives on through the migration). Fails now.
  </behavior>
  <read_first>
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pattern 2 (Listing model column definitions — every JSONB field name)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 1 (JSONB mutation not detected — the exact convention the tests must pin down)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 5 (migration idempotency guards — db.merge + file-rename)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 7 (save_ku_enrichment preserves manual — behaviour must survive migration)
    - app/tests/test_data_store.py — file only; extract the current fixture-use idiom (`tmp_agent_state`, `data_store.load_app_data`) so RED tests match project style; do NOT copy assertions
  </read_first>
  <action>
    Create four new test files under `app/tests/` following the project style (module docstring, `import pytest`, function-scoped `def test_*` with fixture parameters).

    Discipline points for the executor:
    - Every test uses `db_session` fixture (added Task 2). Migration tests also inject `tmp_path` and `monkeypatch`.
    - Import `from models import Listing` lazily INSIDE each test body (top-level import would explode the whole test file with a ModuleNotFoundError at collection time — pytest would report zero collected tests instead of 7 RED failures, which loses signal).
    - Use `sqlalchemy.text("SELECT 1")` (not raw string) in test_db_smoke.py per RESEARCH § V5 Input Validation.
    - test_status_enum_rejects_invalid asserts on `pytest.raises(sqlalchemy.exc.DataError)` (Postgres native ENUM rejects at flush per D-04).
    - test_jsonb_mutation_requires_reassign must have TWO commits with an explicit `db_session.commit(); db_session.close()` + fresh `SessionLocal()` reopen between — the in-place mutation only disappears across a real round-trip through the DB (Pitfall 1 warning-signs section).
    - test_alembic.py imports `from alembic.config import Config` and `from alembic import command`. Point `alembic_cfg.set_main_option("script_location", os.path.join(APP_DIR, "alembic"))` and `.set_main_option("sqlalchemy.url", postgresql_db_fixture_url)`.
    - test_migration.py uses `import migrate_from_json` (lazy) — Wave 3 adds the module.

    Every test MUST currently fail with either ImportError (`db`, `models`, `migrate_from_json`, `alembic/`) or AssertionError (Wave 1-3 will make them GREEN). This is the RED anchor.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker && ls app/tests/test_db_smoke.py app/tests/test_models.py app/tests/test_alembic.py app/tests/test_migration.py >/dev/null && grep -c '^def test_' app/tests/test_db_smoke.py app/tests/test_models.py app/tests/test_alembic.py app/tests/test_migration.py | grep -E ':[1-9]' && echo OK"</automated>
  </verify>
  <done>Four RED test files exist; each contains at least one `def test_*` function; every test currently fails when run (ImportError or AssertionError acceptable — the RED anchor for Wave 1-3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer laptop → VPS `.env` | Manual paste of `POSTGRES_PASSWORD`; never touches git |
| pytest-postgresql subprocess → test process | Local Unix socket, no network exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-00-01 | Information Disclosure | POSTGRES_PASSWORD in checkpoint prose | high | mitigate | Task 0 instructs user to run `openssl rand -base64 32` locally and paste on VPS; never commit; the checkpoint prose contains no example value. Never log the value in Claude's response. |
| T-07-00-02 | Tampering | pytest-postgresql package supply chain | low | mitigate | Package audited in RESEARCH § Package Legitimacy Audit — [OK], ClearcodeHQ (dbfixtures), 10 yrs on PyPI, hundreds of thousands of weekly downloads. No `checkpoint:human-verify` required. |
| T-07-00-03 | Denial of Service | pytest-postgresql spawns Postgres subprocess per session | low | accept | Subprocess is auto-torn-down by fixture teardown; port is `None` (auto-assigned) so parallel runs cannot collide. |
| T-07-00-SC | Tampering | pip installs of sqlalchemy / alembic / psycopg / pytest-postgresql | high | mitigate | RESEARCH § Package Legitimacy Audit ran — all four [OK] (no [SUS], no [SLOP], seam warnings all `unknown-downloads` on packages with tens of millions of weekly downloads). No `checkpoint:human-verify` gate needed on install (SUS verdicts on standard-of-the-industry packages do not warrant blocking the loop). |
</threat_model>

<verification>
1. `cd app && pytest tests/test_db_smoke.py tests/test_models.py tests/test_alembic.py tests/test_migration.py --collect-only -q` — should COLLECT 7 tests (proves file structure is correct).
2. `cd app && pytest tests/test_db_smoke.py tests/test_models.py tests/test_alembic.py tests/test_migration.py -x --no-header` — should FAIL fast (RED anchor). Any test PASSING here is a bug; either the test is not actually asserting the contract, or Wave 1 accidentally already ran.
3. `cd app && pytest tests/ -x --ignore=tests/test_db_smoke.py --ignore=tests/test_models.py --ignore=tests/test_alembic.py --ignore=tests/test_migration.py` — every OTHER existing test file must still pass; Wave 0 must NOT regress the current suite (Phase 6 exit criterion: full-suite green).
</verification>

<success_criteria>
- 4 new test files exist under `app/tests/`; collect 7 tests total.
- All 7 tests fail at HEAD (RED). Wave 1-3 will make them GREEN incrementally.
- `app/requirements.txt` gained exactly 4 new lines (sqlalchemy, alembic, psycopg[binary], pytest-postgresql) — no removals, no reordering.
- `app/tests/conftest.py` has a new `db_session` fixture, factories import present, and every existing fixture is unchanged.
- User has confirmed POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB are set in /opt/aparts-looker/.env on the VPS.
- Every existing test file still passes (`cd app && pytest -x --ignore=tests/test_db_smoke.py --ignore=tests/test_models.py --ignore=tests/test_alembic.py --ignore=tests/test_migration.py`).
</success_criteria>

<output>
Create `.planning/phases/07-database-migration/07-00-SUMMARY.md` when done.
</output>
