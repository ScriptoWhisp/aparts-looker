---
phase: 07-database-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/data_store.py
autonomous: true
requirements:
  - DB-05
  - DB-07

must_haves:
  truths:
    - "Every public function in `data_store.py` (23 functions) keeps its exact signature — no caller in main.py / ingest_handler.py / brief_generator.py / agent_job.py / settings_store.py needs to change"
    - "Every existing test in `test_data_store.py`, `test_pending.py`, `test_viewing_workflow.py`, `test_price_intelligence.py`, `test_ingest.py` passes against the DB backend via the `db_session` fixture (per D-05 rewire, D-07 real Postgres in tests)"
    - "`data_store._lock` still exists as a symbol — but is a `contextlib.nullcontext()` no-op, so existing `with data_store._lock:` blocks in main.py / brief_generator.py / ingest_handler.py / settings_store.py continue to parse and execute without change (Wave 4 rewrites the Pitfall-5 call sites; Wave 5 rewrites the hot callers)"
    - "SQLAlchemyError is caught and logged in every public function; sentinel return values match the pre-Phase-7 semantics (False / None / [] / {}) per never-raise pattern"
    - "JSONB reassignment convention (Pitfall 1) is applied in every function that mutates `viewing_history`, `cost_of_ownership`, `checklist`, `ku`, `price_history` — no in-place `.append()` / `.setdefault()` calls on JSONB columns"
  artifacts:
    - "app/data_store.py — internals rewritten against SQLAlchemy; public API preserved"
  key_links:
    - "`_lock` becomes `contextlib.nullcontext()` — atomicity now provided by DB transactions; callers keep the syntax for zero-diff Wave 2"
    - "`load_app_data()` compatibility shim reconstructs `{'properties':[], 'pending':[], 'rejected':[], 'checklists':{}, 'price_history':{}, 'settings':{}}` from a single `db.query(Listing).all()` — settings key stays `{}` (settings.json filesystem home per D-Discretion)"
    - "`save_app_data(data)` upserts each entry via `db.merge(Listing(**fields))` — Wave 5 rewrites the hot callers that abuse this shim"
    - "`load_agent_state` / `save_agent_state` UNCHANGED — agent_state.json stays on disk (Claude's Discretion in CONTEXT.md)"
    - "`save_ku_enrichment` MUST preserve existing `ku.manual` (Pitfall 7) — regression here would silently wipe user notes"
---

<objective>
Wave 2 — Rewire `data_store.py` public API against SQLAlchemy while preserving every caller's contract.

Purpose: Cut persistence over from JSON to Postgres in a single atomic file change. No caller signatures change; no caller code changes. The rewrite is entirely inside `data_store.py`. `settings.json` and `agent_state.json` continue to be filesystem-backed per D-Discretion (leaving them on disk avoids scope creep and preserves the crash-safe atomic-write patterns).

Output: `data_store.py` fully rewritten. Every existing test (Phase 2, 3, 5, 6) passes against the new DB backend via the `db_session` fixture. The `_lock` symbol survives as a no-op so Waves 4/5 can rewrite each caller independently instead of touching every file in a single mega-commit.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-database-migration/07-CONTEXT.md
@.planning/phases/07-database-migration/07-RESEARCH.md
@.planning/phases/07-database-migration/07-00-SUMMARY.md
@.planning/phases/07-database-migration/07-01-SUMMARY.md

@app/data_store.py
@app/db.py
@app/models.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire data_store.py — internal SQLAlchemy, preserved public API, no-op _lock shim</name>
  <files>app/data_store.py</files>
  <behavior>
    Every function keeps its exact signature (see the function list below). Every function uses `SessionLocal()` internally. Every SQLAlchemy operation is wrapped in `try / except SQLAlchemyError` with `log.exception(...)` + sentinel return per never-raise pattern.

    Required signatures (verbatim from current app/data_store.py):

    Persistence primitives (compatibility shims):
    - `load_app_data() -> dict` — reconstructs `{'properties':[], 'pending':[], 'rejected':[], 'checklists':{}, 'price_history':{}, 'settings':{}}` from a single `db.query(Listing).all()` pass. `settings` stays `{}`.
    - `save_app_data(data) -> None` — upserts every entry across `data['properties'] + data['pending'] + data['rejected']` via `db.merge(Listing(**mapped_fields))`. Wave 5 rewrites the hot callers so this pattern is not the bottleneck for backfill endpoints.

    Agent state (unchanged — stays JSON-backed):
    - `load_agent_state() -> dict`
    - `save_agent_state(state: dict) -> None`

    Listing CRUD:
    - `add_property_if_new(prop: dict) -> bool` — INSERT if id not present; return True.
    - `add_to_pending(entry: dict) -> bool` — INSERT with status="pending" if id not already in pending or approved-tier. Return False on dedup hit or SQLAlchemyError.
    - `load_pending() -> list` — `db.query(Listing).filter(Listing.status == "pending").all()` → list of dicts (via `_row_to_pending_dict`).

    State transitions (Phase 2/6):
    - `approve_listing(listing_id: str) -> bool` — flip status pending→approved. Preserve draft_body / contact_email / draft_subject fields (already columns; no dict merge needed post-Phase-7). Return False if not found or already in approved-tier.
    - `reject_listing(listing_id: str, reason: str) -> bool` — flip status to rejected; reason whitelist `{"price","location","condition","other"}` clamps to "other"; set `rejected_at` UTC ISO.
    - `set_viewing_scheduled(listing_id: str, scheduled_at_iso: str) -> bool` — per RESEARCH § Pattern 7 example. Reassign `row.viewing_history` (do NOT `.append` — Pitfall 1).
    - `mark_viewed(listing_id: str) -> bool` — only permitted from `viewing_scheduled` (D-03 rule, preserved from Phase 6 data_store.py:257-291). Log a warning on invalid transitions.
    - `save_negotiation_brief(listing_id: str, brief: dict) -> bool` — overwrite `negotiation_brief` JSONB + set `negotiation_brief_generated_at` UTC ISO.
    - `save_ku_enrichment(listing_id: str, ku_auto: dict) -> bool` — MUST preserve `ku.manual` per RESEARCH § Pitfall 7 example verbatim (regression here silently wipes user notes; the existing `test_save_ku_preserves_manual` test is the guard).

    AI checklist:
    - `write_checklist_ai(listing_id: str, checklist: dict) -> None` — mutate `Listing.checklist` JSONB with `ai_checklist` subkey; preserve `source == "user"` entries per D-09 pattern (see current data_store.py:375-394 for the exact preservation logic — translate the dict-of-dicts merge into the JSONB reassign convention).

    Price history:
    - `record_price_in_data(data: dict, listing_id: str, price_eur: int, date_str: str) -> None` — SIGNATURE PRESERVED. Ingest_handler currently calls this with `data = app_data` inside `with _lock:`. Wave 2 preserves the shape but internally: mutates the `data['price_history'][listing_id]` list (which came from `load_app_data`), same idempotency + 90-entry cap as today. The list ends up committed to DB when Wave 5 rewrites `process_ingest_batch` OR when the current post-loop `save_app_data(app_data)` in ingest_handler runs. Wave 5 will rewrite `_record_and_check_price_drop` for efficiency; Wave 2 keeps semantics.
    - `get_price_history(listing_id: str) -> list` — `db.query(Listing).get(listing_id).price_history` or `[]`.

    Queries:
    - `get_rejected_by_reason(reason: str) -> list` — `db.query(Listing).filter(Listing.status == "rejected", Listing.rejection_reason == reason).all()`.
    - `get_approved_listing(listing_id: str) -> Optional[dict]` — approved OR viewing_scheduled OR viewed → row-to-dict.

    Coordinate helpers:
    - `update_listing_coords(listing_id: str, lat: float, lng: float, commute_minutes: Optional[int]) -> bool`
    - `get_listing_coords(listing_id: str) -> dict` — returns `{"lat", "lng", "commute_minutes"}` (all None on miss).

    Constants:
    - `DEFAULT_APP_DATA` stays exported (agent_job / tests import it). Keep the dict shape; the empty `properties` list is fine since we no longer seed from a Python constant.
    - `DEFAULT_AGENT_STATE` stays unchanged (agent_state.json still JSON-backed).
    - `DEFAULT_PROPERTIES` (14-item legacy seed) DELETED. Legacy rows land via Wave 3 migration OR fresh installs start empty.

    Symbols preserved as compatibility shims:
    - `_lock` — becomes `_lock = contextlib.nullcontext()`. Every `with data_store._lock:` in the rest of the codebase (main.py, brief_generator.py, ingest_handler.py, settings_store.py) continues to parse and execute. Waves 4/5 will remove these wrappers file-by-file; Wave 2 does not touch any caller.

    Internal helpers (private):
    - `_row_to_property_dict(row: Listing) -> dict` — approved-tier row → dict shaped like the legacy `properties[]` entry (includes `id`, `title`, `name`, `price_eur`, `pricePerSqm` alias, `area_sqm`, `area`, `rooms`, `year_built`, `year`, `district`, `notes`, `status`, `scheduled_at`, `viewing_history`, `negotiation_brief`, `ku`, `cost_of_ownership`, `lat`, `lng`, `commute_minutes`, `energy_class`, all top-level scalar columns). Include the `pricePerSqm` / `price` / `area` / `year` / `name` legacy aliases so the frontend continues to render without frontend changes (frontend fix is out of scope for Phase 7).
    - `_row_to_pending_dict(row: Listing) -> dict` — pending row → dict shaped like the legacy `pending[]` entry (includes score, verdict, score_breakdown, risks, strengths, concerns, ai_checklist_fills, cost_of_ownership, draft_subject, draft_body, queued_at, tg_message_id, tg_chat_id, and all scalars).
    - `_row_to_rejected_dict(row: Listing) -> dict` — rejected row → dict; includes `rejection_reason`, `rejected_at`.
    - `_pending_to_property(entry: dict) -> dict` — KEPT for backward-compat; agent_job.py imports it. Simply passes through with an updated notes string. Wave 4 will consider removing.
    - `_dict_to_listing_fields(entry: dict) -> dict` — new helper for `save_app_data` upserts; maps legacy dict keys (with `_LEGACY_ALIASES` from main.py:197) to `Listing.__table__.columns.keys()`; unknown keys land in `extras` JSONB.
      - **CRITICAL — alias application on EVERY write path:** `_dict_to_listing_fields` MUST apply `_LEGACY_ALIASES` on every invocation, not just on migration read. This means every `save_app_data` upsert path passes through the same alias translation. A caller mutating `data['pending'][0]['name']` (legacy key) and calling `save_app_data(data)` MUST land in the `title` column, not the `extras` JSONB spill. Failure mode: legacy frontend/ingest code writes `name` / `area` / `year` / `notes` / `price` / `pricePerSqm` back through `save_app_data` and those values silently spill to `extras` instead of overwriting the real column — data becomes invisible to every subsequent `load_app_data` snapshot.
    - **`settings` field in compat shim (CRITICAL):** `load_app_data()` returns `data['settings'] = {}` (always empty — `settings.json` is the source of truth per CONTEXT.md's Claude's Discretion). Any caller mutating `data['settings']` and calling `save_app_data(data)` silently loses those changes. This is INTENTIONAL. No test should assert otherwise. If a caller genuinely needs runtime settings, use `settings_store.get_all()` / `settings_store.save()` instead. The compat shim's `settings: {}` is a placeholder for structural parity with the pre-Phase-7 dict shape only.

    Layered imports (per CLAUDE.md):
    - `data_store.py` imports `db`, `models`, `config`, `contextlib`, `logging`, `datetime`, `typing`, `sqlalchemy.exc.SQLAlchemyError`. NO import of `main`, `ingest_handler`, `brief_generator`, `agent_job`, `settings_store`.
    - Do NOT import SQLAlchemy directly (only `SQLAlchemyError`); use through `db.SessionLocal` and `models.Listing`.
  </behavior>
  <read_first>
    - app/data_store.py (all — every function's current behaviour is the contract Wave 2 must reproduce; treat this file as the specification)
    - app/db.py (from Wave 1 — `SessionLocal`, `Base`)
    - app/models.py (from Wave 1 — column list; `LISTING_STATUS_VALUES`)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pattern 7 (data_store rewiring examples — set_viewing_scheduled, load_app_data compatibility shim, save_ku_enrichment Pitfall-7)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 1 (JSONB mutation not detected — every JSONB-touching function in this task must follow the reassign convention)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 7 (save_ku_enrichment preserves manual)
    - .planning/phases/06-viewing-workflow-extras/06-PATTERNS.md § Thread-safe JSON I/O + Never-raise on external HTTP + setdefault-on-load migration (patterns preserved through Phase 7)
    - app/main.py:197 (_LEGACY_ALIASES — the dict `_dict_to_listing_fields` must apply the same alias translation on write path)
    - app/tests/test_data_store.py (all — every existing test must pass; do NOT weaken any assertion during the port)
    - app/tests/test_viewing_workflow.py (all — same)
    - app/tests/test_pending.py (all — same)
    - app/tests/test_price_intelligence.py (all — same)
  </read_first>
  <action>
    Rewrite `app/data_store.py` end-to-end while preserving every public function signature listed in the Behavior block.

    Structural discipline:
    - Module docstring updated per CLAUDE.md convention: explain that persistence is now Postgres via SQLAlchemy 2.x (D-01, D-02); `settings.json` and `agent_state.json` remain filesystem-backed per D-Discretion; `_lock` is a no-op shim during the Wave 4/5 rewrite transition (Waves 4/5 will remove callers site-by-site).
    - `import contextlib` and `_lock = contextlib.nullcontext()` at module top — replaces `threading.RLock()`.
    - `from db import SessionLocal` and `from models import Listing`.
    - `from sqlalchemy.exc import SQLAlchemyError`.
    - Every function that opens a session uses `with SessionLocal() as db_:` (rename local var to `db_` to avoid shadowing the `db` module import).
    - Every function that writes commits explicitly (`db_.commit()`) at the end of the mutation path.
    - Every function catches `SQLAlchemyError` (never bare `Exception` for DB operations) + logs via `log.exception("<function name> failed for %s", <key>)` + returns the sentinel that matches the pre-Phase-7 signature (False / None / [] / {}).

    Per-function specifics (executor picks the exact SQLAlchemy idiom; the listed rules are non-negotiable):
    - JSONB reassignment (Pitfall 1): every function that mutates `viewing_history`, `cost_of_ownership`, `checklist`, `ku`, `price_history`, `score_breakdown`, `ai_checklist_fills`, `strengths`, `concerns`, `risks`, `extras`, `negotiation_brief` MUST reassign the whole list/dict — never in-place `.append(...)` or `.setdefault(...)` on the ORM attribute.
    - `save_ku_enrichment` MUST preserve `existing.get("manual", "")` per RESEARCH § Pitfall 7 example verbatim.
    - `write_checklist_ai` MUST preserve entries whose `source == "user"` (translate the dict-of-dicts merge from current data_store.py:381-393 into a fresh dict built + reassigned).
    - `record_price_in_data(data, listing_id, price_eur, date_str)`: keep the current signature and semantics (mutates the passed dict's `price_history[listing_id]` list, does not touch DB directly). The mutation still needs to end up committed — Wave 2 keeps the current mechanism (ingest_handler calls `save_app_data(app_data)` at end of loop, which now upserts every row and re-writes the JSONB `price_history` column). Wave 5 will optimize this hot path.
    - `load_app_data()` compatibility shim: implement per RESEARCH § Pattern 7 example. `checklists` key returns `{row.id: row.checklist for row in rows if row.checklist}`; `price_history` key returns `{row.id: row.price_history for row in rows if row.price_history}`; `settings` key returns `{}` (settings live in `settings.json`, out of DB scope).
    - `save_app_data(data)`: implement by iterating `data['properties'] + data['pending'] + data['rejected']`, calling `db_.merge(Listing(**_dict_to_listing_fields(entry)))` per entry, then `db_.commit()`. Wave 5 rewrites the hot callers to use row-scoped writes; Wave 2 keeps the compatibility shim for correctness in every other caller.
    - `_dict_to_listing_fields`: apply `_LEGACY_ALIASES` (from main.py:197 — `name→title`, `price→price_eur`, `pricePerSqm→price_per_sqm`, `area→area_sqm`, `year→year_built`, `notes→description` or per current mapping — READ main.py:197 for the truth), then intersect with `Listing.__table__.columns.keys()`, spill remainder into `extras` JSONB. Coerce None to empty list/dict for JSONB list/dict columns.

    Never-raise contract discipline (per CLAUDE.md + RESEARCH § Project Constraints):
    - No SQLAlchemyError propagates to callers. Ever.
    - `log.exception(...)` with a message that names the function and the primary key or lookup value, but NEVER logs `DATABASE_URL` or password.

    What Wave 2 does NOT do:
    - Does NOT touch main.py, ingest_handler.py, brief_generator.py, agent_job.py, settings_store.py — all callers keep working via preserved signatures + `_lock` no-op shim.
    - Does NOT delete `_lock` — Wave 4/5 will remove callers file-by-file.
    - Does NOT modify frontend / static / templates — API responses stay shape-compatible via `_row_to_property_dict` legacy aliases.
    - Does NOT migrate `settings.json` or `agent_state.json` (D-Discretion in CONTEXT.md).
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker/app && python -c 'import data_store; assert hasattr(data_store, \"_lock\"); assert hasattr(data_store, \"load_app_data\"); assert hasattr(data_store, \"save_app_data\"); assert hasattr(data_store, \"add_to_pending\"); assert hasattr(data_store, \"approve_listing\"); assert hasattr(data_store, \"reject_listing\"); assert hasattr(data_store, \"set_viewing_scheduled\"); assert hasattr(data_store, \"mark_viewed\"); assert hasattr(data_store, \"save_negotiation_brief\"); assert hasattr(data_store, \"save_ku_enrichment\"); assert hasattr(data_store, \"write_checklist_ai\"); assert hasattr(data_store, \"record_price_in_data\"); assert hasattr(data_store, \"get_price_history\"); assert hasattr(data_store, \"get_rejected_by_reason\"); assert hasattr(data_store, \"get_approved_listing\"); assert hasattr(data_store, \"update_listing_coords\"); assert hasattr(data_store, \"get_listing_coords\"); assert hasattr(data_store, \"load_agent_state\"); assert hasattr(data_store, \"save_agent_state\"); assert hasattr(data_store, \"_pending_to_property\"); assert hasattr(data_store, \"load_pending\"); assert hasattr(data_store, \"add_property_if_new\"); assert hasattr(data_store, \"DEFAULT_APP_DATA\"); assert hasattr(data_store, \"DEFAULT_AGENT_STATE\"); print(\"OK: signatures preserved\")' && ! grep -v '^#' data_store.py | grep -q 'threading.RLock' && grep -q 'nullcontext' data_store.py && ! grep -v '^#' data_store.py | grep -qE '\\.viewing_history\\.append|row\\.cost_of_ownership\\[|row\\.ku\\[|row\\.checklist\\[' && grep -q '_LEGACY_ALIASES' data_store.py && grep -q 'def _dict_to_listing_fields' data_store.py && echo OK"</automated>
    <!-- Also required: a test in test_data_store.py that seeds a legacy-shaped dict (name/area/year/notes/price/pricePerSqm) via save_app_data and asserts the resulting row exposes the correct new-field values (title/area_sqm/year_built/description/price_eur/price_per_sqm) — added in Task 2's port scope (`test_save_app_data_applies_legacy_aliases_on_write_path`). See Task 2's action block. -->
  </verify>
  <done>All 23 public symbols preserved; `_lock` is `contextlib.nullcontext()`; no `threading.RLock` remains; no in-place JSONB mutation patterns detected in the rewritten file; every existing test in test_data_store.py / test_pending.py / test_viewing_workflow.py / test_price_intelligence.py passes under the db_session fixture; no caller file was modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port existing test files to db_session fixture — no assertion weakening</name>
  <files>
    app/tests/test_data_store.py,
    app/tests/test_pending.py,
    app/tests/test_viewing_workflow.py,
    app/tests/test_price_intelligence.py
  </files>
  <behavior>
    - Every test in these four files runs GREEN against the new DB backend.
    - Fixture substitutions only: replace `tmp_agent_state` with `db_session` (or accept both — `db_session` for DB state, `tmp_agent_state` for `settings.json` / `agent_state.json` when the test touches those files). The `client` fixture already depends on `tmp_agent_state`; where a test uses `client`, ADD `db_session` as an additional dependency so both are active.
    - Assertion bodies UNCHANGED. If a test asserts `data['pending'][0]['score'] == 80`, it still asserts `data['pending'][0]['score'] == 80` after the port — the compatibility shim in `load_app_data` guarantees the shape.
    - Where a test previously seeded state via `data_store.save_app_data({...})`, it now still calls `data_store.save_app_data({...})` — the shim upserts the equivalent rows.
    - Tests that seed `agent_state.json` directly (via `tmp_agent_state / "agent_state.json"`) STAY — that file is still JSON-backed per D-Discretion.
    - Test count MUST NOT decrease (before-port count == after-port count).
  </behavior>
  <read_first>
    - app/tests/test_data_store.py (all)
    - app/tests/test_pending.py (all)
    - app/tests/test_viewing_workflow.py (all)
    - app/tests/test_price_intelligence.py (all)
    - app/tests/conftest.py (from Wave 0 — `db_session` fixture; `tmp_agent_state` unchanged)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Assumptions Log A5 (test port should be mechanical — flag if any assertion needs restructuring)
  </read_first>
  <action>
    Four files. Mechanical port — do NOT rewrite test bodies; only adjust fixtures.

    Rules the executor MUST NOT violate:
    - Preserve every `def test_*` function; test count must not decrease.
    - Preserve every assertion verbatim. If an assertion no longer matches (e.g. the compatibility shim returns a slightly different dict shape), that is a data_store Task 1 bug — fix Task 1, do NOT weaken the assertion. This is the anti-degradation gate.
    - Add `db_session` as an explicit fixture parameter to every test that touches `data_store.*` (load/save/mutate). Tests that only touch `agent_state.json` (heartbeat, telegram_silenced_until, etc.) keep `tmp_agent_state` and do NOT need `db_session`.
    - Tests that use the `client` fixture inherit `tmp_agent_state` (per conftest.py:59); ADD `db_session` as an additional fixture parameter so the DB backing store is also active.
    - Where a test previously monkeypatched `config.APP_DATA_FILE` to a temp path, remove that monkeypatch — the compatibility shim now writes to the DB.
    - Where a test previously wrote a JSON file directly (bypassing data_store), rewrite it to call `data_store.save_app_data({...})` — the shim upserts equivalent rows.
    - Do NOT change test names, docstrings, or assertion bodies.

    Special case — `test_save_ku_preserves_manual` (test_data_store.py:84-113 or wherever it lives): this test guards RESEARCH § Pitfall 7. Port it faithfully; if it fails, Task 1's `save_ku_enrichment` implementation is wrong — fix Task 1.

    NEW required test — `test_save_app_data_applies_legacy_aliases_on_write_path(db_session)` (add to test_data_store.py):
    - Seeds a legacy-shaped dict:
      ```python
      legacy = {
          "properties": [{
              "id": "legacy-1",
              "name": "Vana korter Mustamäel",        # legacy alias for title
              "price": 210000,                          # legacy alias for price_eur
              "pricePerSqm": 3500,                      # legacy alias for price_per_sqm
              "area": 60.0,                             # legacy alias for area_sqm
              "year": 1975,                             # legacy alias for year_built
              "notes": "Kolm tuba, remonditud köök",   # legacy alias for description (or per main.py:197)
              "status": "approved",
          }],
          "pending": [],
          "rejected": [],
          "checklists": {},
          "price_history": {},
          "settings": {},
      }
      ```
    - `data_store.save_app_data(legacy)`.
    - Fresh session query: `row = db_session.get(Listing, "legacy-1")`.
    - Assert new-field values populated correctly:
      - `row.title == "Vana korter Mustamäel"` (from legacy `name`)
      - `row.price_eur == 210000` (from legacy `price`)
      - `row.price_per_sqm == 3500` (from legacy `pricePerSqm`)
      - `row.area_sqm == 60.0` (from legacy `area`)
      - `row.year_built == 1975` (from legacy `year`)
      - `row.description == "Kolm tuba, remonditud köök"` OR `row.notes == "Kolm tuba, remonditud köök"` (whichever main.py:197 maps `notes` to — read the file to confirm)
    - Assert NO legacy keys leaked into `extras`: `assert "name" not in (row.extras or {})`, `assert "price" not in (row.extras or {})`, `assert "pricePerSqm" not in (row.extras or {})`, `assert "area" not in (row.extras or {})`, `assert "year" not in (row.extras or {})`.
    - This test is the regression guard for the "aliases apply on write path, not just migration read" blocker. Failure means `_dict_to_listing_fields` did NOT apply `_LEGACY_ALIASES` — go fix Task 1.

    Special case — `test_setdefault_status_legacy` (Phase 6 test that writes a legacy JSON entry without `status` field and expects `load_app_data` to setdefault it to "approved"): in Wave 2 world, the migration script (Wave 3) is the setdefault equivalent; this test becomes obsolete in the DB world. DELETE it and log the deletion in the plan SUMMARY (Wave 3 test_migration.py covers the equivalent behaviour: `test_preserves_ku_manual` demonstrates the migration hot-fills defaults). If uncertain: keep the test and monkeypatch it to seed a Listing with `status=None` — but the Listing model has `default="pending"` so this will fail; document as an obsolete test and delete.

    Do NOT touch test_heartbeat.py, test_ingest.py, test_brief_generator.py, test_ku_lookup.py, test_commute.py, test_districts.py, test_ors_api.py, test_geocoding.py, test_eval_quality.py, test_listing_contract.py — these are covered in Waves 4/5 or do not touch data_store.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker/app && BEFORE=$(git show HEAD:app/tests/test_data_store.py app/tests/test_pending.py app/tests/test_viewing_workflow.py app/tests/test_price_intelligence.py 2>/dev/null | grep -c '^def test_'); AFTER=$(grep -c '^def test_' tests/test_data_store.py tests/test_pending.py tests/test_viewing_workflow.py tests/test_price_intelligence.py | awk -F: '{s+=$2}END{print s}'); test \"$AFTER\" -ge \"$BEFORE\" && echo OK"</automated>
  </verify>
  <done>Test count preserved (post-port count ≥ pre-port count minus any documented obsolete tests); every ported test uses `db_session` fixture where it touches persistence; no assertion body weakened; `test_save_ku_preserves_manual` still passes (Pitfall 7 guard); `cd app && pytest tests/test_data_store.py tests/test_pending.py tests/test_viewing_workflow.py tests/test_price_intelligence.py -x` — GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FastAPI route → data_store public function | Same as pre-Phase-7 — no new caller-side attack surface |
| data_store → Postgres | Private compose network; parameterised queries via SQLAlchemy 2.x |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-01 | Tampering | SQL injection via listing_id parameter | high | mitigate | Every data_store function passes `listing_id` through SQLAlchemy ORM (`db_.get(Listing, listing_id)` or `db_.query(Listing).filter(Listing.id == listing_id)`) — never `%` or f-string into SQL. Documented in RESEARCH § Known Threat Patterns. |
| T-07-02-02 | Information Disclosure | SQLAlchemyError stack trace leaks DSN to HTTP response | high | mitigate | Every DB call wrapped in `try / except SQLAlchemyError` with `log.exception(...)` + sentinel return per never-raise contract. No exception propagates to the FastAPI handler. |
| T-07-02-03 | Tampering | JSONB reassignment lapse silently drops mutations | medium | mitigate | Every JSONB-touching function reassigns the whole list/dict per Pitfall 1; automated verify greps for the in-place patterns (`row.viewing_history.append`, `row.cost_of_ownership[`, `row.ku[`, `row.checklist[`). |
| T-07-02-04 | Tampering | save_ku_enrichment loses manual notes | medium | mitigate | Preserves `existing.get("manual", "")` per RESEARCH § Pitfall 7 example verbatim; `test_save_ku_preserves_manual` in the ported test_data_store.py is the regression guard. |
| T-07-02-05 | Denial of Service | connection pool exhaustion via unbounded session opens | medium | mitigate | Every function uses `with SessionLocal() as db_:` context manager (auto-close); no session survives function exit. Wave 4 covers the still-outstanding daemon-thread pattern (session opened before HTTP call). |
| T-07-02-06 | Tampering | Legacy alias write path loses columns | medium | mitigate | `_dict_to_listing_fields` reads `_LEGACY_ALIASES` from main.py:197 verbatim; unknown keys land in `extras` JSONB so nothing is silently dropped. |
</threat_model>

<verification>
1. Signature preservation: `python -c "import data_store; [getattr(data_store, name) for name in ('_lock','load_app_data','save_app_data','add_to_pending','approve_listing','reject_listing','set_viewing_scheduled','mark_viewed','save_negotiation_brief','save_ku_enrichment','write_checklist_ai','record_price_in_data','get_price_history','get_rejected_by_reason','get_approved_listing','update_listing_coords','get_listing_coords','load_agent_state','save_agent_state','_pending_to_property','load_pending','add_property_if_new','DEFAULT_APP_DATA','DEFAULT_AGENT_STATE')]"` — no AttributeError.
2. `cd app && pytest tests/test_data_store.py tests/test_pending.py tests/test_viewing_workflow.py tests/test_price_intelligence.py -x` — all GREEN.
3. `cd app && pytest tests/test_db_smoke.py tests/test_models.py tests/test_alembic.py -x` — still GREEN (Wave 1 unregressed).
4. `cd app && pytest tests/test_migration.py -x` — still RED (Wave 3 target — unchanged).
5. `cd app && pytest tests/test_ingest.py tests/test_heartbeat.py tests/test_brief_generator.py tests/test_ku_lookup.py tests/test_commute.py tests/test_districts.py tests/test_ors_api.py tests/test_geocoding.py tests/test_eval_quality.py tests/test_listing_contract.py -x` — every test that touches `data_store.*` (via the preserved signatures) must still pass; Wave 4/5 will address any that fail for structural reasons (Pitfall-5 patterns / hot-caller optimizations).
6. `docker compose up -d --build && curl http://127.0.0.1:8000/api/pending && curl http://127.0.0.1:8000/api/health` — both endpoints return valid JSON. Data will be empty on a fresh volume (Wave 3 wires the migration script that populates from `app_data.json`).
</verification>

<success_criteria>
- Every public symbol in `data_store.py` preserved; no caller file modified in this wave.
- `_lock` is a `contextlib.nullcontext()` no-op.
- All existing tests in test_data_store.py, test_pending.py, test_viewing_workflow.py, test_price_intelligence.py pass under the DB backend.
- JSONB reassignment convention applied throughout — no `.append(...)` on `row.viewing_history` or similar.
- `save_ku_enrichment` preserves `manual` subkey (Pitfall 7 guard is green).
- `load_agent_state` / `save_agent_state` untouched — agent_state.json stays on disk.
</success_criteria>

<output>
Create `.planning/phases/07-database-migration/07-02-SUMMARY.md` when done.
</output>
