---
phase: 07-database-migration
plan: 01
type: execute
wave: 1
depends_on:
  - 07-00
files_modified:
  - app/db.py
  - app/models.py
  - app/config.py
  - app/alembic.ini
  - app/alembic/env.py
  - app/alembic/script.py.mako
  - app/alembic/versions/0001_initial_schema.py
  - app/entrypoint.sh
  - app/Dockerfile
  - docker-compose.yml
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-06

must_haves:
  truths:
    - "`docker compose up -d --build` on a fresh clone with POSTGRES_* set in .env produces both containers healthy, and `docker compose logs app` shows: `Postgres ready.` → `Applying Alembic migrations...` → `INFO [alembic.runtime.migration] Running upgrade -> 0001, initial schema` → `Starting uvicorn...`"
    - "`docker compose exec postgres psql -U aparts -d aparts_looker -c '\\dt'` returns `listings` and `alembic_version`"
    - "`docker compose exec postgres psql -U aparts -d aparts_looker -c 'SELECT typname FROM pg_type WHERE typname = ''listing_status''' -t` returns `listing_status`"
    - "`cd app && pytest tests/test_db_smoke.py tests/test_models.py tests/test_alembic.py -x` — 5 tests PASS (Wave 0 RED becomes GREEN); tests/test_migration.py stays RED (Wave 3 will flip it)"
    - "`GET /api/health` (existing endpoint) still returns `{\"ok\": true}` after the container starts — nothing in main.py has been touched yet"
  artifacts:
    - "app/db.py — engine + SessionLocal + Base + get_db (~40 lines)"
    - "app/models.py — Listing ORM class per D-03/D-04/D-05"
    - "app/config.py (modified) — POSTGRES_* + DATABASE_URL block appended"
    - "app/alembic.ini + app/alembic/env.py + app/alembic/versions/0001_initial_schema.py"
    - "app/entrypoint.sh — pg_isready loop → alembic upgrade head → placeholder for migrate_from_json (Wave 3 wires it) → uvicorn"
    - "app/Dockerfile (modified) — install postgresql-client for pg_isready; COPY entrypoint.sh; CMD runs it"
    - "docker-compose.yml (modified) — postgres service, postgres_data volume, app depends_on postgres healthy"
  key_links:
    - "docker-compose service_healthy → app entrypoint pg_isready loop (defence in depth); if this chain breaks, the app container restarts on every deploy"
    - "alembic/env.py `import models` — WITHOUT this Base.metadata is empty and every autogen revision is empty; RESEARCH § Pattern 4 warning"
    - "Listing.status ENUM values MUST include exactly the five values from Phase 6 (`pending`, `approved`, `rejected`, `viewing_scheduled`, `viewed`) — mismatch means Wave 3's migration script rejects legacy rows"
---

<objective>
Wave 1 — Database scaffold: SQLAlchemy engine, ORM model, Alembic baseline revision, Postgres container in docker-compose, entrypoint that waits-migrates-runs.

Purpose: Stand up the plumbing so Waves 2-5 can rewire callers. No caller code changes yet — `data_store.py` is untouched, all HTTP routes still hit JSON. This wave adds the DB as a parallel resource; the compatibility shim in Wave 2 is what actually cuts traffic over.

Output: A running Postgres container reachable from the app container by name `postgres:5432`, a `listings` table matching the SQLAlchemy metadata, Alembic version `0001` recorded, and 5 of the 7 Wave 0 RED tests turned GREEN.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-database-migration/07-CONTEXT.md
@.planning/phases/07-database-migration/07-RESEARCH.md
@.planning/phases/07-database-migration/07-00-SUMMARY.md

@app/config.py
@app/Dockerfile
@docker-compose.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/db.py + app/models.py + config.py POSTGRES_* block</name>
  <files>app/db.py, app/models.py, app/config.py</files>
  <behavior>
    - After task: `python -c "from db import engine, SessionLocal, Base; from models import Listing; print(Listing.__tablename__)"` (run inside `app/`) prints `listings`.
    - `Listing.__table__.columns.keys()` includes every column named in RESEARCH § Pattern 2 (id, url, title, name, district, address, notes, price_eur, price_per_sqm, area_sqm, rooms, year_built, material, energy_class, condition, floor, floor_total, parking, needs_renovation, broker_name, contact_email, image_url, image_count, description, lat, lng, commute_minutes, status, scheduled_at, queued_at, rejected_at, rejection_reason, removed, removed_at, score, verdict, raw_ok, tg_message_id, tg_chat_id, draft_subject, draft_body, cost_of_ownership, viewing_history, negotiation_brief, negotiation_brief_generated_at, ku, checklist, price_history, score_breakdown, ai_checklist_fills, strengths, concerns, risks, extras, created_at, updated_at).
    - Wave 0 tests `test_db_smoke.py::test_engine_connects` and `test_models.py::test_listing_roundtrip`, `test_models.py::test_status_enum_rejects_invalid`, `test_models.py::test_jsonb_mutation_requires_reassign` all pass.
    - Layered imports preserved (per CLAUDE.md): `db` imports only `config` + SQLAlchemy; `models` imports only `db` + SQLAlchemy; neither imports `main`/`ingest_handler`/`brief_generator`/`agent_job`/`data_store`.
  </behavior>
  <read_first>
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pattern 1 (db.py engine + SessionLocal + Base — copy verbatim shape)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pattern 2 (Listing ORM model — copy the full column list; every JSONB field name is load-bearing for Wave 2's compatibility shim)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 3 (MappedAsDataclass requires defaults on every field)
    - .planning/phases/07-database-migration/07-RESEARCH.md § `.env` additions + config.py addition (exact env-var names)
    - app/config.py (existing constants pattern to mirror — UPPERCASE, `os.environ.get(...)`)
    - app/data_store.py:26-63 (DEFAULT_APP_DATA / DEFAULT_AGENT_STATE — every top-level key here must have a column or JSONB blob in Listing)
  </read_first>
  <action>
    Three files. Take the RESEARCH.md examples as the canonical shape — the executor's job is to instantiate them, not re-derive them.

    1) app/db.py (new file) — SQLAlchemy engine, sessionmaker, declarative Base, FastAPI get_db dependency.
       - Module docstring per CLAUDE.md convention explaining that one engine lives per process, `SessionLocal()` opens a short-lived session, and daemon threads must call `SessionLocal()` themselves (never inherit `Depends(get_db)` per RESEARCH § Anti-Patterns).
       - `Base(MappedAsDataclass, DeclarativeBase)` — 2.x new-style API per D-02.
       - `engine = create_engine(config.DATABASE_URL, pool_pre_ping=True, future=True)` — `pool_pre_ping=True` matters (per D-02 research: survives pg restart on every deploy).
       - `SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False, expire_on_commit=False)` — `expire_on_commit=False` critical per RESEARCH § Pattern 1 explanation (otherwise attribute access after commit re-hits DB and breaks the never-raise contract).
       - `get_db()` generator that yields a session, closes on end (FastAPI dependency-injection shape per RESEARCH § Pattern 6). Not consumed in Phase 7 but shipped for future routes.
       - No `_lock` shim here — that lives in `data_store.py` for backward compat.

    2) app/models.py (new file) — the single `Listing` ORM class.
       - Module docstring naming D-03/D-04/D-05 as the design constraints; explains that JSONB reassignment convention (Pitfall 1) is required rather than MutableDict.
       - `LISTING_STATUS_VALUES = ("pending", "approved", "rejected", "viewing_scheduled", "viewed")` — MUST match Phase 6 status values verbatim (grep data_store.py:225-291 to confirm).
       - `class Listing(Base):` with every field from RESEARCH § Pattern 2. Every field has a `default=` or `default_factory=` per Pitfall 3.
       - `status: Mapped[str] = mapped_column(SAEnum(*LISTING_STATUS_VALUES, name="listing_status"), default="pending", index=True)` — `name="listing_status"` is required per RESEARCH § Pattern 3 for Postgres to emit `CREATE TYPE`.
       - `id: Mapped[str] = mapped_column(String(64), primary_key=True)` per D-05.
       - JSONB columns use `from sqlalchemy.dialects.postgresql import JSONB` (Postgres-specific type — not the generic `JSON`).
       - `created_at` / `updated_at` use `server_default=func.now()` / `onupdate=func.now()` so writes bypass Python and let Postgres timestamp.
       - Do NOT add `MutableDict.as_mutable(JSONB)` per RESEARCH § Pitfall 1 anti-pattern (false sense of safety).

    3) app/config.py (extend) — append the `# ---- Database (Phase 7) ----` block per RESEARCH § config.py addition. Five env-driven UPPERCASE constants (`POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `POSTGRES_HOST`, `POSTGRES_PORT`) plus the composed `DATABASE_URL` (with a `DATABASE_URL` override env-var respected — supports explicit connection strings for future dev/staging). Do NOT reorder or edit any existing constant. Do NOT log `DATABASE_URL` anywhere per RESEARCH § Security Domain (contains password).
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker/app && python -c 'from db import engine, SessionLocal, Base, get_db; from models import Listing, LISTING_STATUS_VALUES; assert Listing.__tablename__ == \"listings\"; assert LISTING_STATUS_VALUES == (\"pending\",\"approved\",\"rejected\",\"viewing_scheduled\",\"viewed\"); cols = set(Listing.__table__.columns.keys()); needed = {\"id\",\"status\",\"cost_of_ownership\",\"viewing_history\",\"negotiation_brief\",\"ku\",\"checklist\",\"price_history\",\"score\",\"district\",\"lat\",\"lng\"}; missing = needed - cols; assert not missing, missing; print(\"OK\")'"</automated>
  </verify>
  <done>app/db.py + app/models.py + config.py POSTGRES_* block exist; import chain works (`from db import ...; from models import Listing`); Listing has every column named in RESEARCH § Pattern 2; layered-import rule preserved (`grep -q '^import config' app/db.py && ! grep -q 'import main\\|import data_store\\|import ingest_handler' app/db.py app/models.py`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold Alembic + generate baseline revision 0001_initial_schema</name>
  <files>
    app/alembic.ini,
    app/alembic/env.py,
    app/alembic/script.py.mako,
    app/alembic/versions/0001_initial_schema.py
  </files>
  <behavior>
    - Wave 0 test `test_alembic.py::test_alembic_head_matches_metadata` passes: applying head to an empty Postgres produces schema that `compare_metadata(Base.metadata)` returns empty-diff for.
    - `cd app && alembic upgrade head` against a freshly-booted Postgres (Task 3 stack) creates the `listing_status` type and `listings` table with every column from `models.Listing`.
    - `cd app && alembic current` reports `0001 (head)` after the upgrade.
    - `cd app && alembic revision --autogenerate -m "no-op check"` (hypothetical follow-on) produces a revision file whose `upgrade()`/`downgrade()` bodies are effectively empty (only comment stubs) — proof that the baseline captures the full metadata.
  </behavior>
  <read_first>
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pattern 4 (Alembic env.py — the exact file to write; `import models` is load-bearing)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pitfall 4 (Postgres ENUM values immutable in ordinary migration — documented for future planners, not a Wave 1 concern)
    - app/models.py (from Task 1 — the source of truth for the schema Alembic must generate)
  </read_first>
  <action>
    Four files. Standard Alembic-with-SQLAlchemy layout per RESEARCH § Pattern 4.

    1) app/alembic.ini — minimal config; `script_location = alembic`; `sqlalchemy.url =` left blank (env.py overrides it from `config.DATABASE_URL`). File-based logging enabled per Alembic template; keep default `logger_alembic` at INFO level.

    2) app/alembic/env.py — per RESEARCH § Pattern 4 verbatim shape. Key points:
       - `sys.path.insert(0, os.path.abspath(os.path.dirname(__file__) + "/.."))` so `app/` is importable when Alembic runs from anywhere.
       - `import config as app_config`; `from db import Base`; `import models` — the `import models` line is LOAD-BEARING per RESEARCH § Pattern 4 (without it Base.metadata is empty, autogen produces empty revisions).
       - `alembic_config.set_main_option("sqlalchemy.url", app_config.DATABASE_URL)` — reads the same DSN as the app at runtime.
       - `target_metadata = Base.metadata`.
       - Both `run_migrations_offline` and `run_migrations_online` implementations per RESEARCH § Pattern 4 example.

    3) app/alembic/script.py.mako — standard Alembic template (produced automatically by `alembic init`); do NOT invent one. If `alembic init alembic` was not run in the executor environment, copy the mako template from Alembic 1.18.5's source. The template shape is stable and does not need editing.

    4) app/alembic/versions/0001_initial_schema.py — the baseline revision. Two acceptable approaches:
       (a) Run `alembic revision --autogenerate -m "initial schema"` against a running Postgres (spin up a throwaway container: `docker run --rm -e POSTGRES_USER=alembic -e POSTGRES_PASSWORD=x -e POSTGRES_DB=x -p 55432:5432 -d postgres:16-alpine`, wait for it, `DATABASE_URL=postgresql+psycopg://alembic:x@localhost:55432/x alembic -c app/alembic.ini revision --autogenerate -m "initial schema"`). This produces the file automatically; hand-audit it to confirm `CREATE TYPE listing_status` precedes `CREATE TABLE listings`.
       (b) Write the revision by hand mirroring `models.Listing.__table__.columns`. Only choose this if (a) fails.

       Regardless of path: the file MUST use `revision = "0001"` (not autogenerated hash — we own the numbering) and `down_revision = None`. The `upgrade()` body must issue `op.create_table("listings", ...)` with every column from `models.Listing` and `op.create_index("ix_listings_status", "listings", ["status"])` + any other indexes declared `index=True` on the model. The `downgrade()` body drops the table and then `op.execute("DROP TYPE listing_status")` (order matters — table must go first because it references the type).

    Alembic idiom reminders for the executor:
    - JSONB columns are `sa.dialects.postgresql.JSONB`, not `sa.JSON`.
    - ENUM columns are `sa.Enum(*LISTING_STATUS_VALUES, name="listing_status")` — the `name=` must match `models.Listing.status` per RESEARCH § Pattern 3.
    - Never invent Alembic versions later than `0001` in this wave; only the baseline lands here.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker/app && test -f alembic.ini && test -f alembic/env.py && test -f alembic/script.py.mako && ls alembic/versions/0001_*.py >/dev/null 2>&1 && grep -q 'import models' alembic/env.py && grep -q 'target_metadata = Base.metadata' alembic/env.py && grep -q 'listing_status' alembic/versions/0001_*.py && grep -q 'listings' alembic/versions/0001_*.py && echo OK"</automated>
  </verify>
  <done>alembic.ini + alembic/env.py + alembic/script.py.mako + alembic/versions/0001_initial_schema.py exist; `import models` and `target_metadata = Base.metadata` present in env.py; baseline revision references both `listing_status` type and `listings` table; Wave 0 test `test_alembic.py::test_alembic_head_matches_metadata` will pass under Task 3's stack.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: docker-compose Postgres service + entrypoint.sh + Dockerfile changes</name>
  <files>
    docker-compose.yml,
    app/entrypoint.sh,
    app/Dockerfile
  </files>
  <behavior>
    - `docker compose up -d --build` on the local repo, with `.env` containing POSTGRES_USER/PASSWORD/DB, produces both `app` and `postgres` containers reaching `healthy`.
    - `docker compose logs app` shows (in order):
      1. `Waiting for Postgres at postgres:5432...`
      2. `Postgres ready.`
      3. `Applying Alembic migrations...`
      4. `INFO [alembic.runtime.migration] Running upgrade -> 0001, initial schema`
      5. `Migrating legacy JSON if present (idempotent)...` — WAVE 3 will land the script; for WAVE 1 the placeholder line simply logs and exits 0 (Wave 3 will replace the placeholder with the real `python migrate_from_json.py` invocation).
      6. `Starting uvicorn...`
    - `docker compose exec postgres psql -U aparts -d aparts_looker -c '\dt'` returns `listings` and `alembic_version`.
    - `curl http://127.0.0.1:8000/api/health` (existing endpoint) returns `{"ok": true}` — main.py and all callers untouched, JSON persistence still active.
    - The existing `apartment_data` volume is preserved (still holds settings.json, agent_state.json, and pre-migration app_data.json).
  </behavior>
  <read_first>
    - .planning/phases/07-database-migration/07-RESEARCH.md § Pattern 5 (Container entrypoint — the exact bash to write)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Code Examples § Adding Postgres to docker-compose.yml (canonical service block)
    - .planning/phases/07-database-migration/07-RESEARCH.md § Assumptions Log A3 (5s healthcheck interval + 15s start_period should be enough for postgres:16-alpine cold start; bump to 30s if we ever observe restart loops)
    - docker-compose.yml (existing — preserve caddy service + apartment_data volume verbatim)
    - app/Dockerfile (existing — preserve every line above `CMD`)
    - .github/workflows/deploy.yml (context only — it runs `git pull && docker compose up -d --build`, so the entrypoint MUST be robust enough to survive a rerun)
  </read_first>
  <action>
    Three files.

    1) docker-compose.yml — add a `postgres` service, add the `postgres_data` volume, add `depends_on: postgres: condition: service_healthy` on the `app` service. RESEARCH § Code Examples § docker-compose is the exact template. Discipline points:
       - `image: postgres:16-alpine` per RESEARCH § Assumptions Log (~50 MB extra RAM per container is acceptable; A3 covers cold-start timing).
       - `environment:` block reads `${POSTGRES_USER}` / `${POSTGRES_PASSWORD}` / `${POSTGRES_DB}` from `.env` (already loaded by docker-compose via `env_file: .env` on the `app` service, but Postgres reads env directly — do NOT rely on `env_file` for the Postgres service; use the `environment:` block).
       - `volumes:` mounts `postgres_data:/var/lib/postgresql/data`.
       - `healthcheck: pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}` — interval 5s, timeout 5s, retries 10, start_period 15s per Assumption A3.
       - Do NOT expose port 5432 publicly (no `ports:` block on postgres — reachable only via compose network name `postgres`; RESEARCH § docker-compose comment "No ports: exposed — only reachable inside the compose network.")
       - `depends_on: postgres: condition: service_healthy` on the `app` service.
       - Preserve caddy service exactly. Preserve `apartment_data` volume (still holds settings.json, agent_state.json, and — after Wave 3 — the .pre-pg7 backup).
       - Add `postgres_data:` under `volumes:`.

    2) app/entrypoint.sh (new file, executable) — per RESEARCH § Pattern 5 verbatim.
       - `#!/usr/bin/env bash`
       - `set -e` (fail-fast per constraint 3 in the plan prompt — if any step fails, container restarts and the operator sees the log immediately).
       - `pg_isready` loop with 1-second sleep until Postgres accepts connections. RESEARCH offers a Python one-liner fallback (`python -c "import psycopg; psycopg.connect('$DATABASE_URL')"`) — the executor's call. Recommend: use `pg_isready` because it is faster and cleaner in the logs; add `postgresql-client` to the Dockerfile.
       - `echo "Applying Alembic migrations..."` then `alembic upgrade head` — Alembic reads `sqlalchemy.url` from `env.py` which reads `config.DATABASE_URL`, so no shell-level DSN needed.
       - `echo "Migrating legacy JSON if present (idempotent)..."` — Wave 1 SHIP: this line ONLY echoes (no `python migrate_from_json.py` yet). Wave 3 will change this line to invoke the real script. Do NOT stub a placeholder script; the wave-3 landing is what enables the invocation.
       - `echo "Starting uvicorn..."` then `exec uvicorn main:app --host 0.0.0.0 --port 8000` — `exec` matters so uvicorn becomes PID 1 and receives SIGTERM cleanly.

    3) app/Dockerfile (modified) — three additive changes:
       - `RUN apt-get update && apt-get install -y postgresql-client && rm -rf /var/lib/apt/lists/*` — for `pg_isready`. Add BEFORE `pip install -r requirements.txt` (cleaner layer caching).
       - `COPY entrypoint.sh /app/entrypoint.sh` + `RUN chmod +x /app/entrypoint.sh` — do NOT `COPY entrypoint.sh .` and rely on cwd; use the absolute path.
       - Replace `CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]` with `CMD ["/app/entrypoint.sh"]`.
       - Preserve every other line, including the `playwright install chromium --with-deps` (still needed for scraper-side legacy code paths in the same image).
       - **Verify `WORKDIR /app` is preserved** (already present at line 3 pre-Phase-7; do NOT remove — the plain `python migrate_from_json.py` invocation in entrypoint.sh depends on cwd being `/app`). If a refactor removes it, the entrypoint's `alembic upgrade head` and `python migrate_from_json.py` invocations both fail. Verification grep: `grep -q '^WORKDIR /app$' app/Dockerfile`.
  </action>
  <verify>
    <automated>bash -c "cd /Users/danieltjulinov/PycharmProjects/aparts-looker && grep -q 'image: postgres:16-alpine' docker-compose.yml && grep -q 'pg_isready' docker-compose.yml && grep -q 'condition: service_healthy' docker-compose.yml && grep -q 'postgres_data' docker-compose.yml && test -x app/entrypoint.sh && grep -q 'alembic upgrade head' app/entrypoint.sh && grep -q 'exec uvicorn' app/entrypoint.sh && grep -q 'postgresql-client' app/Dockerfile && grep -q '/app/entrypoint.sh' app/Dockerfile && grep -q '^WORKDIR /app$' app/Dockerfile && echo OK"</automated>
    <human-check>Run locally: `docker compose up -d --build`, wait 30s, then `docker compose logs app | grep -E 'Postgres ready|Applying Alembic|Running upgrade|Starting uvicorn'` — must show all four lines in order. Then `curl http://127.0.0.1:8000/api/health` must return `{"ok": true}`. Deferred to end-of-phase checkpoint (workflow.human_verify_mode=end-of-phase).</human-check>
  </verify>
  <done>docker-compose.yml has postgres service + volume; app entrypoint.sh executable + implements wait/migrate/run; Dockerfile installs postgresql-client and runs entrypoint.sh; existing caddy service + apartment_data volume preserved; `main:app` binding untouched (no route changes in Wave 1).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app container → postgres container | Private docker-compose network only; no external port |
| GitHub Actions runner → VPS | SSH key auth; runs `git pull && docker compose up -d --build` |
| `.env` on VPS → both containers | File permissions on `/opt/aparts-looker/.env`; never in git |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-01 | Information Disclosure | DATABASE_URL leak in logs / exceptions | high | mitigate | RESEARCH § Security Domain: never `log.error("connect failed: %s", DATABASE_URL)`; catch `SQLAlchemyError` broadly, log with `log.exception("DB call failed")` (no DSN in format string). Task 1 module docstring on `db.py` explicitly warns against logging DATABASE_URL. |
| T-07-01-02 | Information Disclosure | postgres container exposed publicly | high | mitigate | Task 3: no `ports:` block on postgres service in docker-compose.yml — reachable only via compose network name `postgres`. Verified by explicit comment in the file. |
| T-07-01-03 | Tampering | Alembic drift between environments | medium | mitigate | `alembic upgrade head` runs at every container start (entrypoint.sh); baseline revision checked into git; RESEARCH § Anti-Patterns explicitly forbids using `Base.metadata.create_all()` outside tests. |
| T-07-01-04 | Denial of Service | Postgres cold-start races app startup | medium | mitigate | Two-layer defence: docker-compose `depends_on: condition: service_healthy` gates app boot; entrypoint.sh has additional `pg_isready` loop (RESEARCH § Pattern 5 justification: "cheap defence in depth against a temporary network hiccup between the two containers"). |
| T-07-01-05 | Tampering | Postgres native ENUM value drift | low | accept | Only one value change is anticipated (adding `sold` in a future phase). RESEARCH § Pitfall 4 documents the hand-written Alembic op required. Not a Wave 1 concern. |
| T-07-01-SC | Tampering | postgres:16-alpine image supply chain | medium | mitigate | Official image from Docker Hub (Docker Official Images, PGDG maintained); pinned to `:16-alpine` tag (not `:latest`); no additional package installs on top. |
</threat_model>

<verification>
1. Static: `bash app/entrypoint.sh -n` OR `bash -n app/entrypoint.sh` — no syntax errors.
2. Compose validate: `docker compose config >/dev/null` — no config errors.
3. Fresh boot: `docker compose down -v && docker compose up -d --build` in a clean state; wait 45s; `docker compose ps` must show both containers `Up (healthy)`.
4. Alembic head: `docker compose exec app alembic current` must print `0001 (head)`.
5. Schema present: `docker compose exec postgres psql -U aparts -d aparts_looker -c '\dt'` must list `listings` and `alembic_version`.
6. Health route unchanged: `curl http://127.0.0.1:8000/api/health` → `{"ok": true}`.
7. Wave 0 tests turn GREEN: `cd app && pytest tests/test_db_smoke.py tests/test_models.py tests/test_alembic.py -x` — 5 tests pass. `tests/test_migration.py` stays RED (Wave 3 target).
8. Full suite still green outside the new files: `cd app && pytest -x --ignore=tests/test_migration.py`. Existing Phase 6 tests continue to pass because `data_store.py` is untouched (still JSON-backed).
</verification>

<success_criteria>
- Two containers healthy; app container's `depends_on: postgres: condition: service_healthy` gate satisfied.
- `listings` table + `listing_status` ENUM + `alembic_version` table exist in Postgres.
- Baseline revision `0001` is `head`.
- Wave 0 tests `test_db_smoke.py`, `test_models.py` (all 3), `test_alembic.py` — 5 tests PASS.
- Existing test suite (Phase 6) still passes — no regression in `data_store.py` or callers because Wave 1 does not touch them.
- Layered imports preserved: `db.py` imports `config` only; `models.py` imports `db` only.
</success_criteria>

<output>
Create `.planning/phases/07-database-migration/07-01-SUMMARY.md` when done.
</output>
