---
phase: 07-database-migration
plan: 06
type: execute
wave: 6
depends_on:
  - 07-05
files_modified: []
autonomous: false
requirements:
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - DB-05
  - DB-06
  - DB-07
  - DB-08

must_haves:
  truths:
    - "End-to-end deploy-flow verification: `docker compose down && docker compose up -d --build` produces both containers healthy; migration runs cleanly; app answers `GET /api/health`"
    - "Data survives container restart — a listing added post-migration is still present after `docker compose restart app`"
    - "Approve/reject/schedule-viewing/mark-viewed flow works end-to-end through the web UI + Telegram integration"
    - "Backup file `app_data.json.pre-pg7` present on the VPS volume (evidence the migration ran on the real dataset)"
    - "Full pytest suite green: `cd app && pytest -x` — 0 failures"
    - "User confirms the settings save feels instant (< 1s), a marked improvement over pre-Wave-5 latency"
  artifacts: []
  key_links:
    - "This is the phase-exit gate — everything below runs on the LIVE VPS via `git push` → GitHub Actions → `docker compose up -d --build`"
    - "Rollback plan: if the deploy is broken, `docker compose down && git checkout <pre-phase-7-tag> && docker compose up -d --build` restores the JSON world; `app_data.json.pre-pg7` still exists so `mv app_data.json.pre-pg7 app_data.json` re-enables the old code path"
---

<objective>
Wave 6 — End-of-phase manual verification checkpoint. Runs after all code + docs are committed; before the phase is marked complete.

Purpose: Confirm the migration actually landed on the production VPS, the app responds correctly, the historical dataset is intact, and the settings-save latency improvement is real. Per `.planning/config.json` `workflow.human_verify_mode = end-of-phase` — Waves 0-5 defer their human-check gates to this consolidated checkpoint.

Output: A single blocking-human checkpoint whose approval signals Phase 7 complete.
</objective>

<execution_context>
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/workflows/execute-plan.md
@/Users/danieltjulinov/PycharmProjects/aparts-looker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-database-migration/07-CONTEXT.md
@.planning/phases/07-database-migration/07-RESEARCH.md
@.planning/phases/07-database-migration/07-00-SUMMARY.md
@.planning/phases/07-database-migration/07-01-SUMMARY.md
@.planning/phases/07-database-migration/07-02-SUMMARY.md
@.planning/phases/07-database-migration/07-03-SUMMARY.md
@.planning/phases/07-database-migration/07-04-SUMMARY.md
@.planning/phases/07-database-migration/07-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: End-to-end phase verification (production deploy + smoke tests)</name>
  <files>(no repo file changes — end-of-phase manual verification)</files>
  <action>Execute the how-to-verify blocks A-D below in order; this task modifies no repo files.</action>
  <what-built>
    Waves 0-5 landed:
    - Wave 0: Test scaffolds + pytest-postgresql fixture + deploy prerequisites
    - Wave 1: SQLAlchemy engine + Listing model + Alembic baseline + Postgres container in docker-compose + entrypoint.sh
    - Wave 2: data_store.py rewired against Postgres; public API preserved
    - Wave 3: migrate_from_json.py + entrypoint invocation
    - Wave 4: Pitfall-5 conversions (brief_generator, ingest_handler, 5 main.py daemon-thread endpoints)
    - Wave 5: Hot-caller optimizations (settings recompute, backfill-costs, backfill-commutes, geocode-backfill, delete_all_listings, cost_override / reset)

    The app now persists to Postgres 16 via SQLAlchemy 2.x + Alembic. `settings.json` and `agent_state.json` remain filesystem-backed. The `_lock` symbol survives as a `contextlib.nullcontext()` no-op inside `data_store.py` but no caller uses it anymore.
  </what-built>
  <how-to-verify>
    Full end-to-end verification. Run each block in order; DO NOT skip.

    ═══ Block A — Local test suite (before pushing to VPS) ═══

    1. From the repo root: `cd app && pytest -x --tb=short 2>&1 | tail -20`
       Expected: `passed` with 0 failures. If any test fails, DO NOT DEPLOY — go back to the wave that owns that test.

    2. Local docker check: `docker compose down -v && docker compose up -d --build`
       Wait 60 seconds. Then: `docker compose ps` — both `app` and `postgres` containers must show `Up (healthy)`.

    3. Local log inspection: `docker compose logs app | grep -E 'Postgres ready|Applying Alembic|Running upgrade|Migrating legacy|Starting uvicorn'`
       Expected: all 5 log lines present in order. If `Migrating legacy JSON if present...` is followed by `No <path> — nothing to migrate` (because you cleaned the volume), that is CORRECT — first-boot on empty volume.

    4. Local health: `curl -s http://127.0.0.1:8000/api/health | jq .`
       Expected: `{"ok": true}` (or the existing shape).

    5. Local schema check: `docker compose exec postgres psql -U aparts -d aparts_looker -c '\dt'`
       Expected: `listings` and `alembic_version` tables present.

    6. Local seed + round-trip: `curl -X POST http://127.0.0.1:8000/api/ingest -H 'Authorization: Bearer <INGEST_TOKEN>' -H 'content-type: application/json' -d '{"listings":[{"id":"local-test-1","url":"https://kv.ee/test","title":"Local test","price_eur":200000,"area_sqm":60,"rooms":3,"image_count":10,"description":"test"}]}'` then `curl -s http://127.0.0.1:8000/api/pending | jq .`
       Expected: 1 pending item with `id: "local-test-1"`.

    7. Local restart: `docker compose restart app`. Wait 20 seconds. Re-run curl from step 6 (GET only). Expected: `local-test-1` still present. This proves persistence survives container restart.

    ═══ Block B — Production deploy ═══

    8. Confirm `.env` on VPS has POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB set (Wave 0 checkpoint should have handled this — verify once more): `ssh root@46.62.152.9 "grep -E '^POSTGRES_(USER|PASSWORD|DB)=' /opt/aparts-looker/.env | wc -l"` → should return `3`.

    9. Push the branch: `git push origin main`. GitHub Actions triggers `docker compose up -d --build` on the VPS.

    10. Monitor GitHub Actions: watch the job complete (green). If red, read the log and diagnose before continuing.

    11. On the VPS: `ssh root@46.62.152.9 "cd /opt/aparts-looker && docker compose logs app --tail 100"` — look for the same 5 log lines (Postgres ready / Applying Alembic / Running upgrade -> 0001 / Migrating legacy / Starting uvicorn). This time, `Migrating legacy JSON` should be followed by `Loading N properties + M pending + K rejected` where N/M/K match the actual dataset size, then `Migrated OK. Renamed app_data.json → app_data.json.pre-pg7`. If N == 0, the migration didn't find the source file — check volume mount and file path.

    12. Backup file present: `ssh root@46.62.152.9 "docker compose exec app ls -la /app/data/app_data.json.pre-pg7"` — file exists.

    13. Public health: `curl -s https://<production-URL>/api/health` → `{"ok": true}`.

    14. Data survived: `curl -s https://<production-URL>/api/pending` and `curl -s https://<production-URL>/api/entries` (or whatever the main-list endpoint is). Row counts should match what you saw pre-migration.

    ═══ Block C — User workflow smoke tests ═══

    15. Open the web dossier in a browser. Confirm:
        - Overview shows the expected number of listings.
        - Clicking a pending listing shows the detail panel.
        - Clicking "Approve" moves the listing to the approved-tier.
        - Clicking "Reject" with a reason moves it to rejected.
        - Clicking "Schedule viewing" opens the datetime picker + saves.
        - Clicking "Mark viewed" flips the status.
        - Clicking "Regenerate brief" triggers the daemon; wait 5s; brief appears.
        - Clicking "Refresh KÜ" triggers the daemon; wait 10s; KÜ card appears (if the address has a KÜ).
        - Manual `manual` notes previously added to `ku` (if any) SURVIVED the migration.

    16. Telegram smoke test: `curl -X POST https://<production-URL>/api/check-now`. Wait for the scheduled tick to fire. Confirm a new pending card arrives in Telegram with inline [Approve] [Reject] [More] buttons. Click one; confirm the state transition happens.

    17. Settings save latency test:
        - Time the setting-save endpoint: `time curl -X POST https://<production-URL>/api/settings -H 'content-type: application/json' -d '{"cost_interest_pct": 4.5}'`
        - Expected: < 1 second, even on the full production dataset. Was 3-8 seconds pre-Wave-5.

    ═══ Block D — Answers required ═══

    Please answer explicitly:
    - [ ] All Block A steps green?
    - [ ] All Block B steps green?
    - [ ] Web UI workflows in Block C intact?
    - [ ] Telegram flow in Block C intact?
    - [ ] `ku.manual` notes preserved through migration?
    - [ ] Settings save observably faster than pre-Phase-7?
    - [ ] Backup file `app_data.json.pre-pg7` present on VPS volume?

    ═══ Проверьте до подтверждения ═══

    Выполните все шаги Block A-D. Если что-то не работает — не подтверждайте; опишите проблему. Каждая проблема — это регрессия, за которую отвечает конкретная волна (Wave 0-5); откатите её PR-коммит или заведите gap-plan через `/gsd-plan-phase 7 --gaps`.
  </how-to-verify>
  <verify><human-check>All Block A-D checklist items pass on the live production VPS deploy.</human-check></verify>
  <done>Operator has responded "approved" after verifying every Block A-D item; production VPS is running Phase 7 code; historical dataset migrated; app_data.json.pre-pg7 backup file present.</done>
  <resume-signal>Type "approved" once all Block A-D items pass. If any item fails, describe the failure and which wave's changes likely caused it — the orchestrator will route to gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer laptop → VPS | SSH; `git push` triggers GitHub Actions |
| Production Postgres → app | Private compose network |
| User browser → HTTPS to Caddy | Caddy Basic Auth in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06-01 | Repudiation | Missed regression not caught before deploy | high | mitigate | Block A test suite is the hard gate — if `pytest -x` shows any failure, do NOT push to VPS. |
| T-07-06-02 | Denial of Service | Deploy leaves app in restart loop | high | mitigate | Rollback plan documented in must_haves.key_links — `git checkout <pre-phase-7-tag>` + `docker compose up -d --build` restores the JSON world; `app_data.json.pre-pg7` remains available for `mv` back to `app_data.json`. |
| T-07-06-03 | Tampering | Migration ran twice (rows duplicated) | high | mitigate | Wave 3 idempotency guards (file rename + db.merge upsert). Block B step 11 checks the log line `Migrated OK. Renamed...` — if this line is missing, migration did not actually complete and Block B step 12 confirms via file existence. |
| T-07-06-04 | Information Disclosure | Manual notes (`ku.manual`) lost | high | mitigate | Wave 3 migration script preserves the whole `ku` dict including `manual` subkey; Wave 2 `save_ku_enrichment` preserves it on subsequent refresh (Pitfall 7). Block C step 15 explicitly verifies `ku.manual` presence. |
| T-07-06-05 | Denial of Service | Postgres backup gap | high | accept | Explicitly deferred per D-06 in CONTEXT.md. Postgres volume is the sole copy; data-loss risk on server disk failure accepted. Recommend surfacing as Phase 8 candidate immediately after Phase 7 ships. |
</threat_model>

<verification>
This wave IS the verification. All prior waves defer their human-check gates to this consolidated checkpoint per `workflow.human_verify_mode = end-of-phase`. There are no additional automated verifications beyond the ones listed inside `how-to-verify` above.

The single automated check the executor should run before presenting the checkpoint:
- `cd app && pytest -x --tb=line 2>&1 | tail -5` — must show `passed`.
</verification>

<success_criteria>
- User approves the Block A-D checklist.
- Production VPS deploy succeeded (GitHub Actions green).
- Historical dataset migrated with `app_data.json.pre-pg7` backup file present.
- All web + Telegram workflows still functional.
- `ku.manual` notes preserved.
- Settings save latency observably improved.
- Phase 7 exits complete; ROADMAP.md Phase 7 marked `[x]`; per `commit_docs: true` a phase-completion doc commit is made.
</success_criteria>

<output>
Create `.planning/phases/07-database-migration/07-06-SUMMARY.md` when done. This SUMMARY should include:
- User's `approved` signal + timestamp.
- Notes on any manual intervention needed during the deploy (e.g. `.env` mismatch, container restart).
- The observed row count from the migration log (Block B step 11).
- Confirmation that Phase 7 → done; ROADMAP.md updated to `[x]`.
- A pointer to backup deferred work (Phase 8 candidate) per RESEARCH § Deferred Ideas.
</output>
